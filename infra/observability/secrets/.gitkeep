# Place a file named `scrape_token` here for the local Prometheus overlay.
# It must contain ONLY the token value and must match METRICS_SCRAPE_TOKEN in .env.
# This directory is gitignored for the token file — never commit a real token.
