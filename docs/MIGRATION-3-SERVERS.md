# Перенос флота на три сервера (A + B + маршрутизатор)

Черновик плана. Финализируется после появления серверов.

## Что переносим

Единственное постоянное состояние сервиса — **Postgres**. Это установлено измерением:

- `redis` содержит только счётчики частоты запросов (`rl:*`) — реплицировать не нужно,
  при переключении они обнуляются без последствий;
- файлов на диске нет: медиа хранится ссылками у провайдера, документы чата — текстом
  в БД, вложения приходят в теле запроса;
- постоянных томов ровно два на инстанс: `pgdata` и `redisdata`;
- конфигурация (`.env`, `.secrets`, `certs/appstore`) копируется один раз и обновляется
  при изменении, а не непрерывно.

**Объём данных всего флота — около 2.5 ГБ.** Перенос данных не является узким местом.

## Топология

Оба мощных сервера несут ВСЕ 27 инстансов, но активная нагрузка разведена крест-накрест:
половина инстансов основные на A и резервные на B, вторая половина — наоборот.

В обычном режиме каждая машина несёт вдвое меньше нагрузки, чем сегодня. При отказе
выживший берёт все 27 — это ровно сегодняшняя нагрузка, которая работает.

## Связность: A и B у разных провайдеров

Приватной сети между разными провайдерами не существует, поэтому серверы связываются
шифрованным туннелем (WireGuard). Он даёт постоянную частную адресацию независимо от
провайдера, и Postgres слушает репликацию ТОЛЬКО на интерфейсе туннеля — наружу порт
базы не открывается никогда.

**Разные провайдеры выбраны сознательно.** Отказы 2026-08-24, 2026-08-25 и 2026-08-26
не имели внутренних причин: машина останавливалась на уровне гипервизора. Общий провайдер
у A и B воспроизвёл бы ровно этот класс отказа на обоих серверах одновременно.

Требование к размещению: все узлы в одном регионе. Задержка между провайдерами влияет на
отставание репликации и добавляется к каждому запросу через маршрутизатор.

## Репликация

Потоковая репликация Postgres, не периодическая выгрузка. Причины:

1. отставание — доли секунды вместо интервала расписания; при отказе не теряются
   покупки, списания кредитов и переписка;
2. постоянная нагрузка минимальна — 27 периодических выгрузок дали бы ровно ту дисковую
   нагрузку, из-за которой сервер падал 2026-08-24.

## Переключение

Резервный Postgres работает в режиме чтения; чтобы принять трафик, его повышают до основного.

**Обязательная защита:** вернувшийся после аварии сервер НЕ должен возобновлять приём
записей — иначе базы разойдутся необратимо. Возврат к исходному распределению делается
вручную, пересборкой вернувшегося сервера как резервного.

## Маршрутизатор

Третий сервер завершает TLS для всех доменов и проверяет живость по `/ready` — точке,
которая уже используется как healthcheck контейнера и отвечает только при готовности
приложения. Прикладные серверы отдают HTTP внутри приватной сети.

### Адрес клиента через маршрутизатор

Маршрутизатор — HTTP-прокси, а не L4-туннель, поэтому он проставляет `X-Forwarded-For` с
реальным адресом клиента. Но приложение доверяет этому заголовку **только от известного
прокси**, и в списке доверия по инерции остался адрес прежней схемы. Разбор, последствия и
защита от повторения — [07-deployment.md §Биндинг и доступ](07-deployment.md#биндинг-и-доступ)
(инцидент 2026-08-30 на `velunixa`).

Коротко: пока туннельной сети не было в `TRUSTED_PROXY_IPS`, за клиента принимался **сам
маршрутизатор**, все пользователи инстанса схлопывались в один адрес и делили лимиты «на IP».
Значение задано в [`docker-compose.fleet.yml`](../docker-compose.fleet.yml), а не в `.env`
инстансов: оно следует из этой топологии, а не из настроек отдельного инстанса.

### Отказ самого маршрутизатора

Внешний балансировщик (Cloudflare) отклонён владельцем, поэтому вход остаётся своим — и
без дополнительной меры он становится единственной точкой отказа, ради устранения которой
вся схема и строится.

Мера: **конфигурация маршрутизатора разворачивается на всех трёх узлах** — на C как
рабочая, на A и B как запасные входы. В DNS публикуется только C, но при его отказе
A-записи переключаются на A или B, где всё уже поднято и проверено.

Это превращает отказ входного узла из простоя «до аренды новой машины» в простой на
время распространения DNS. Отсюда требование: **TTL A-записей 60 секунд постоянно**, а не
только на время переезда.

Конфигурация всех трёх входов генерируется из одного источника — реестра инстансов
`docs/07-deployment.md`, — иначе запасной вход разойдётся с рабочим и не выручит в тот
единственный момент, когда понадобится.

## Сертификат нового домена

A-запись домена должна указывать на маршрутизатор **до** того, как домен попадёт в
конфигурацию маршрутизатора. Traefik пробует выпустить сертификат в момент появления роутера;
если домена в тот момент нет в DNS, Let's Encrypt отвечает NXDOMAIN, попытка проваливается — и
сама больше не повторяется. Инстанс при этом выглядит рабочим: маршрутизация есть, `/ready` и
`/docs` открываются, но отдаётся самоподписанный `TRAEFIK DEFAULT CERT`.

Если так уже вышло, перезапускать маршрутизатор не нужно — достаточно заставить провайдер
перечитать конфигурацию, и попытка пойдёт заново:

```
ssh srvR "cd /opt/router/dynamic && cp dynamic.yml /tmp/d && mv /tmp/d dynamic.yml"
```

Проверяется издателем сертификата, а не кодом ответа (`verify.sh`, раздел СЕРТИФИКАТЫ): по коду
подмена не видна — `curl` без `-k` просто не соединяется, а с `-k` соединяется молча.

## Порядок работ

1. Туннель WireGuard между A, B и маршрутизатором; репликация только через него.
2. Развернуть флот на A и B, данные перенести с текущего сервера.
3. Поднять репликацию в обе стороны по распределению.
4. Настроить маршрутизатор, проверить его по IP до переключения DNS.
5. Снизить TTL записей до 60 секунд заранее — за сутки; оставить таким постоянно.
6. Переключить A-записи (действие владельца).
7. Проверить каждый инстанс, затем учебное переключение на резерв.


---

## Состояние на 2026-08-27

### Машины

| | адрес | ядер | память | ОС | состояние |
|---|---|---|---|---|---|
| A | 87.239.135.218 | 6 | 7.5 ГБ | CentOS Stream 9 | **sshd не отвечает** |
| B | 65.21.144.39 | 8 | 15.2 ГБ | Ubuntu 26.04 | готов |
| R | 2.29.30.63 | 2 | 3.7 ГБ | Ubuntu 26.04 | готов |

**A стоит в той же сети /24, что и отказывающий прежний сервер** (87.239.135.154). Это
противоречит цели «разные провайдеры»: инцидент у этого провайдера положит и A, и прежний
сервер одновременно. B и R — Hetzner, Хельсинки, задержка между ними 1.3 мс.

Память A (7.5 ГБ) меньше, чем занимает флот (замер: 15.6 ГБ при четырёх воркерах, ~9.6 ГБ
при двух). Владелец сообщил, что ОЗУ будет добавлено.

### Сделано

- Docker на всех трёх; WireGuard-туннель B↔R поднят (1.3 мс), участник A заведён заранее.
- На B: репозиторий, 27 каталогов инстансов с маркерами ролей, swap 8 ГБ, страж ролей.
- На R: Traefik со всеми 26 подтверждёнными доменами; таблица ролей; запасной вход на B.
- CI переведён на цикл по двум серверам с учётом роли (на резерве нет ни миграции, ни api).
- **Сквозной путь проверен на живом инстансе:** интернет → маршрутизатор → туннель →
  инстанс → запись в Postgres и чтение обратно. Отсечение проверено в обе стороны:
  понижение роли останавливает api и переводит маршрутизатор на 503, повышение возвращает 200.
- Порты инстансов из интернета закрыты (проверено): api и postgres слушают только туннель.

### Ждёт

1. **A недоступен.** Порт 22 принимает соединение, но SSH-баннер не приходит; ICMP работает.
   Снаружи не чинится — нужна перезагрузка из панели провайдера.
2. **Прежний сервер (87.239.135.154) не поднят.** Без него нет ни `.env` инстансов с ключами
   провайдеров, ни `.secrets` с ключами подписи токенов, ни самих данных. Наполнить инстансы
   до его возвращения невозможно.
3. **Секрет `SSH_HOST`** в GitHub должен указывать на новые серверы.
4. **Домен avelyra** — `avelyra.shop` ведёт на чужой хостинг; настоящее значение в `.env`
   упавшего сервера.


---

## Что произойдёт, когда прежний сервер вернётся

**Заморозка сработает сама, в течение 30 секунд.** На маршрутизаторе стоит таймер
`auto-freeze.timer`: он опрашивает прежний сервер и при первом же ответе останавливает `api`
и `redis` наших инстансов, оставляя `postgres`. Метка `/opt/router/.old-frozen` не даёт
сработать повторно.

Автоматика здесь не роскошь: после загрузки 240 контейнеров стартуют одновременно, и окно
между «поднялся» и «упал снова» измеряется минутами. Дожидаться в нём реакции человека нельзя.

Чужие проекты не трогаются — список инстансов в скрипте явный, а не выведенный из файловой
системы: на машине 240 контейнеров, наших из них 83.

### Дальше — одна команда на маршрутизаторе

    /opt/router/fleet/migrate-all.sh

Она делает всё по порядку: заморозку (если ещё не сделана), перенос каждого инстанса на тот
сервер, где он основной по таблице ролей, постановку репликации на соседа и сводку.

Порядок менять нельзя. Репликация ставится ТОЛЬКО после того, как данные на месте: поднять
резерв на пустой базе значит получить пустую реплику и потерять единственный экземпляр данных
при первом же переключении.

### Затем — смена A-записей

Все домены из `domains.txt` переводятся на маршрутизатор `2.29.30.63`, TTL 60 секунд.

Сразу после распространения записей на маршрутизаторе выполняется:

    /opt/router/enable-tls.sh

Скрипт сам сверяет, что записи уже указывают на него, и отказывается работать, если нет.

## Порядок команд целиком

| Когда | Где | Команда |
|---|---|---|
| прежний сервер вернулся | сам | (автоматически) `auto-freeze.timer` |
| A вернулся | A | `bootstrap-server.sh A <ключ_wg>` |
| данные | маршрутизатор | `fleet/migrate-all.sh` |
| проверка | маршрутизатор | `fleet/verify.sh` |
| **смена A-записей** | **регистратор** | **все домены → 2.29.30.63** |
| сертификаты | маршрутизатор | `enable-tls.sh` |
| проверка | маршрутизатор | `fleet/verify.sh` |
| учебное переключение | маршрутизатор | `fleet/promote.sh --all-from A` |


## Проверено на живых серверах (2026-08-26)

Между B и маршрутизатором — тот же сетевой путь, что будет между A и B.

**Репликация.** Базовая копия через туннель (32 МБ), поток запущен, отставание 0 с.
Запись на основном появилась на резерве за 3 секунды. Повышение резерва: база вышла из
режима восстановления, данные целы, запись принимается.

**Переключение маршрутизатора.** При живом основном трафик идёт на него (проверено по
логам обоих экземпляров). После остановки основного: первая попытка 502, вторая через
5 секунд — 200, обслужил резерв.

**Возврат.** Как только основной оживает, Traefik возвращает трафик ему автоматически.

Последнее — самое важное для понимания схемы. Для обычного веб-сервера это правильное
поведение, но при отказе с репликацией — опасное: вернувшийся сервер несёт данные на
момент своей смерти. Поэтому автоматический возврат нейтрализуется двумя мерами:

1. `promote.sh` перезаписывает конфигурацию маршрутизатора — после повышения основным
   значится ПОВЫШЕННЫЙ сервер, и возвращаться некуда;
2. страж ролей на вернувшемся сервере останавливает его `api`; остановленный контейнер
   при политике `unless-stopped` не поднимается и после перезагрузки.

Остаточный риск: если повышение НЕ состоялось (сервер отсутствовал недолго), его
контейнеры при загрузке стартуют сами — и это верно, он и остаётся основным. Окно между
стартом контейнеров и первой сверкой ролей сокращено до 15 секунд.


---

## Перенос выполнен (2026-08-27)

Прежний сервер поднялся в 06:43, немедленно после этого его инстансы остановлены (load
упал с 17.9 до 12.1), сняты все данные, машина больше не нужна.

**Итог: 27 инстансов из 27, 23 392 пользователя, расхождений ноль.** Число пользователей
сверено по каждому инстансу отдельно — при несовпадении `api` не поднимался бы.

Порядок работ был изменён на ходу ради скорости: сначала сняты ВСЕ дампы на диск нового
сервера (247 МБ), и только потом шло восстановление. Причина — машина падала пять раз за
четверо суток, и держать её в зависимости на всё время восстановления было нельзя.

Домен `avelyra` выяснился из его `.env`: **avelyraweb.shop** (2882 пользователя).
`avelyra.shop` принадлежит чужому хостингу и к сервису отношения не имеет.

### Временное отступление от схемы

**Все 27 инстансов сейчас основные на B.** Сервер A так и не отдал SSH, а данные нужно
было снимать немедленно. B это несёт спокойно: 82 контейнера, load 2.7 при восьми ядрах,
9 ГБ памяти из 15.

Разведение на два сервера и репликация делаются после оживления A — через
`replication.sh` и `promote.sh`, без простоя. Целевое распределение сохранено в
`instances.tsv` этого репозитория; на маршрутизаторе временно проставлено `B` у всех.

### Генерация не работает по внешней причине

Чат отвечает `upstream_error`. В логах: `credit_balance_exhausted` — **на счету OpenAI
закончились деньги**. Оба ключевых слота отработали переключение и упёрлись в одно и то
же, значит дело в аккаунте, а не в ключе. К переезду отношения не имеет: регистрация,
кошелёк, настройки и вся база работают.
