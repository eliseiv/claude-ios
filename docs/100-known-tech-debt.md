# 100 — Known Tech Debt

Реестр осознанно принятого технического долга. Формат `TD-NNN`.

| ID | Описание | Причина | Влияние | Триггер закрытия |
|---|---|---|---|---|
| TD-001 | Append-only `audit_logs` обеспечивается только на уровне приложения (нет DB-триггера/REVOKE на UPDATE/DELETE). | Простота на старте. | Теоретическая возможность изменить аудит при ошибке/злоупотреблении прямым доступом к БД. | При требованиях комплаенса — добавить REVOKE UPDATE/DELETE + write-only роль. |
| TD-002 | Контекст сессии для Claude реконструируется из `chat_steps` на каждый шаг (нет отдельного кэша собранного prompt). | Достаточно для старта; prompt caching Anthropic снижает стоимость. | Доп. чтение БД на каждый шаг tool-loop. | При росте латентности p95 — кэш собранного контекста в Redis. |
| TD-003 | Rate limiting и idempotency хранятся в Redis без репликации между регионами. | Single-region на старте. | При мульти-регионе потребуется пересмотр. | При выходе в multi-region. |
| TD-004 | Дефолтные rate limits и size-лимиты — эвристические (Q-003-1/Q-003-2), не калиброваны на реальной нагрузке. **Подпункт TD-004a (ADR-020, pypdf CPU-guard):** `_check_pdf_pages` (`src/app/chat/attachments.py`) парсит PDF через `pypdf.PdfReader` на декодированных байтах (до `ATTACHMENT_MAX_BYTES_DOCUMENT` = 8 MB) синхронно, без таймаута/изоляции. Структурно-злонамеренный PDF (раздутый/зацикленный xref, аномальная структура при малом числе страниц) способен вызвать CPU-spike в воркере на парсинге **до** проверки числа страниц. Риск ограничен: вход ≤ 8 MB (cap до парсинга), `ATTACHMENT_PDF_MAX_PAGES`=100 / `ATTACHMENT_MAX_COUNT`=10, парсинг только структуры (без полного рендера). Не эксплуатируется как amplification на MVP-объёме. | Нет прод-данных. Парсинг pypdf синхронный, в рамках лимитов размера/числа. | Возможны ложные `429`/`413` или недостаточная защита; **TD-004a:** локальный CPU-spike воркера на злонамеренном PDF (DoS-вектор ограничен 8 MB-cap и max_count=10). | После сбора прод-метрик откалибровать size/rate-лимиты. **TD-004a:** при появлении прод-метрик деградации на PDF-парсинге — обернуть `_check_pdf_pages` таймаутом/изоляцией (отдельный пул/процесс или `asyncio` timeout с прерыванием), либо ужесточить document-cap. |
| TD-005 | **Закрыт (2026-06-02), ревизован (2026-06-02 → [ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)).** Финальная deploy-схема = **общий сервер за внешним Traefik + GitHub Actions SSH** (заменяет прежнюю VPS+Caddy-standalone). Сервис в `/opt/<service>` на общем Linux-сервере (`87.239.135.154`); reverse-proxy и TLS/ACME — внешний Traefik (`/opt/edge`), не наш стек. Наш `docker-compose`: `api` (`expose: 8000`, без `ports:` 80/443, сети `web` external + `default`, Traefik-labels), `postgres`/`redis` только в `default` без портов. Выкатка — GitHub Actions → SSH → `git pull --ff-only` + явные `build` / `run --rm migrate` / `up -d --no-build` + readiness-gate на сервере (образ собирается на сервере, не из registry; детали — [07-deployment.md §Процедура деплоя](07-deployment.md#процедура-деплоя-github-actions--ssh); GitHub Secrets `SSH_HOST`/`SSH_USER`/`SSH_PRIVATE_KEY`). Топология и процедура — [07-deployment.md §Топология](07-deployment.md#топология-mvp--общий-сервер-за-внешним-traefik) и [§Процедура деплоя](07-deployment.md#процедура-деплоя-github-actions--ssh). Не зависит от Q-002-1: KMS на MVP — local master key из env ([ADR-003](adr/ADR-003-byok-envelope-encryption.md)). | Closed→revised: владелец инфраструктуры предоставил общий сервер с уже работающим Traefik. | Платформа выкатки зафиксирована; deploy-процедура и rollback (`git checkout <prev-commit>` + rebuild) описаны в 07-deployment.md. Прежние Caddy/nginx/`deploy-vps.sh`-артефакты — legacy (не используются). | Закрыт фиксацией shared-server+Traefik-топологии ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). При вводе registry/immutable-tag, миграции на оркестратор (k8s) или облачный KMS — новый ADR. |
| TD-007 | `STOREKIT_TEST_MODE` — env-gated режим тестовой верификации StoreKit (HS256-JWS под `STOREKIT_TEST_SECRET`) вместо реальной Apple-подписанной транзакции и Apple root CA. Семантика — [09-e2e-testing.md §2](09-e2e-testing.md#2-storekit_test_mode--env-gated-режим-тестовой-верификации). | Реальная Apple sandbox-интеграция и Apple root CA недоступны в e2e/CI; нужен воспроизводимый путь для прогона активации подписки + grant кредитов end-to-end (ADR-006). | Дефолт `false` → prod fail-closed не меняется (реальная JWS-верификация). Test-mode активен только при `STOREKIT_TEST_MODE=true` И непустом `STOREKIT_TEST_SECRET`; при включении — WARNING в лог на старте. Риск случайного включения в prod минимизирован, но это осознанный test-only seam. | Интеграция реальной Apple App Store Server API sandbox (Q-007-1): подключить sandbox-endpoint и Apple sandbox root CA, после чего test-mode-ветку удалить. ETA — после закрытия Q-007-1 (выбор sandbox/prod posture App Store). |
| TD-008 | **Закрыт (2026-05-25).** `migrations/env.py` брал URL из `get_settings().database_url`, а не из переданного Alembic `context.config` (`sqlalchemy.url`). Исправлено: URL берётся из `context.config` с fallback на `get_settings().database_url`, если ключ пуст. | Хрупкость порядка тестов и невозможность прогнать миграции против произвольной БД, переданной через Alembic Config (e2e-БД / testcontainers). qa и reviewer отметили перед e2e. | До фикса миграции игнорировали URL из Alembic Config → ненадёжность при e2e/тестах с отдельной БД. | Закрыт реализацией [09-e2e-testing.md §3.1](09-e2e-testing.md#31-td-008-migrationsenvpy--sqlalchemyurl-из-alembic-config); подтверждён e2e (migrate-job прошёл, миграция 0002 применена, `/ready` db=ok). |
| TD-006 | Окно двойной бесплатной генерации в trial при гонке. Два параллельных первых `/v1/chat/run` (subscription=none, trial_used=false, mode=credits) могут оба пройти policy-allow до атомарного `mark_trial_used`, и оба сгенерировать бесплатный ответ. | Policy-allow вычисляется до генерации и до flip `users.trial_used`; flip атомарен и идемпотентен, но не блокирует параллельную проверку (нет advisory lock на user при trial-allow). | Не биллинговая утечка: flip и списание НЕ дублируются (атомарный `UPDATE ... WHERE trial_used=FALSE`, idempotency-ledger, ADR-005). Максимум — 1 лишняя бесплатная генерация на пользователя при точной гонке двух первых запросов. Стоимость пренебрежимо мала. См. ADR-002 §Trial concurrency. | Если злоупотребление trial станет заметным по метрикам — взять `pg_advisory_xact_lock(user_id)` (или `SELECT ... FOR UPDATE` по `users`) на ветке trial-allow в `/chat/run`, сериализуя первый trial на пользователя. |

| TD-009 | Контент файлов сайта (`site_files.content`) хранится в БД (BYTEA), а не в object-storage. | Простота на старте; транзакционность с tool-операциями; без новой инфраструктуры ([ADR-010](adr/ADR-010-backend-hosted-preview.md)). | При больших/многочисленных проектах раздувает БД и нагружает её отдачей статики. Ограничено лимитами (файл ≤ 1MB, проект ≤ 10MB, ≤ 200 файлов). | При росте объёма/нагрузки — вынести контент в S3-совместимое object-storage (+ CDN), сохранив контракты `site.*`/preview. Интерфейс Website Service спроектирован под замену backend'а хранения. |

| TD-010 | Orphan-вложения (`attachments` с `session_id IS NULL`, не привязанные к сессии) не очищаются фоновым джобом. **Не актуален для MVP** ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)): таблицы `attachments` на MVP нет (inline base64 не персистится), orphan-вложений не возникает. Применимо только при реализации двухшаговой модели (**TD-015**). | Простота на старте; нет планировщика. | Накопление неиспользованных загрузок в БД (BYTEA) — **только** в двухшаговой модели. | Добавить фоновую очистку (`attachments` старше `ATTACHMENT_ORPHAN_TTL`, дефолт 24h, без `session_id`) при появлении планировщика/cron-инфраструктуры **и** реализации двухшаговой модели ([ADR-014](adr/ADR-014-multimodal-attachments.md), **TD-015**). |
| TD-011 | ~~Отправка push не реализована~~ → **частично закрыт [ADR-067](adr/ADR-067-media-ready-push-and-reconciler.md) (2026-08-11):** device-token CRUD + APNs-клиент + триггер media `completed` + reconciler. Остаток: другие триггеры (не media), локализация alert-copy, production hardening (ретраи/метрики APNs). | Media-ready push был блокером UX при фоне iOS >30с. | Media-ready работает при настроенных `APNS_*` и `notificationsEnabled=true`. | Прочие event-триггеры — отдельными ADR; оператор должен задать per-instance `.p8` + `APNS_TOPIC`. См. [modules/notifications/](modules/notifications/00-overview.md). |
| TD-012 | N+1 на `preview` в списке чатов: `ChatsRepository.list_chats` выполняет отдельный index-backed запрос `_preview()` для каждого чата страницы (до `limit`+1 строк), а не одним запросом. См. [modules/chats/02-api-contracts.md](modules/chats/02-api-contracts.md). | Простота на старте; каждый `_preview()` — точечный индексированный select последнего шага сессии; масштаб per-user (страница ≤ 100) делает накладные расходы пренебрежимыми. | До `limit` дополнительных коротких запросов на страницу списка чатов. Приемлемо для текущего per-user масштаба. | При росте размера страниц/нагрузки — заменить per-row `_preview()` на один запрос с LATERAL join или window-функцией (`ROW_NUMBER() OVER (PARTITION BY session_id ...)`), сохранив контракт ответа `GET /v1/chats`. |

| TD-013 | Истёкшие/использованные/отозванные `auth_refresh_tokens` не очищаются фоновым джобом. | Простота на старте; нет планировщика (как TD-010). | Накопление неактивных строк refresh-токенов. Малый объём (по строке на rotation), не влияет на корректность (валидность проверяется по `used_at`/`revoked_at`/`expires_at`). | Добавить фоновую очистку строк с `expires_at < now()` или непустым `used_at`/`revoked_at` старше grace-периода при появлении планировщика/cron ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), [modules/auth/04-data-model.md](modules/auth/04-data-model.md)). |

| TD-014 | Anthropic upstream-ошибки логируются как generic «upstream error» без `status_code`, `error.type`, `error.message` и `request_id`. `src/app/chat/anthropic_client.py` при `anthropic.APIStatusError` (а также `APITimeoutError`/`APIConnectionError`/`AuthenticationError`) мапит в `UpstreamError("anthropic upstream error")`, **проглатывая тело и статус** исходной ошибки. | Минимальная обработка ошибок на старте: ловим исключение SDK и сразу обобщаем в доменный `UpstreamError`. | **Маскировка диагностики**: реальная причина отказа не видна в логах. Подтверждено на проде — Anthropic вернул `400 invalid_request: "This organization has been disabled."`, но в логах был только generic «upstream error» → qa ошибочно списал на старый SDK и предложил bump (ложный фикс); истинная причина найдена только прямой диагностикой в контейнере. Поведение наружу корректно (502), но MTTR диагностики высок. | Реализовать **контракт логирования upstream-ошибок Anthropic** — см. [modules/chat-orchestrator/03-architecture.md §Логирование upstream-ошибок Anthropic](modules/chat-orchestrator/03-architecture.md#логирование-upstream-ошибок-anthropic-td-014). Логировать тело ошибки апстрима (поля-источники `error.type`/`error.message`/`request_id` + `status_code`/`model`) под camelCase лог-ключами `status_code`/`errorType`/`errorMessage`/`anthropicRequestId`/`model`/`exceptionClass` (событие `anthropic_upstream_error`) по матрице уровней; api-key и user-content — под redaction (см. [05-security.md §Логирование](05-security.md#логирование-безопасное)). Error-contract наружу (502) **не меняется**. Scope backend. |

| TD-015 | Двухшаговая модель вложений (отдельный `POST /v1/attachments` upload-эндпоинт + таблица `attachments` + retention/orphan-cleanup) **не реализуется** на MVP — мультимодальный ввод реализован inline base64 в `/v1/chat/run` ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | Inline проще для MVP (нет нового хранилища/lifecycle, один round-trip); двухшаговая модель оправдана только при переиспользовании вложений между сообщениями, очень больших файлах или object-storage. | Нет reuse вложений; нет постоянного хранения загруженного контента (вложение живёт только в рамках первого обращения к модели); каждый запрос с вложением несёт его base64 в теле. Приемлемо для сценариев дизайна (одноразовый ввод к сообщению). | При появлении требований reuse/больших файлов/object-storage — реализовать двухшаговую модель по [ADR-014](adr/ADR-014-multimodal-attachments.md) (сохранён как зафиксированный путь): upload-эндпоинт, таблица `attachments`, TD-010-аналог retention. Тогда же активируется ранее заведённая ветка хранения байтов (TD-009). |
| TD-016 | `anthropic 0.39.0` не типизирует `document`-блок (есть `ImageBlockParam`, нет `DocumentBlockParam` — проверено эмпирически). PDF-`document`-блок передаётся как сырой dict (`cast(Any, messages)`), без типобезопасности/валидации на стороне SDK. **Открыт; включает обязательное live-подтверждение:** wire-совместимость PDF-`document`-блока на текущей версии SDK подтверждается только e2e с реальным Anthropic ([06-testing-strategy.md](06-testing-strategy.md) §Тест-кейсы вложений, строка E2E), который **на 2026-06-03 не выполнен — org Anthropic отключена** (см. memory/deployment-state). Прогон обязателен сразу после восстановления org. | SDK старый (late-2024); bump SDK — отдельное решение с риском регрессий в существующем tool-loop/usage-parsing. На MVP dict-passthrough работает (wire-совместимость подтверждается e2e с реальным Anthropic). | Нет статической типизации/SDK-валидации `document`-блока; при изменении формата document-блока в будущих моделях рассинхрон выявится только в рантайме/e2e. image-блок типизирован (`ImageBlockParam`), document — нет. | Bump `anthropic` до версии с `DocumentBlockParam` (и betas, если потребуется) при необходимости типобезопасности или при отказе API на текущем формате; перепроверить tool-loop/usage-parsing. Решение об апгрейде SDK фиксируется отдельным изменением [02-tech-stack.md](02-tech-stack.md). |

| TD-017 | **Закрывается реализацией [ADR-089 §2](adr/ADR-089-attachment-limits-and-error-taxonomy.md) (решение принято 2026-08-24; код на эту дату НЕ написан — запись остаётся открытой до выката `GW-9`).** Ранее: `SizeLimitMiddleware` отклонял тело по заголовку `Content-Length` **до** парсинга, а при его отсутствии (`Transfer-Encoding: chunked`) проверка **пропускалась**. Дополнительно вскрылось следствие того же дизайна: ответ `413` отправлялся **не прочитав тело**, поэтому клиент, ещё передающий байты, видел разрыв соединения и показывал пользователю «нет связи» вместо «файл слишком большой» (репорт QA BUG-004). **Решено:** middleware переведён на чистый ASGI и считает **фактически прочитанные байты** (работает без `Content-Length`); перед закрытием отдаётся `413` с `Connection: close` и ограниченным drain остатка тела (`SIZE_LIMIT_DRAIN_BYTES`, дефолт 1 MiB). | Выбран первый из двух вариантов закрытия, названных в исходной записи, — streaming-guard (второй, «подтвердить, что Traefik режет chunked», отвергнут: он перекладывает инвариант на чужой конфиг). | **После реализации:** обход transport-лимита chunked-запросом невозможен; клиент получает HTTP-ответ вместо broken pipe. **До реализации поведение прежнее** (репорт QA воспроизводится). Остаточно: drain ограничен бюджетом — при теле, превышающем его, соединение всё ещё закрывается досрочно (осознанный компромисс: безлимитный drain отменял бы смысл лимита). Закрывается выкатом фаз `GW-9`/`GW-10` ([modules/api-gateway/07-implementation-phases.md](modules/api-gateway/07-implementation-phases.md)) с покрытием из [modules/api-gateway/09-testing.md §transport-лимит](modules/api-gateway/09-testing.md) (сквозной тест «`413`, а не разрыв» + chunked без `Content-Length`). Калибровка самих значений остаётся в TD-004. |

| TD-018 | Anthropic/OpenAI-вызов оркестрации на JSON-ручке — **non-streaming** при `ANTHROPIC_MAX_TOKENS=16000` ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md)). **UX-часть закрыта [ADR-069](adr/ADR-069-sse-text-streaming.md):** `POST /v1/chat/v2/run/stream` отдаёт SSE text deltas. Остаётся: partial-tool_use accumulation, «дописать с места» после `max_tokens`, stream на `/tool-result`. | MVP JSON-контракт + отдельный stream-эндпоинт (не ломать старых клиентов). | JSON `/run` по-прежнему ждёт полный ответ; обрезка → `blockReason=max_tokens` без продолжения. | Partial tool_use + resume после `max_tokens` — отдельным ADR при необходимости. |

| TD-022 | При пустом `SERVICE_DOMAIN` (локальная разработка) `site.preview` отдаёт **относительный** preview-URL (`/v1/preview/...`), а не абсолютный ([ADR-031](adr/ADR-031-absolute-preview-url.md)). | Локально публичный хост неизвестен; хардкод `localhost`/порта хрупок и ломал бы dev/offline-тесты. На проде `SERVICE_DOMAIN` всегда задан → абсолютный URL. | В dev-окружении модель снова может «достроить» хост к относительному пути (как было на проде до фикса). Не влияет на прод (домен задан) и на безопасность (авторизация в подписи). | При необходимости абсолютного URL в dev — задать `SERVICE_DOMAIN` локально (напр. `localhost:8000`) или ввести dev-only базовый URL. На проде долг не материализуется. |
| TD-020 | **Consumable-пакеты токенов НЕ через Adapty** в текущей итерации ([ADR-029](adr/ADR-029-adapty-subscription-webhook.md)). Adapty-вебхук покрывает только 4 события подписки; покупка пакетов токенов остаётся на `/v1/tokens/purchase` (StoreKit consumable IAP, [ADR-015](adr/ADR-015-consumable-token-iap.md)). | Scope-ограничение итерации: подписки — приоритет; consumable-события Adapty требуют отдельного проектирования idempotency и policy-guard. | Два разных пути пополнения кредитов (подписка → Adapty; разовые пакеты → StoreKit). Приемлемо: пути не пересекаются по продуктам. | При решении переносить consumable на Adapty ([Q-029-1](99-open-questions.md)) — спроектировать consumable-события Adapty + idempotency (по аналогии с `adapty_webhook_events`) + сохранить policy-guard «требует подписки» ([Q-015-1](99-open-questions.md)). |
| TD-021 | **`/v1/subscription/sync` (StoreKit JWS) НЕ ретирован** — оставлен рабочим параллельно с Adapty-вебхуком ([ADR-029](adr/ADR-029-adapty-subscription-webhook.md)), хотя источник истины подписок сместился на Adapty. | Не ломать существующий путь до подтверждения, что весь трафик идёт через Adapty; безопасный переходный период. | **Риск двойного начисления:** один путь использует `idempotency_key=sub-grant:{transactionId}`, другой — `adapty-txn:{transaction_id}` ([ADR-047](adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md)); ledger-idempotency между путями НЕ срабатывает (разные префиксы). Митигация на MVP — **контрактная** (клиент использует ОДИН путь подписок, [ADR-029](adr/ADR-029-adapty-subscription-webhook.md), [05-security.md](05-security.md)), не кодом. | После периода наблюдения подтвердить отсутствие StoreKit-`sync`-трафика по подпискам → ретировать `sync` (удалить роут/код подписочной ветки) либо ввести cross-path idempotency-мост ([Q-029-2](99-open-questions.md)). |
| TD-019 | **Закрыт (2026-06-10), вариант A.** Ранее: tz-база (zoneinfo) отсутствовала в runtime-образе → аргумент `tz` инструмента `time.now` не работал в prod (slim-образ `python:3.12-slim-bookworm` без системной tz-базы и без Python-пакета `tzdata` в зависимостях). **Решено:** devops добавил pure-Python зависимость `tzdata` в зависимости проекта (`pyproject.toml` — `tzdata>=2024.1`; `uv.lock` — `tzdata 2026.2`), без правки Dockerfile (вариант A — pure-Python, без системных пакетов, предпочтительно для slim). В собранном prod-образе `zoneinfo.ZoneInfo("Europe/Moscow")` резолвится; аргумент `tz` работает, локальные `local`/`timezone` возвращаются. UTC-набор результата (`utc`/`unix`/`weekday`) от tz-базы не зависел и работал всегда. | Закрыт добавлением tz-базы как pure-Python зависимости `tzdata` (вариант A). | tz-база гарантирована в runtime-образе → `tz` (`Europe/Moscow` и пр.) штатно отдаёт локальное время. Невалидная/мусорная зона по-прежнему деградирует к tool-result error `invalid_timezone` (ход не падает) — это нормальное поведение контракта, не дефект ([ADR-026 §6](adr/ADR-026-global-server-side-tools-and-time-now.md)). | Закрыт добавлением `tzdata` в зависимости проекта (вариант A, [ADR-026 §10](adr/ADR-026-global-server-side-tools-and-time-now.md)). При переходе на иной runtime-образ — перепроверить наличие tz-базы. |

| TD-023 | **Закрыт (2026-06-19, [ADR-041](adr/ADR-041-openai-native-pdf-attachment.md)).** Ранее: PDF-вложения не поддерживались на OpenAI-инстансах — при `provider=openai` `type: document` (`application/pdf`) → `ValidationFailedError` (`422 unsupported_media_type`); `image`/`text` работали. **Решено:** OpenAI теперь принимает PDF на вход (vision-модели gpt-4o+), поэтому в openai-ветке маппинга (`src/app/chat/attachments.py::_openai_content_block`, `document`) `application/pdf` больше **НЕ** `422`. Основной путь — OpenAI Chat Completions content-часть `file` (`{"type":"file","file":{"filename","file_data":"data:application/pdf;base64,..."}}`, точный wire-shape backend сверяет с установленным `openai` SDK 1.109.1); гарантированный фолбэк (если SDK/endpoint не принимает `file`-часть) — серверное извлечение текста PDF через `pypdf` (паттерн `workspaces/text_extract.py`) → text-блок. Anthropic-ветка не трогалась; валидация/лимиты (magic-bytes, page-guard ≤100, doc ≤8 MB) не менялись (в пределах OpenAI 100стр/32MB); сырой base64 не персистится; биллинг неизменен. | OpenAI ранее не принимал PDF в Chat Completions vision; на момент ADR-033 backend-предобработка была вне scope провайдер-абстракции. | Контракт `/v1/chat/run` теперь единообразен: PDF принимается на обоих провайдерах ([05-security.md §Провайдер LLM: OpenAI](05-security.md#провайдер-llm-openai-adr-033), [API-REFERENCE.md §Вложения](API-REFERENCE.md)). | Закрыт реализацией [ADR-041](adr/ADR-041-openai-native-pdf-attachment.md) (native file-input или text-extraction фолбэк). Богатый PDF-разбор через растеризацию/OpenAI Responses API при потребности — [Q-033-2](99-open-questions.md). |
| TD-024 | **`chat_steps.payload` хранит wire-формат провайдера, ОТВЕТИВШЕГО на виток**, а не нейтральный (провайдер-агностичный) формат ([ADR-033 §3](adr/ADR-033-llm-provider-abstraction.md)). **Приведено к факту 2026-09-11 ([ADR-105 §A](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md)):** прежняя формулировка «кросс-провайдерный реплей в одной БД невозможен, ограничение не материализуется» неверна с [ADR-073](adr/ADR-073-dual-credits-llm-providers.md)/[ADR-074](adr/ADR-074-provider-key-failover.md)/[ADR-044](adr/ADR-044-multi-provider-byok.md)/[ADR-099 §8](adr/ADR-099-crm-admin-economics-and-instance-settings.md): одна сессия законно несёт assistant-шаги нескольких форм — после кросс-обхода, после замены BYOK-ключа на ключ другого провайдера, после смены операторского дефолта модели у сессий с `model IS NULL`. Реплей такой истории обеспечивается **терпимым чтением** ([ADR-105 §A3](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md)), а не хранением. | Нейтральный формат хранения — миграция формата всей истории всех инстансов; терпимое чтение закрывает валидность реплея без миграции. | Перевод чужой формы **с потерями**: рассуждение (`thinking`/`reasoning`), цитаты-аннотации и служебные записи хостового поиска другого провайдера до читателя не доходят — после смены провайдера модель теряет собственное прежнее рассуждение и источники. | Потребность в реплее без потерь (A/B одного диалога, миграция данных между провайдерами, продуктовая смена модели внутри чата) — спроектировать нейтральный формат хранения `chat_steps.payload` + двунаправленную трансляцию neutral↔wire в каждом клиенте (отдельный ADR, [Q-033-1](99-open-questions.md)). |
| TD-026 | **Пресеты промтов нельзя менять без деплоя backend** ([ADR-035](adr/ADR-035-prompt-presets-endpoint.md)): набор/тексты/иконки пресетов `GET /v1/presets` заданы статическим реестром в коде (`src/app/chat/presets.py`). Правка состава = коммит + деплой backend. | На старте — простота, версионирование, review/diff текстов, единый набор на всех действующих инстансах ([ADR-035 §2](adr/ADR-035-prompt-presets-endpoint.md)). Целевое требование («менять без релиза iOS») уже выполнено (пресеты отдаёт backend); правка без релиза **iOS** ≠ правка без деплоя **backend**. | Изменить пресеты нельзя оперативно без CI/CD-деплоя. На текущем масштабе (7 статичных пресетов, редкие изменения) приемлемо. | Промежуточный шаг — config-JSON env (`PROMPT_PRESETS`, парсинг по образцу `token_products()`/`allowed_models()` в [config.py](../src/app/config.py)), правка `.env` + рестарт без пересборки образа; следующий — таблица БД + admin-CRUD ([Q-035-1](99-open-questions.md)). Публичный контракт `GET /v1/presets` при любом переходе не меняется (источник инкапсулирован за роутером). |
| TD-028 | **`context` (per-message, [ADR-037](adr/ADR-037-chatrunrequest-context-allowlist-injection.md)) и `preferences.code_defaults` (серверные дефолты) не связаны.** `context` (язык кода/стиль/тон/локаль на ход) инъектируется в user-сообщение, но НЕ читает/не накладывается на серверные дефолты пользователя из `preferences`; нет правила приоритета (per-message override vs server default) и нет авто-подстановки `code_defaults` когда `context` пуст. Allowlist `context` — статический реестр в коде → расширение требует деплоя backend ([Q-037-1](99-open-questions.md)). | Поставка ADR-037 — атомарный scope: сделать `context` функциональным (per-message, без БД-миграции). Слияние с `preferences.code_defaults` (источник дефолтов, правило приоритета, идемпотентность семантики) — отдельное проектирование, вне scope. | Пользователь, задавший дефолты в preferences, всё равно обязан слать `context` на каждый ход, чтобы они применились (нет «липкости» через серверные дефолты). На текущем масштабе приемлемо (iOS может подставлять свои дефолты в `context` на клиенте). | Спроектировать слияние `context` ⊕ `preferences.code_defaults` (правило: per-message `context` перекрывает server default; при пустом ключе — подстановка из preferences) + источник `code_defaults` в preferences-модуле. Решение фиксируется новым/расширенным ADR ([Q-037-1](99-open-questions.md)). |
| TD-025 | **Закрыт (2026-09-07, [ADR-099](adr/ADR-099-crm-admin-economics-and-instance-settings.md)).** Прежнее содержание: биллинг не дифференцирован по модели ([ADR-034](adr/ADR-034-user-model-selection.md)) — выбор любой модели из allowlist списывал одинаково, премиум-модель тарифицировалась как базовая. Закрыт переводом тарифа чата на **функцию модели**: строка тарифа на каждую модель известного каталога (`chat:<provider>:<model_id>`), правится оператором из CRM без деплоя; единственный мост «цена–гейт–списание» ([ADR-064 §9](adr/ADR-064-study-learn-quiz-generation-mode.md)) сохранён и переключён на ключ «модель». | Долг существовал, пока не было ни источника коэффициентов, ни операторской ручки: дефолт был атомарным scope Поставки 1. | ⚠️ **Цена закрытия названа явно:** режим генерации перестал влиять на цену, поэтому `research`/`reasoning`/`study_learn` дешевеют до цены модели до тех пор, пока оператор не поднимет цену дорогих моделей ([ADR-099 §5.3](adr/ADR-099-crm-admin-economics-and-instance-settings.md)). | Закрыт [ADR-099](adr/ADR-099-crm-admin-economics-and-instance-settings.md); [Q-034-1](99-open-questions.md) закрыт тем же решением. |

> **BUG-A (max_tokens truncation, закрыт дизайном, не TD):** молчаливая потеря обрезанных `tool_use` при `stop_reason="max_tokens"` (ход уходил в else-ветку → `assistant_message`/`toolCall=null`) **устранена** явной обработкой `stop_reason` ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md): `blockReason=max_tokens`, кредит не списывается) + поднятием дефолта `ANTHROPIC_MAX_TOKENS` до 16000. Баг-фикс, не долг. Остаточный non-streaming-долг — **TD-018**.
>
> **BUG-B (single toolCall на parallel tool use, закрыт дизайном, не TD):** потеря всех client-side `tool_use` кроме первого (`first_client_out`) → orphan `tool_use` на continuation → Anthropic 400/502 **устранена** возвратом всех вызовов в `toolCalls[]` + батч `/chat/tool-result` + барьер хода ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md)). Баг-фикс, не долг.

> **BUG-5 (закрыт дизайном, не TD):** ненадёжность `chat_steps.created_at` как порядкового ключа реконструкции (transaction-time `now()` одинаков для шагов одной транзакции; UUID-tie-break случаен → orphan tool_result на server-side tool-loop → Anthropic 400/502) **устранена** введением монотонной колонки `chat_steps.seq` ([ADR-021](adr/ADR-021-deterministic-step-order-and-block-normalization.md), миграция `0006`). Это баг-фикс (восстановление корректности), а не принятый долг — TD не заводится. Реализация — backend ([modules/chat-orchestrator/03-architecture.md §Детерминированный порядок шагов](modules/chat-orchestrator/03-architecture.md#детерминированный-порядок-шагов-и-нормализация-payload-adr-021)). Заодно нормализуются content-блоки перед персистом (служебное SDK-поле `caller` вырезается) — тоже фикс, не долг.

> TD-009 (контент в БД vs object-storage) на MVP покрывает `site_files.content` (website-builder). Хранение байтов вложений **на MVP не возникает** — inline base64 не персистится ([ADR-020 §3](adr/ADR-020-inline-base64-attachments-mvp.md), сырой base64 не пишется в `chat_steps.payload`). Прежняя пометка про `attachments.content` относится к двухшаговой модели ([ADR-014](adr/ADR-014-multimodal-attachments.md)), которая отложена (**TD-015**); при её реализации TD-009 снова охватит байты вложений.

| TD-027 | **Байты файлов-знаний workspace (`workspace_files.content`) хранятся в БД (BYTEA), а не в object storage** ([ADR-036 §4](adr/ADR-036-workspaces-implementation.md)). | Простота на старте; транзакционность с CRUD; без новой инфраструктуры (тот же выбор, что [TD-009](100-known-tech-debt.md) для `site_files`). Хранение в `attachments`/object-storage отвергнуто — фича не должна зависеть от отложенного `attachments` ([TD-015](100-known-tech-debt.md)). | При больших/многочисленных файлах раздувает БД и нагружает её при подаче контекста. Ограничено лимитами (`WORKSPACE_FILE_MAX_BYTES`=8 MB/файл, `WORKSPACE_FILES_TOTAL_BYTES`=32 MB/workspace, `WORKSPACE_FILE_MAX_COUNT`=20). | При росте объёма/нагрузки — вынести `content` в S3-совместимое object storage (+ CDN при необходимости), сохранив контракты `/v1/workspaces/{id}/files` и подачу контекста; `extracted_text` может остаться в БД (нужен для инъекции без чтения байтов). Симметрично [TD-009](100-known-tech-debt.md). |

| TD-029 | **Легаси `byok_keys`-строки с `provider=NULL`** ([ADR-044](adr/ADR-044-multi-provider-byok.md)): миграция `0013` добавляет колонку `provider` без backfill, поэтому BYOK-ключи, сохранённые до миграции, имеют `provider=NULL`. Для таких строк провайдер на генерации определяется fallback-детектом по расшифрованному ключу (`detect_byok_provider(plaintext)`), а `activeModel` на чтениях `/v1/byok` (где ключ намеренно не расшифровывается) деградирует к BYOK-дефолту **активного** инстанса вместо провайдера ключа. | Expand-only без backfill (симметрия с `0009`/`0010`); backfill потребовал бы массовой расшифровки всех ключей (расширение поверхности plaintext, [ADR-003](adr/ADR-003-byok-envelope-encryption.md)) либо знания провайдера, которого нет в строке. | Генерация byok для легаси-строк корректна (fallback-детект). Возможна неверная `activeModel` в ответе `/v1/byok` для до-миграционного OpenAI-ключа на Anthropic-инстансе (и наоборот) до следующего `set`. Узкое окно: только легаси-строки, только до следующего `set_key` (который пишет `provider`). | Закрывается естественно при следующем `set_key` пользователя (колонка заполняется детектором). При необходимости — одноразовый backfill-джоб с расшифровкой под контролируемой процедурой (отдельное решение), либо ленивая запись `provider` при первой генерации легаси-строки. |

| TD-030 | **Артефакты выведенного инстанса `veltrio` сохранены на сервере, пока его домен `veltriohub.shop` принадлежит третьей стороне** ([ADR-056](adr/ADR-056-instance-decommission-veltrio.md)). На диске остаются каталог `/opt/veltrio` с `SERVICE_DOMAIN=veltriohub.shop` в `.env`, тома `veltrio_pgdata`/`veltrio_redisdata` и бэкап `/root/veltrio-decommission-20260718140433`. Всё необходимое для перехвата чужого домена лежит готовым: любой `docker compose up` в этом каталоге пересоздаёт Traefik Host-роутер `veltriohub.shop`. Единственная защита — отсутствие записи `veltrio:veltrio` в `INSTANCES` обоих workflow; защита декларативная, автоматической проверки нет ([ADR-056 §Решение п.6](adr/ADR-056-instance-decommission-veltrio.md) — наблюдатели ручные). | Отключение сделано **обратимым**: удаление каталога/томов/бэкапа безвозвратно уничтожает данные инстанса ради экономии дискового места. Решение об удалении и его срок — отдельное операторское решение, в [ADR-056 §Решение п.4](adr/ADR-056-instance-decommission-veltrio.md) не принимается. | Риск перехвата `veltriohub.shop` жив всё время, пока каталог и тома существуют, а домен принадлежит третьей стороне. Опасность со временем забывается — поэтому носителем риска является эта запись, а не текст ADR. Наблюдатель отказа — `curl -sI https://veltriohub.shop/healthz` не должен отвечать нашим сервисом ([ADR-056 §Решение п.6](adr/ADR-056-instance-decommission-veltrio.md)). | Закрывается при выполнении **любого** из двух условий: (а) каталог `/opt/veltrio`, тома `veltrio_pgdata`/`veltrio_redisdata` и бэкап удалены оператором (риск отпадает вместе с артефактами); **либо** (б) подтверждено, что домен `veltriohub.shop` больше не принадлежит третьей стороне (перехватывать нечего). До наступления любого из них запись остаётся открытой. |

| TD-031 | **Пул квиза реплеится провайдеру целиком на каждом последующем ходе сессии** ([ADR-064 §4](adr/ADR-064-study-learn-quiz-generation-mode.md), зафиксировано [ADR-065 §6](adr/ADR-065-study-learn-advertisement-gate-and-history-spoiler-strip.md)). Результат `quiz.generate` сохраняется как обычный tool-результат в `chat_steps.payload` и участвует в реконструкции истории (`_build_messages`), то есть уходит провайдеру **на каждом** ходе сессии после квиза. Верхняя граница по действующим константам: 10 вопросов × (`question` 1000 + 10 × `option` 400 + `explanation` 2000) ≈ **70 КБ** на один квиз-ход, и вклады складываются по ходам. Это делает пул **крупнейшим** сохраняемым результатом инструмента в проекте. **Границы намеренно не ужаты:** они продуктовые (пул до 10 вопросов с внятными пояснениями), их урезание — продуктовое решение, которого никто не принимал. Долг фиксируется, чтобы рост входных токенов на `study_learn`-сессиях не выглядел необъяснимым. **Направление разрешения:** реплеить прошлые квиз-результаты компактным **плейсхолдером** вместо полного пула — прямой прецедент [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md) (тяжёлые вложения персистятся плейсхолдером и в реплее не повторяются); пул при этом остаётся доступен клиенту через историю. Решение — отдельным ADR при подтверждённом росте расхода. |

| TD-032 | **Provider-side continuation (`previous_response_id`, OpenAI Responses API) выключена ЯВНЫМ выключателем.** `_CONTINUATION_ENABLED: Final = False` (`src/app/chat/openai_responses_client.py`); `_usable_previous_response_id` (там же) возвращает `None` на каждом ходе, поэтому **каждый** v2-ход провайдера реплеит полную локальную историю. `chat_sessions.provider_state` продолжает **писаться** (`set_provider_state`), но не **читается** ни одним ходом. Выключатель заведён явно, а не как побочный эффект: раньше цепочка не работала сама собой — `provider_state.model` хранил датированный снапшот (`gpt-5.1-2025-11-13`), а вызывающий просил алиас (`gpt-5.1`), и сверка имён роняла handle на каждом ходе. Запись в `usage.model` запрошенного **алиаса** (нужна для себестоимости — прайс ключуется алиасом, [ADR-079 §1](adr/ADR-079-crm-provider-cost-duration-payments.md)) молча включила бы цепочку, то есть изменила бы поведение продукта под видом фикса цены. | **Включать нельзя, пока не закрыты три дыры (в порядке, в котором ломается ход).** (1) `response id` принадлежит ОДНОМУ аккаунту OpenAI, а цепочка ключей [ADR-074](adr/ADR-074-provider-key-failover.md) ставит за основным ключом `OPENAI_API_KEY_BACKUP` — **второй аккаунт**. (2) `_provider_state_for_attempt` (`src/app/chat/orchestrator.py`) сверяет только **имя провайдера**, а не слот ключа, породивший handle, и отдаёт state **любому** OpenAI-кандидату. (3) Каждый `/chat/run` начинает цепочку с основного ключа, поэтому handle, выпущенный резервным аккаунтом, уходит основному; отказ не является credential failure, `next_attempt_index` возвращает `None`, failover **не ротирует** — ход падает. (4) Recovery нет: `clear_provider_state` (`ChatRepository`, `src/app/chat/repository.py`) зовётся только на truncation/edit-regenerate и явных сбросах — **три** места вызова в `src/app/chat/orchestrator.py`, перечень пересниматься `grep -n "clear_provider_state" src/app/chat/orchestrator.py`, — и **никогда** на upstream-ошибке ⇒ один отравленный handle ломает сессию навсегда. | Полный локальный replay на **каждом** ходе v2: больше входных токенов на длинных сессиях (растёт линейно по длине истории). **Prompt caching этим не отменяется** — повторяемый префикс по-прежнему кэшируется провайдером, поэтому цена растёт не пропорционально объёму. Ходы корректны и детерминированы; пользователь разницы не видит. Экономии от `provider_state` **нет**, пока долг открыт. | **Отдельный ADR** (не «флип константы»), закрывающий все три дыры одним контуром обработки отказа: (а) **слот ключа внутри `provider_state`** (какой ключ выпустил handle); (б) **сверка слота** в `_provider_state_for_attempt` — не совпал, state не передаётся; (в) **детектор невалидного `previous_response_id`** на ответе провайдера → `clear_provider_state` + **однократный** retry полным локальным реплеем. Только после этого `_CONTINUATION_ENABLED` переводится в `True`. |

| TD-033 | **Media-половина `GET /v1/admin/costs/daily` идёт seq scan'ом** ([ADR-092 §8](adr/ADR-092-crm-daily-costs-endpoint.md)). `_MEDIA_DAILY_SQL` (`src/app/admin/crm_costs.py:143-158`) отбирает строки единственным предикатом `j.created_at >= :ts_from AND j.created_at < :ts_to`, но ведущего индекса по `created_at` у `media_jobs` нет: `ix_media_jobs_user_created` начинается с `user_id`, `ix_media_jobs_non_terminal` — частичный по статусу (оба — в `MediaJob.__table_args__`, `src/app/models/tables.py`). Chat-половина такой индекс получила (`ix_steps_created_at`, миграция `0029_steps_created_idx`), media-половина осталась незакрытой. | Эффект на **порядки** меньше, чем у chat: `media_jobs` растёт по строке за генерацию, `chat_steps` — по строке за каждый вызов LLM. Заводить вторую миграцию в той же волне ради таблицы, которая на действующем объёме сканируется за миллисекунды, — цена выката без выигрыша. | Полный скан `media_jobs` на каждый запрос страницы «Расход API» CRM. Запрос идёт из фонового воркера CRM раз в 30 минут на бэк, то есть нагрузка предсказуема и не пользовательская. Деградация наступит при росте `media_jobs` до сотен тысяч строк. | Отдельная expand-only миграция `CREATE INDEX ix_media_jobs_created_at ON media_jobs (created_at)` (тот же приём и то же обоснование «без `CONCURRENTLY`», что у `0029`) — при росте таблицы либо при первом наблюдаемом росте времени ответа эндпоинта. |

| TD-034 | **11 вызовов `logger.*(extra=…)` мимо конвенции `log_event` — поля молча теряются в JSON-логах.** `JsonFormatter` (`src/app/observability/logging.py:25-27`) читает **только** `record.extra_fields`, который проставляет `log_event` (`:46-48`); прямой `extra={"sessionId": …, "stepId": …}` кладёт значения в атрибуты `LogRecord`, куда форматтер не смотрит, и в JSON они **не попадают**. **Места заданы ПРИЗНАКОМ и командой, а не списком строк** — перечень номеров уже протухал от чужих волн, при том что состав долга не менялся: `grep -rn -A3 "logger\.\(info\|warning\|error\|exception\|debug\)(" src/app --include=*.py | grep -E "extra=\{"`. **Замер на 2026-09-08: 11 вызовов в трёх файлах** — `src/app/chats/service.py` (**3**, ключ `stepId`); `src/app/memory/indexer.py` (**3**, `messageStepId`/`sessionId`); `src/app/request_logs/service.py` (**5**, `requestLogId`/`mediaJobId`). | Не падает и не шумит: ключи не конфликтуют с зарезервированными атрибутами `LogRecord` (в отличие от `filename`, дававшего `500` в `attachment_refs`), а `sessionId` и так приходит в каждую запись из contextvar. Поэтому дефект немой и не был замечен при ревью каждого из мест по отдельности. | **Корреляционные id теряются ровно там, где нужнее всего** — все 11 мест это ветки отказа или деградации, 8 из них `logger.exception(...)` (`memory/indexer.py`, `request_logs/service.py`), 3 — `logger.warning` на нераспознанном tool-id в истории (`chats/service.py`). По логу видно, что индексация памяти/запись `request_logs` упала, но не видно, **какой** шаг/ход/лог-строку это затронуло: `stepId`, `messageStepId`, `requestLogId`, `mediaJobId` в JSON отсутствуют. Разбор инцидента вырождается в поиск по времени. | Механическая замена на `log_event(logger, logging.<LEVEL>, "<event>", **fields)` во всех 11 местах (образец — `app.chat.orchestrator`, `app.billing_adapty.service`, [ADR-046](adr/ADR-046-adapty-webhook-outcome-logging.md)). Для `logger.exception` — `log_event(..., logging.ERROR, ...)` с `exc_info=True` либо сохранение `logger.exception` c `extra={"extra_fields": {...}}`; выбор — за исполнителем, но поле обязано доехать до JSON. Отдельного ADR не требует: конвенция уже действует, это приведение кода к ней. Закрывается, когда repo-wide `grep -rn 'extra={' src/app/ | grep -v extra_fields` пуст. |
| TD-035 | **`PRESETS_DEFAULT_LOCALE` обслуживает не только пресеты.** С [ADR-097](adr/ADR-097-character-personas.md) та же переменная задаёт язык каталога `GET /v1/characters`, с [ADR-100](adr/ADR-100-assistant-speech-output.md) — каталога `GET /v1/voices`, а с [ADR-098](adr/ADR-098-broadapps-paywall-experiments-and-default-product.md) её значение уходит в `context.locale` сегментации пейволла. **Пересчёт на 2026-09-08: три каталога и один исходящий вызов, а не «пресеты».** Имя переменной уже её фактической области действия, и оператор, читающий только имя, не догадается, куда она достаёт. | Альтернатива — второй env (`CHARACTERS_DEFAULT_LOCALE`) — хуже: два источника одного факта (языка каталогов инстанса) неизбежно разъедутся, а «язык каталогов» у инстанса один. Переименование в `CATALOG_DEFAULT_LOCALE` требует правки `.env` на сорока живых инстансах с окном, в котором часть инстансов уже читает новое имя, а часть ещё старое, — цена выше, чем неточное имя. | Только когнитивная: поведение корректно, дефолт `en` graceful. Риск — оператор меняет язык пресетов и не ожидает, что сменились язык персонажей, язык каталога голосов и локаль, уходящая в сегментацию пейволла. Смягчено тем, что все три каталога обязаны быть на одном языке — расхождение было бы дефектом, а не гибкостью; **несмягчённой остаётся четвёртая точка** — `context.locale` пейволла: это не каталог и на одном языке с ними быть не обязан. Подпись настройки `catalog.presets_default_locale` называет все четыре ([ADR-099 §8.1](adr/ADR-099-crm-admin-economics-and-instance-settings.md)), поэтому оператор поверхности предупреждён; читатель одного лишь имени env — нет. | Переименование в `CATALOG_DEFAULT_LOCALE` с периодом, когда читаются оба имени (новое приоритетнее), затем удаление старого — при следующей волне, которая и так трогает `.env` всех инстансов. Отдельного выката ради имени не планируется. |
| TD-036 | **Выпущенные сборки приложений считают цену медиа-генерации САМИ, по мультипликативной формуле** (`credits` × пачки × `resolutionMultipliers` × `audioMultiplier`, `GET /v1/media/models`). Поячеечный тариф ([ADR-099 §4.2](adr/ADR-099-crm-admin-economics-and-instance-settings.md)) этой тройкой в общем случае непредставим: после непредставимой правки легаси-поля выводятся из ячеек с округлением ВВЕРХ, и старый клиент показывает цену **выше** фактической. | Обновить уже выпущенные бинарники нельзя; из двух направлений ошибки выбрано безопасное — списание никогда не превышает показанного ([ADR-099 §4.4](adr/ADR-099-crm-admin-economics-and-instance-settings.md)). | Завышенная цена на пейволле генерации отпугивает пользователя. На дефолтных (представимых) тарифах расхождения нет вовсе; факт непредставимости виден оператору метрикой `media_price_legacy_overquote`. | Клиенты переходят на аддитивный `prices[]` (точные ячейки) — после чего легаси-поля можно перестать выводить из ячеек и объявить справочными. |
| TD-037 | **Оверлеи-сироты не чистятся.** Строка `admin_tariffs`/`admin_products` на вариант, исчезнувший из каталога (модель снята с кода, продукт убран из env), остаётся в таблице и в разрешении не участвует. | Осознанно: цена обязана пережить временное исчезновение варианта (откат релиза, смена env), а автоудаление сделало бы правку оператора уничтожаемой чужим деплоем. | Таблицы медленно накапливают неиспользуемые строки; на объёме десятков строк это ничего не стоит. Строка-сирота **не** отдаётся в `/pricing` и **не** влияет на списание. | При росте числа сирот — операторская ручка «показать неиспользуемые» либо ретенция по `updated_at` отдельным решением. |
| TD-038 | **Три env-переменные остались без потребителя.** После [ADR-099](adr/ADR-099-crm-admin-economics-and-instance-settings.md) цена хода — функция модели, поэтому `CHAT_CREDIT_COST_RESEARCH`, `CHAT_CREDIT_COST_REASONING` и `CHAT_CREDIT_COST_STUDY_LEARN` не читает никто; они по-прежнему заданы в `.env` инстансов и проходят валидатор положительности, но на цену не влияют. Устаревает и строка «declared ≠ wired» [ADR-064 §12](adr/ADR-064-study-learn-quiz-generation-mode.md), объявлявшая потребителем `CHAT_CREDIT_COST_STUDY_LEARN` балансовый гейт, дебит и `capabilities` (тело ADR не переписывается — immutability; ревизия зафиксирована в [adr/INDEX.md §Ревизии](adr/INDEX.md)). | Удаление переменной — отдельное изменение конфигурации 41 инстанса со своим риском; волна ADR-099 и так меняет цену хода, и совмещать два изменения `.env` в одном выкате незачем. `CHAT_CREDIT_COST_GENERAL` потребителя сохраняет — он дефолт тарифной строки. | Оператор, поднявший `CHAT_CREDIT_COST_RESEARCH`, не получит ни ошибки, ни эффекта: величина выглядит рабочей ручкой цены, ею не являясь. Это **мёртвое объявление**, и опасно оно именно правдоподобием. Митигация до закрытия — пометка в [07-deployment.md §env](07-deployment.md#конфигурация-env) и кейс-регрессия «правка `_RESEARCH` не меняет цену». | Пересмотр вместе с первой же волной, трогающей `.env` всех инстансов: удалить три строки из шаблонов `.env.example`/`.env.prod.example` и из `Settings`, оставив `CHAT_CREDIT_COST_GENERAL`. До тех пор — held. |
| TD-039 | **Цена озвучки живёт в переменной окружения, а не в тарифном контракте.** Величина — `TTS_CREDIT_COST`, дефолт **1 кредит за озвученный ответ**; вводится волной [ADR-100](adr/ADR-100-assistant-speech-output.md) (`POST /v1/chat/speech`). Долг в том, что величина **денежная**, а дом денежных величин — **тарифный контракт**, а не поверхность настроек инстанса ([ADR-099 §8.2 (г)](adr/ADR-099-crm-admin-economics-and-instance-settings.md)). Вариант, которого нет в `GET /v1/admin/pricing`, **для оператора не существует** (`CRM ADR-109 §5` — норма контрагента: сторона сервиса обязана отдавать строку на каждый поддерживаемый вариант; ⚠️ префикс `CRM` обязателен — `ADR-109` **в этом** репозитории занят другим решением): оператор не видит цену озвучки и не может ею управлять, при том что страница обещает все тарифы в одном месте. **Замер переснят 2026-09-08 (поздний вечер):** величина **живая** — поле `tts_credit_cost` (`src/app/config.py`, алиас `TTS_CREDIT_COST`, дефолт `1`, валидатор `_positive_tts_credit_cost` там же); соседняя волна ввела её в код после первой записи этого долга. ⚠️ Адрес переведён с номера строки на имя символа: прежний `config.py:725` протух за сутки (фактически `:737`) — файл меняется активными волнами, и номер строки в нём не переживает даже одного дня ([README §ссылка на код](README.md)). Долг от этого не меняется по существу — денежная величина по-прежнему живёт в env вместо тарифного контракта, — но регистрируется теперь на **действующую** цену, а не на предписание. | Тарифный контракт в этот момент переписывался соседней волной ([ADR-099](adr/ADR-099-crm-admin-economics-and-instance-settings.md)), и правка спецификации с двух сторон дала бы конфликт. Приземление в env — сознательный размен: волна озвучки не блокируется, а перенос делается одним понятным шагом. | Цена озвучки не видна и не правится из CRM: единственный способ её изменить — правка `.env` + рестарт, то есть ровно та операция, ради ухода от которой заводилась поверхность экономики. Пока строки нет, обещание страницы «все тарифы в одном месте» неполно — и неполно **молча**. | Строка **`speech:{voice_id}`** (либо `speech:default`) в `GET /v1/admin/pricing` с единицей «**за озвученный ответ**»; дефолт строки воспроизводит текущее значение переменной **бит-в-бит** — перенос не повод пересматривать цену. ⚠️ **Условие, а не примечание:** единица «за озвученный ответ» честна ровно потому, что длина озвучки ограничена сверху жёстким потолком, известным **до** вызова (`TTS_MAX_CHARS` по ADR-100). Станет потолок настраиваемым или исчезнет — единица перестаёт быть честной и обязана поменяться вместе с ним (`CRM ADR-109 §5.1`: единица объявляется по тому, как считают на самом деле). ⛔ **`VOICE_OUTPUT_ENABLED` в этот долг НЕ входит** — это продуктовый флаг, симметричный `VOICE_INPUT_ENABLED`, и его дом — поверхность настроек инстанса; классифицируется он волной, которая вводит его в код, по границе свипа [ADR-099 §8.2](adr/ADR-099-crm-admin-economics-and-instance-settings.md) — предикат по нему и шести соседям уже применён поимённо в [Q-099-5](99-open-questions.md) (там `VOICE_OUTPUT_ENABLED` назван кандидатом в §8.1, а `TTS_CREDIT_COST` отнесён сюда). Отдельного долга на него не заводится. ⚠️ **Cross-ref односторонний, и это названо, а не умолчано:** [ADR-100](adr/ADR-100-assistant-speech-output.md) — документ соседней волны — ссылки на `TD-` не несёт, поэтому читатель ADR-100 в отрыве об этом долге не узнает. Проставление обратной ссылки требует правки чужого документа и оставлено владельцу волны озвучки. |
| TD-040 | **РЕШЕНО К ЗАКРЫТИЮ ДО ВЫКАТА, код ещё не приведён.** Долг фиксировал: `PATCH /v1/admin/products/{product_id}` отвергает **`400`** правку продукта, чей источник не задаёт `purchase_kind` или `credits` (строка `PRODUCTS_CATALOG` без `kind`/`credits`), — включая правку **одного `archived`**, потому что колонки оверлея `admin_products` объявлены `NOT NULL`. ⛔ **Прежняя оценка «цена принята, случай редкий» СНЯТА как неверная:** контрагент нормативно **обязан** присылать archived-правку именно на этом классе (`CRM ADR-073 §4`: контрол архива не подчиняется правилу «`tokens: null` ⇒ read-only»), и класс у него **основной** — `CRM ADR-073 §Контекст`: 30 позиций из 39 уходят в архив. То есть объявленная фича `products.write_archived` не работала ровно там, ради чего архив и заводился. | Оставить отказ и уведомить вторую команду — отклонено: у CRM **нет механизма** скрыть контрол для отдельной строки (`features` объявляются на путь, а не на элемент), поэтому уведомление не дало бы ей ничего, чем распорядиться, и контрол продолжил бы падать вопреки fail-closed норме `CRM ADR-072 §7`. Разбор альтернативы — [ADR-099 §Альтернативы](adr/ADR-099-crm-admin-economics-and-instance-settings.md). | На 2026-09-08 (ночь) код волны отвергает archived-правку строки с неполным источником; ADR-099 §6.1/§9 уже нормируют обратное. Расхождение закрывается фронтом работ [ADR-099 §14](adr/ADR-099-crm-admin-economics-and-instance-settings.md) **до выката** — миграция `0033` не применена ни на одном из 41 инстанса, поэтому правится она сама, второй миграции не требуется. | Закрывается выполнением [ADR-099 §14](adr/ADR-099-crm-admin-economics-and-instance-settings.md): `name`/`purchase_kind`/`tokens` в `admin_products` — **nullable** с семантикой «оверлей этого поля не задаёт», пофилдовое слияние у каждого читателя строки, `archived`-правка без единой ветки отказа. Наблюдатель закрытия: `PATCH {"archived": true}` на строке `PRODUCTS_CATALOG` без `kind` и без `credits` отвечает `200`, начисления и витрина не меняются. Остаточное ограничение вынесено отдельно — [TD-043](100-known-tech-debt.md). |

| TD-041 | **Решение `ADR-096` выкачено, но документа нет ни в каталоге `docs/adr/`, ни в реестре, а его таблица отсутствует в модели данных.** Коммит `8127bbc` (2026-09-03) ввёл таблицу `legacy_user_ids` (миграция `0030_legacy_user_ids`) и **третью — намеренно последнюю — ветвь** разрешения пользователя платёжных вебхуков в `src/app/billing_common/resolve.py` (тест `tests/integration/test_legacy_user_resolution_adr096.py`). В `docs/` об этом нет ничего: `03-data-model.md` таблицы не описывает, [ADR-053](adr/ADR-053-cloudpayments-webhook-user-resolution-via-auth-devices.md)/[ADR-055](adr/ADR-055-adapty-webhook-user-resolution-via-auth-devices.md) по-прежнему описывают резолв **двухступенчатым**. | Волна закрывала денежный инцидент (платежи перенесённых пользователей терялись) и до документации не дошла; пропуск не был замечен, потому что сравнение «файлы `docs/adr/` ↔ строки реестра» расхождений **не даёт** — реестр внутренне согласован и просто не знает о номере. | **Двойная.** (1) Порядок ветвей резолва — денежный инвариант: таблица заполняется разово из данных переноса и не обновляется, поэтому живые данные обязаны идти первыми, иначе устаревшая строка уводит **чужой платёж не тому человеку** молча. Инвариант живёт только в комментарии кода и в тесте — читающий `docs/` его не видит и при следующей правке резолва может переставить ветви. (2) Номер `096` выглядит свободным: формула «следующий свободный» выдала бы его новому решению и переименовала бы уже сославшиеся на него коммит, тест и ветвь кода (пометка-заглушка — [adr/INDEX.md §Нумерация](adr/INDEX.md)). | Написать `ADR-096-*.md` по факту выката (document-as-built: код и миграция — источник истины о поведении), завести строку реестра, добавить `legacy_user_ids` в [03-data-model.md](03-data-model.md) и **пометить контраст** в ADR-053/ADR-055 (резолв стал трёхступенчатым, третья ветвь последняя). Не требует правки кода. |
| TD-042 | **Внутренние ссылки `ADR-NNN` и имена переменных окружения попадают в фактически сгенерированный OpenAPI.** Нормы, которые это запрещают, уже действуют: [08-api-documentation.md §R2ter](08-api-documentation.md#r2ter-лаконичность-user-facing-текстов-для-тестировщиков) («без ADR/Q/TD-ссылок и без имён переменных окружения»), §R9.4 («внутренние идентификаторы в user-facing тексты не попадают») и action item того же документа §Scope. **Долг — расстояние между нормой и кодом**, а не новое решение. ⛔ **Перечень задан ПРИЗНАКОМ, а не списком файлов** — иначе он протухнет при первом же новом `description`: *носитель = строка сгенерированного OpenAPI (`summary`/`description`/`Field.description`/описание security scheme), содержащая `(?:ADR\|TD\|Q)-\d{2,3}` либо имя env-переменной*. Команда пересъёмки (вывод безопасен — печатает пути и имена, не значения): `PYTHONPATH=src python -c "import json,re;from app.main import app;s=json.dumps(app.openapi(),ensure_ascii=False);print(len(re.findall(r'(?:ADR\|TD\|Q)-[0-9]{2,3}',s)))"`. **Замер на 2026-09-08 (поздний вечер): 17 носителей** (команда выше печатает **12** — это число *вхождений* ADR-ссылок, а не носителей: у `POST /v1/chat/v2/run/stream` их два в одном `description`) — 11 с ADR-ссылками (`POST /v1/chat/v2/run/stream`; схемы `AppleSignInRequest`, `CrmDailyCostItem`, `MediaJobRefSchema`, `MediaJobModerationSchema`, `MediaUploadRequest`, `MediaChoicesSchema`, `MediaChoiceOptionSchema`, `MediaSelectionIn`, поля `ChatResponse.mediaChoices` и `ChatV2RunRequest.mediaSelection`) и 6 с именами env (`GET /v1/media/templates/images` и `.../videos` — `FAL_API_KEY`; `POST /v1/billing/adapty/webhook` — `ADAPTY_WEBHOOK_SECRET`; `TokenProduct.price` — `TOKEN_PRODUCTS_PRICE_MINOR_UNITS`; `TokenProductsResponse.products` — `PRODUCTS_CATALOG`; **`components.securitySchemes.adaptyWebhook`** — `ADAPTY_WEBHOOK_SECRET`, `adapty_webhook_scheme` в `src/app/api_gateway/openapi_security.py`). ⚠️ Последний носитель — **результат свипа по признаку, а не находки**: ручной перечень его пропустил, потому что искал по файлам роутеров и схем, а описание security scheme живёт в третьем месте. | Происхождение — **пропуск прошлого, не регресс волны [ADR-099](adr/ADR-099-crm-admin-economics-and-instance-settings.md)**: все 17 носителей уже живут на проде, файлы волны чисты, и выкат ADR-099 риск не меняет. Нормы R2ter/R9.4 вводились как требование к **новым** текстам и сплошным свипом по уже написанным подкреплены не были — ровно тот случай, когда правило есть, а его прогон по всем носителям формы не сделан. | **Когнитивная и репутационная, не функциональная.** Интегратор iOS читает `/docs` и видит номера чужих внутренних решений (`ADR-070`) и имена серверных переменных, которых у него нет и быть не может, — шум, из которого он делает неверные выводы о том, чем управляет. Отдельно: имя env в user-facing тексте подсказывает читателю поверхность конфигурации, которой он не владеет; значения при этом **не** раскрываются ни одним носителем (проверено — печатаются только имена). Функционального риска нет: wire-формат, коды и security-механизмы носителями не затрагиваются. | Пройтись по признаку выше и переписать 17 текстов в стиле R2ter — **правкой кода отдельной волной**, вместе с прогоном OpenAPI-снапшота. Правка **не заказана выкатом ADR-099 сознательно**: риск она не меняет, а круг тестов и ревью стоит дороже. Закрытие проверяется той же командой: обе величины (ADR-ссылки и имена env) обязаны стать нулём, и кейс обязан **падать** при возврате любого носителя — иначе проверяется форма, а не факт. |
| TD-043 | **Строку `PRODUCTS_CATALOG` без `kind` нельзя сделать НАЧИСЛЯЮЩЕЙ из CRM.** Класс покупки решает, какой резолвер прочитает число кредитов, а поля `purchase_kind` в теле `PATCH` нет вовсе (состав заморожен `CRM ADR-109`); `POST` тем же `product_id` даёт `400` «дубликат». Поэтому у такой строки `tokens` отдаётся **`null`** ([ADR-099 §6.1](adr/ADR-099-crm-admin-economics-and-instance-settings.md), правило 4), CRM по своей норме гасит карандаш (`CRM ADR-072 §1.1` п. 2), и правка числа оператору **не предлагается**. Архивация при этом доступна (`CRM ADR-073 §4`) — ограничена ровно одна операция. | Материализовать класс догадкой запрещено: он назначил бы продукту путь начисления, которого оператор не выбирал, а оверлей числа **без** класса не прочитал бы ни один резолвер — правка была бы принята и не применена. Расширять замороженное тело `PATCH` полем `purchase_kind` — ревизия контракта силами двух команд ради операции, у которой есть рабочий обходной путь. | ⚠️ **Отличие от закрытого [TD-040](100-known-tech-debt.md) — принципиальное и должно быть видно:** там операция **обещалась и падала** (`400` на контроле, который CRM рендерит), здесь она **не обещается** — контрол не отрисовывается по штатной деградации контрагента. Обещанной и падающей операции не остаётся ни одной; цена — оператор, которому нужно сделать витринную строку начисляющей, идёт в `.env` + рестарт. Частота **инстанс-зависима**: `PRODUCTS_CATALOG` нет ни в `.env.example`, ни в `.env.prod.example`, ни в `infra/fleet/provision.sh` (переснято grep'ом 2026-09-08, ночь), её заводят руками. | Наблюдатель — серия `admin_override_rejected_total{scope="products",reason="source_kind_missing"}`: она считает попытки правки числа у строки без класса (страховочный путь, который CRM не предлагает, но который открыт по admin-ключу). Закрытие потребует согласованного с CRM расширения тела `PATCH` полем класса **либо** отдельной операции «назначить класс»; на нашей стороне в одиночку не закрывается. |

| TD-044 | **У оси D (`CODE_TOOLS_ENABLED`, [ADR-094 §3](adr/ADR-094-code-assistant-tools.md)) нет defensive guard в tool-loop.** Предикат `offered_code_tool` (`src/app/chat/tools.py`) применяется только при сборке offer-set (`_offered_to_model`); в оркестраторе он **не вызывается** ни разу (`grep -n "offered_code_tool\|CODE_TOOLS" src/app/chat/orchestrator.py` даёт только строки системного промта: `_CODE_TOOLS_INSTRUCTION` и его подстановку в `_compose_system_prompt`). Соседние гейты guard имеют: ось C — `TOOL_GENERATION_MODES`, денилист [ADR-081](adr/ADR-081-disabled-tool-families.md) и media [ADR-072](adr/ADR-072-chat-media-tools-instance-gate.md) — три соседние ветви `_handle_tool_use` в `orchestrator.py`. Обнаружено при проектировании оси E ([ADR-102 §10](adr/ADR-102-mapkit-client-tools.md)), у которой guard нормативно обязателен. | Вероятность низкая: инструмент, не предложенный модели, вызывается ею редко (провайдер не обязан этого не делать, но и повода нет). Ось D включена ровно на одном инстансе, где клиент инструменты кода реализует, — то есть на «опасных» инстансах вызов и так неисполним, а на целевом исполним. | **Инструмент кода, выдуманный моделью на инстансе без `CODE_TOOLS_ENABLED`, дойдёт до клиента и оставит ход незавершённым**: барьер [ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md) ждёт `tool-result`, которого приложение прислать не может, таймаута нет — пользователь не получает ответа вовсе. Для сравнения, тот же сценарий у оси E, денилиста и media заканчивается мягким `tool_not_available` и продолжением хода. | Добавить в tool-loop ветку по образцу денилиста: `tool_name in CODE_TOOLS` и предикат оси D ложен → `_record_refused_tool_call(..., ToolExecution.error("tool_not_available", …))`. Зона `backend`; отдельного ADR не требует — норма «гейт ≠ guard» уже действует ([03-architecture.md §Оси гейтинга](modules/chat-orchestrator/03-architecture.md#оси-гейтинга-tool-набора-adr-022--adr-026--adr-064), инвариант 4). Закрывается, когда guard есть и покрыт тестом «модель вернула `git.push` при выключенном флаге → ход выжил». |

| TD-045 | **Таблица результатов инструментов в теле [ADR-090 §3](adr/ADR-090-chat-documents.md) расходится по составу полей с модульным контрактом документов.** ADR-090 §3 объявляет результат `document.create` как `documentId` + `filename` + `size` + `version` (**без `mediaType`**), а результат `document.read` — как `documentId` + `filename` + `mediaType` + `content` + `version` (**без `size`**). Модульный контракт [modules/documents/02-api-contracts.md §Инструменты модели](modules/documents/02-api-contracts.md#инструменты-модели-server-side-global) даёт `mediaType` у **всех четырёх** инструментов, а у `document.read` — ещё и `size`. Тот же состав, что в модульном контракте, лежит в основе [ADR-101 §1](adr/ADR-101-chat-response-documents.md): элемент `ChatResponse.documents[]` объявлен **дословной** карточкой `_doc_brief()` (`documentId`, `filename`, `mediaType`, `size`, `version`), и совпадение там нормативно, потому что у поля два производителя. Какая из двух сторон верна — **здесь не устанавливается**: установлено только, что два нормативных документа об одном и том же говорят разное; авторитетен о фактическом составе сам код (`src/app/chat/global_tools.py`, `_doc_brief`), и сверка с ним — часть закрытия долга. | Тело принятого ADR не правится попутно и не правится вслепую: это отдельное действие (уточняющая ревизия шапки ADR-090 либо новый ADR) и оно требует сверки с кодом, которой в проходе-обнаружителе не было — расхождение найдено при разборе противоречия ADR-101, где ADR-090 был источником домена, а не предметом правки. | Читатель, реализующий или тестирующий инструменты документов **по телу ADR-090**, получит состав без `mediaType` у `create` и без `size` у `read`. Цена не косметическая: ровно из этой карточки собирается `ChatResponse.documents[]` ([ADR-101 §4](adr/ADR-101-chat-response-documents.md)), причём его второй производитель восстанавливает значения из **сохранённого** tool-результата — недостающее поле там не восполнить ничем, и ответ разошёлся бы между ногами одного хода. | Сверить §3 ADR-090 с фактическим `_doc_brief()` и с модульным контрактом; привести к коду ту сторону, которая от него отличается, одним решением (правка тела ADR-090 оформляется как явная ревизия с датой, а не молча). Зона `architect`; предварительная сверка состава по коду — `backend`. Закрывается, когда все три места (ADR-090 §3, модульный контракт, ADR-101 §1) называют один состав, подтверждённый цитатой из кода. |

| TD-046 | **В `docs/` есть внутренние markdown-ссылки, которые не резолвятся.** Признак (по нему долг и закрывается, перечень мест — лишь снимок): внутренняя markdown-ссылка — текст в квадратных скобках, цель в круглых, с необязательным `#якорь` в хвосте цели, — у которой **либо** целевого файла нет по пути, разрешённому относительно каталога файла-источника, **либо** файл есть, а такого якоря в нём нет (якорь заголовка = GitHub-слаг: нижний регистр, не-буквенно-цифровое отбрасывается, пробелы → дефисы, `_` **сохраняется**). Прогон по всему `docs/` на `HEAD` = `5795a03`: **312** файлов, **6695** внутренних ссылок, из них **624** с якорем → **5 битых якорей** и **7 ссылок на несуществующий файл**, итого **12**. Битые якоря: `03-data-model.md:240` → `100-known-tech-debt.md#td-001` (в реестре нет заголовков на строки TD — это ячейки таблицы); `API-REFERENCE.md:1184` и `modules/chat-orchestrator/README.md:37` → `#get-v1models--…-adr-034` (в заголовке-цели три ADR, в якоре один); `adr/ADR-092-crm-daily-costs-endpoint.md:166` → `01-architecture.md#observability` (заголовок «Наблюдаемость»); `modules/audit/00-overview.md:13` → `100-known-tech-debt.md#known-tech-debt` (реальный слаг — `100--known-tech-debt`). Несуществующие файлы: шесть переименованных ADR (`ADR-020-inline-attachments.md`, `ADR-017-multi-instance-deploy.md`, `ADR-016-byok-key-lifecycle.md`, `ADR-026-global-server-side-tools.md`, `ADR-010-preview-sandbox.md`, `ADR-011-tools.md`) плюс `02-tech-stack.md:47` → `Dockerfile` (файл лежит в корне репозитория, а путь резолвится в `docs/Dockerfile`). **Наивный прогон даёт не 7, а 9:** два лишних — не ссылки, а иллюстрации внутри прозы (`modules/chat-orchestrator/03-architecture.md:364` и `modules/chat-orchestrator/09-testing.md:272`, оба описывают правило «ссылка → её текст» и содержат образец синтаксиса с целью `url`). Проверка обязана их исключать (образец в прозе), иначе критерий закрытия «ноль» недостижим в принципе. | Дефекты предшествуют работам, в которых обнаружены, и лежат на строках, которых те работы не касались; чинить их попутно — drive-by в чужих разделах. Массового вреда нет: ссылка ведёт в начало файла или не открывается, содержание документа при этом верно. | Читатель, которому ссылка обещала конкретный раздел, попадает в начало документа или в никуда — дороже всего это в цепочках «норма → раздел-источник», где ссылка и есть доказательство, что норма где-то сформулирована. Шесть ссылок на переименованные ADR вдобавок создают впечатление, что документ-цель утрачен. Класс тихий: ни один гейт репозитория ссылки не проверяет, поэтому число растёт незаметно. | Ввести машинную проверку признака (прогон по всему `docs/`, отчёт «файл:строка → цель») и починить всё, что она находит; счётчик в этой строке — снимок на `5795a03`, а не перечень к закрытию: закрытие определяется **нулём по прогону**, а не вычёркиванием двенадцати. Зона `architect` (правка ссылок) + `devops` (постановка проверки в CI, если решено гейтить). Отдельного ADR не требует. |

| TD-047 | **Флаг возможности инстанса читается мимо реестра настроек — оператор не может включить возможность из панели.** Признак (по нему долг и закрывается, перечень имён — лишь снимок): булево поле `Settings`, гейтящее **продуктовую возможность** (то, что пользователь видит как «умеет / не умеет»), значение которого потребитель берёт напрямую из `Settings`, а не через реестр [ADR-099 §8](adr/ADR-099-crm-admin-economics-and-instance-settings.md) (`src/app/instance_config/settings_registry.py` → аксессоры `src/app/instance_config/values.py` → `resolve_setting()` с порядком «оверлей → env → дефолт кода»). Прогон признака на `HEAD` = `c354862` (команда: `grep -nE '^\s+[a-z_]+: bool = Field' src/app/config.py` — **17** булевых полей; каждое отнесено к «возможность / не возможность», затем каждая возможность сверена с `_SPECS` реестра и с классами [ADR-099 §8.2](adr/ADR-099-crm-admin-economics-and-instance-settings.md)): флагов возможностей — **9**, из них через реестр читаются **6** (`chat.characters_enabled`, `chat.memory_enabled`, `chat.voice_input_enabled`, `chat.code_tools_enabled`, `chat.media_tools_enabled`, `moderation.enabled`), **мимо реестра — 3**. Из этих трёх у двух дом есть и решение принято сознательно: `CHAT_LEGACY_WEB_SEARCH_ENABLED` — [ADR-099 §8.2](adr/ADR-099-crm-admin-economics-and-instance-settings.md) класс **(ж)** («не проходит по безопасности при любом значении»), `VOICE_OUTPUT_ENABLED` — отложен предикатом [Q-099-5](99-open-questions.md) до отдельного круга ревью. **Дома нет ровно у одного — `MAPS_TOOLS_ENABLED`** ([ADR-102 §10](adr/ADR-102-mapkit-client-tools.md)): в реестре не объявлен, в классах §8.2 не назван (`grep -c MAPS_TOOLS_ENABLED docs/adr/ADR-099-*.md` → **0**), читается прямым `get_settings().maps_tools_enabled` в трёх точках `src/app/chat/orchestrator.py` — `_system_prompt_for` (строка промта) и методы `ChatOrchestrator._generate_loop` / `._handle_tool_use` — контраст с соседней осью D, которая ту же величину берёт через `instance_config.code_tools_enabled()`. **Сегодня это НЕ дефект поведения:** оверлея для карт нет, и прямое чтение — единственно верное; долг заведён на **пробел возможности**, а не на ошибку. | ADR-099 §8.2 прямо возлагает классификацию новой переменной на **ту волну, которая её вводит**, а не на волну реестра. Волна [ADR-102](adr/ADR-102-mapkit-client-tools.md) ввела `MAPS_TOOLS_ENABLED` и этот шаг не сделала: обе волны шли параллельно, и на момент проектирования карт реестра ещё не существовало. Расширение состава поверхности требует отдельного круга ревью — тем же основанием, по которому отложены семь переменных озвучки. | Оператор включает `chat.characters_enabled`, `chat.media_tools_enabled` и `chat.code_tools_enabled` из панели, а карты — только правкой `.env` инстанса и рестартом, хотя это ровно такой же продуктовый выключатель. Расхождение тихое: панель не сообщает, что показывает **не все** возможности, — отсутствующая строка неотличима от возможности, которой у сервиса нет вовсе. Дороже всего это при поинстансной раскатке [Q-102-1](99-open-questions.md), где флаг снимается **инстанс за инстансом** по мере готовности приложения — то есть ровно тот сценарий, ради которого панель и делалась. | Классифицировать `MAPS_TOOLS_ENABLED` по [ADR-099 §8.2](adr/ADR-099-crm-admin-economics-and-instance-settings.md): либо объявить строку в реестре (`chat.maps_tools_enabled`, `bool`, группа «Чат») и перевести потребителей на аксессор `instance_config.*`, либо отнести к классу-исключению с названным основанием. Решается вместе с [Q-099-5](99-open-questions.md) — тем же кругом ревью, что и семь переменных озвучки: вопрос у них общий («какие ещё возможности пускать в панель»), и разводить его на два круга незачем. Закрытие определяется **прогоном признака**, а не вычёркиванием `MAPS_TOOLS_ENABLED`: любой флаг возможности, введённый следующей волной мимо реестра, — та же строка долга. ⚠️ **Второй носитель признака уже назван, и его волна шага классификации НЕ пропустила:** [ADR-104 §8](adr/ADR-104-voice-mode-websocket.md) вводит `VOICE_MODE_ENABLED` — продуктовый флаг возможности — **сознательно мимо реестра**, потому что ось составная и её вторая конъюнкта `VOICE_OUTPUT_ENABLED` в реестре тоже не объявлена (отложена [Q-099-5](99-open-questions.md)): панельный переключатель при выключенной в `.env` половине молча не дал бы ничего. Прогон признака поэтому даст на нём **три** флага возможности мимо реестра, как только ADR-104 будет реализован (сегодня решение docs-only, в `src/app/config.py` поля нет — датированный замер выше остаётся верным). Все три (`MAPS_TOOLS_ENABLED`, `VOICE_OUTPUT_ENABLED`, `VOICE_MODE_ENABLED`) решаются **одним** кругом ревью вместе с [Q-099-5](99-open-questions.md). Зона `architect` (решение и классификация) + `backend` (реестр и перевод потребителей). |

| TD-048 | **Фантомная строка леджера: списание записано, денег не взято, задачи нет.** Признак (по нему долг и закрывается, перечень путей — лишь снимок): путь, на котором `WalletService.consume` **уже вставила** строку `ledger_transactions` (`src/app/wallet/service.py:133-146`) и отмену этой строки возложила на **откат транзакции** (`src/app/wallet/service.py:161-172`: условный `UPDATE wallets ... WHERE balance >= :amount` вернул `None` → `raise InsufficientCreditsError` с комментарием «roll back the just-inserted ledger row by failing the tx»), а **вызывающая сторона это исключение ПЕРЕХВАТЫВАЕТ и возвращает управление штатно** — то есть запрос доходит до `commit`, и отката, на который рассчитан кошелёк, не происходит (`session_scope` откатывает только при вылетевшем исключении — `src/app/db.py:47-56`). Прогон признака на `HEAD` = `075a4d6` (команда: `grep -rn '\.consume(' src/ --include=*.py` — **5** прямых вызовов, из них один внутри `MediaGenerationService.submit`, поэтому дополнительно `grep -rn '\.submit(' src/ --include=*.py` по вызывающим этот метод — **4**; итого **8** путей, для каждого прочитан ближайший перехватчик и установлено, вылетает ли исключение за пределы запроса): **носитель — 1**. Это `media.generate_image` / `media.generate_video` из tool-loop чата: `src/app/chat/global_tools.py:488` зовёт сабмит, `:521` ловит `InsufficientCreditsError` и превращает его в мягкую ошибку инструмента `ToolExecution.error(...)`, ход выживает и доходит до `await self._session.commit()` (`src/app/chat/orchestrator.py:3028`). Измерено `qa` на балансе **2** при цене задачи **4**: в `ledger_transactions` строка `debit` на 4, таблица задач **пуста** (`consume` в `src/app/media_generation/service.py:281` предшествует `self._repo.create(...)` на `:289`), баланс остался **2**. Остальные 7 путей признаку **не отвечают**, и это проверено, а не предположено: `src/app/chat/orchestrator.py:3195` ловит и **явно откатывает** (`await self._session.rollback()` на `:3203` перед `blocked`) — эталонный контраст, ровно тот же перехват, но транзакция роняется; `src/app/chat/orchestrator.py:1877` (визардный сабмит) ловит только чтобы `raise` (`:1888`); `src/app/admin/crm_service.py:941` ловит и поднимает `HTTPException` (`:948`) — исключение всё равно вылетает; `src/app/api_gateway/routers/wallet.py:97`, `src/app/api_gateway/routers/media.py:267` и `:327`, `src/app/chat/speech.py:440` (через `src/app/api_gateway/routers/voices.py:153`) перехватчика, гасящего исключение, не имеют. Отдельно проверен и НЕ является носителем сегодня однотипный гасящий перехватчик `src/app/chat/orchestrator.py:2036` (SSE: `except BaseException` → при непустых дельтах отдаёт error-событие и `return`, не пробрасывая): вставленная строка до него не доходит — визардная ветка стоит до генерации (`:1389`, дельт ещё нет), а media-tool-путь гасит отказ раньше, в `global_tools`. Он остаётся **вторым местом к перепроверке** при появлении любого нового списания. **Дефект предсуществующий, к волне [ADR-103](adr/ADR-103-media-jobs-turn-scoped-merge.md) отношения не имеет** — она поля ответа собирает, кошелька не касается. | Кошелёк и вызывающая сторона держат **несовместимые контракты, и ни один из них не сформулирован в `docs/`**. Кошелёк выбрал «вставить строку, затем проверить баланс» ради атомарности и идемпотентности по `(user_id, idempotency_key)`: уникальный индекс — единственный источник истины о «уже оплачено», и до вставки узнать это нельзя. Отмену он возложил на откат — механизм верный, но **невидимый вызывающему**: сигнатура `consume` о нём не сообщает, и перехватчик, гасящий отказ ради выживания хода, выглядит безобидно. Решение `global_tools` не ронять ход на мягком отказе само по себе **правильное** (ADR-068 §1: soft-отказ инструмента, ход продолжается, отказ виден в `serverTools[]`) — цена его в том, что вместе с ходом выживает и строка. | Пользователь видит в истории кошелька **списание, которого не было**: строка `debit` есть, баланс не изменился, сгенерированного ничего нет. То же попадает в расчёт расходов панели, то есть искажает операторскую отчётность, а не только экран пользователя. Класс тихий и **асимметричный по знаку**: денег у пользователя не забирают (баланс цел), поэтому жалобы «списали лишнее» не будет — расхождение копится незаметно, а обнаруживается при сверке леджера с балансом. Наблюдается только на пути «модель зовёт media-инструмент при нехватке кредитов», то есть на самом типовом отказе для пользователя с малым балансом. | Устранять **по признаку**, а не по одному найденному месту, и предпочтительно **на стороне кошелька**, где живёт причина: перехватчик о невидимом ему требовании «не гаси это исключение» знать не обязан. Направления (выбор — отдельным решением `architect`): (а) `consume` сама снимает вставленную строку при непрошедшем балансовом гейте (SAVEPOINT / `begin_nested()` вокруг пары «вставка + условный UPDATE»), после чего перехват на вызывающей стороне становится безопасным при любом контракте вызывающего; (б) балансовый гейт до вставки с сохранением идемпотентности — заведомо сложнее и гонку не снимает; (в) точечная правка `global_tools` (откатить перед возвратом мягкой ошибки) закрывает **экземпляр**, а не класс, и потому недостаточна как единственная мера. Закрытие определяется **прогоном признака** (`grep` вызовов `consume` и вызывающих `MediaGenerationService.submit`, разбор ближайшего перехватчика каждого) — **ноль носителей**, а не вычёркиванием `global_tools`; любой новый гасящий перехватчик над списанием — та же строка долга. Зона `architect` (решение по (а)/(б)) + `backend` (реализация) + `qa` (кейс: баланс меньше цены задачи → строк `debit` в леджере **ноль**, а не одна). |

| TD-049 | **Адрес на код по НОМЕРУ СТРОКИ в `docs/` протухает молча — ни один гейт этого не видит.** Признак (по нему долг и закрывается, перечень мест — лишь снимок): ссылка на код вида `<файл>:<строка>` или `<файл>:<от>-<до>` — а равно её сокращённая форма `` `:NNN` ``, наследующая файл у соседней ссылки, — у которой содержимое кода по этому адресу **не соответствует** тому, что о нём говорит документ. Носитель — ЛЮБОЙ файл `docs/` (ADR, module-docs, реестры), а не только свежие решения: адрес ломает **любая** волна, тронувшая файл, в том числе волна, которая этот документ не открывала. **Прогон признака на `HEAD` = `2c6579b`** (команды: `grep -rEoh --include=*.md '[A-Za-z0-9_./-]+\.[a-z]{2,4}:[0-9]+(-[0-9]+)?' docs/` — полная форма; `` grep -rhoE --include=*.md '`:[0-9]+(-[0-9]+)?`' docs/ `` — сокращённая; далее по КАЖДОМУ адресу прочитан код и сверен с утверждением документа): **313** файлов `docs/`, **187** адресов полной формы в **45** файлах (из них **8** ведут на строки самих `.md`, остальные **179** — на код) и **61** сокращённой. Приведено к факту этим проходом (перевод на **имя символа плюс файл без номера строки**): **84** полной формы и **22** сокращённой в **32** документах; из этих 84 шесть содержанию соответствовали и переведены заодно, потому что стояли в тех же предложениях. **Остаток на 2c6579b: 103** адреса полной формы в **26** файлах и **39** сокращённой — по каждому из них содержимое кода утверждению документа СЕГОДНЯ соответствует, то есть признаку они не отвечают и протухнут от следующей же волны. Прецедент внутри самого реестра: у TD-039 адрес `config.py` протух **за сутки** (был `:725`, стал `:737`) и уже переведён на имя символа. | Номер строки дешевле имени символа при написании и точнее указывает место, поэтому так писали годами. Ошибка при этом **не наблюдаема**: адрес остаётся синтаксически валидным, файл существует, строка существует — просто содержит другой код. Ни линт, ни типы, ни тесты, ни сборка `docs/` этого не ловят; TD-046 закрывает только markdown-ссылки внутри `docs/` и ссылок на код не касается. | **Читатель уводится в другой код и не узнаёт об этом.** Худший наблюдённый класс — адрес, попавший на **противоположную** сущность: [ADR-024](adr/ADR-024-history-payload-domain-normalization.md) ссылался на `to_anthropic_tool_name`, а по адресу лежала обратная функция `to_domain_tool_name`; [ADR-101 §1](adr/ADR-101-chat-response-documents.md) держал весь аргумент состава поля на «дословно равен `_doc_brief()`», а по адресу лежал `_parse_uuid`. Второй класс — адрес, попавший в **чужой блок настроек**: [ADR-045](adr/ADR-045-per-path-body-limit-workspace-files.md) адресовал `SIZE_LIMIT_BODY` в комментарий про JWT-секрет, [ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md) — `ANTHROPIC_MAX_TOKENS` в комментарий про порядок failover. | **Конвенция, действующая с этой записи: новая ссылка на код в `docs/` даётся ИМЕНЕМ СИМВОЛА и файлом БЕЗ номера строки** (`ChatRepository.tool_results_for_message_step`, `src/app/chat/repository.py`). Номер строки допустим только там, где именованной сущности нет вовсе (строка внутри SQL-литерала, конкретная ветка длинной функции), и тогда рядом обязателен `HEAD`, на котором адрес снят. Долг закрывается, когда прогон признака по всему `docs/` даёт **0** несоответствий И в `docs/` не остаётся адреса по номеру строки без имени символа рядом. |

| TD-050 | **Результат медиа-генерации, завершившийся у fal ПОСЛЕ дедлайна, теряется, а закупка у fal оплачена; такой исход не наблюдается.** Признак: задача, доведённая до `failed` правилом [ADR-105 §B2](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md) при `lastObservation = upstream_pending` (fal в момент дедлайна ещё сообщал «в работе»). После `failed` провайдер не опрашивается (терминальная задача отдаётся из БД — [ADR-060](adr/ADR-060-media-generation-fal.md)), поэтому результат, пришедший позже, не забирается и не показывается. | Увидеть поздний результат можно только опросом после терминального состояния (ломает правило «терминальная задача провайдера не дёргает» и возвращает ту же бесконечную нагрузку, от которой защищает дедлайн) либо вебхуком (отвергнут [ADR-060](adr/ADR-060-media-generation-fal.md): публичная callback-поверхность). Дедлайн `MEDIA_JOB_DEADLINE_SECONDS` (дефолт 6 ч) выбран в 216 раз выше максимума живого прогона, чтобы такой исход был редким, — но его частота на проде **не измерена**. | Пользователь не теряет денег (кредиты возвращены) и видит `failed`; оператор платит fal за результат, который не выдан. Размер не наблюдается: доля поздних результатов неизвестна по построению. | Счёт `media_generation_deadline_exceeded` с `lastObservation = upstream_pending` за неделю по флоту: **больше нуля** хотя бы на одном инстансе — пересмотреть величину дедлайна по данным (агрегат длительностей `completed` — запрос в [ADR-105 §B3](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md)) либо спроектировать разовую позднюю проверку задач, закрытых по дедлайну. Зона `architect` (решение) + `backend`. |

| TD-051 | **Кросс-обход Anthropic → OpenAI срабатывает и на отказ ФОРМЫ НАШЕГО запроса — дефект сборки запроса к Anthropic маскируется успешным ответом соседа.** По коду: `AnthropicClient.create_message` / `stream_message` (`src/app/chat/anthropic_client.py`) превращают **любой** `anthropic.APIStatusError`, включая `400 invalid_request_error`, в `UpstreamError`; `_is_anthropic_upstream_failure` (`src/app/chat/key_failover.py`) считает сбоем апстрима любой `UpstreamError` в цепочке причин, и `next_attempt_index` уводит ход на OpenAI. Отличается только `400` с маркерами денег (`is_credential_failure`). Событие `chat_provider_failover` пишет `reason=upstream_failure` и `exception_class=UpstreamError` — HTTP-статус Anthropic и его текст в нём не видны. | [ADR-074 §2](adr/ADR-074-provider-key-failover.md) задаёт направление Anthropic → OpenAI как «**любой** сбой апстрима», а перечень «не ротируют: … ошибка формы» относит к переходу ключ → ключ; отказ формы нашего собственного запроса эта формулировка не различает. Найдено при свипе [ADR-105 §A](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md): так тихо «работали» ходы, где Anthropic получал блоки чужой формы. | Каждый такой виток платит за заведомо безуспешный вызов Anthropic и отвечает моделью другого провайдера; дефект сборки запроса (в том числе будущий дефект терпимого чтения [ADR-105 §A3](adr/ADR-105-provider-failure-input-shape-and-media-deadline.md)) не даёт ни одного отказа хода и ни одного отличимого события — виден только как рост `chat_provider_failover` с `reason=upstream_failure` при живом Anthropic. | Решение `architect`: различать ли отказ формы (`4xx`, не credential) и сбой апстрима (`5xx`, таймаут, обрыв) в направлении Anthropic → OpenAI; минимально — HTTP-статус Anthropic в событии `chat_provider_failover`, чтобы отказ формы стал наблюдаем. Закрытие — по прогону признака «ни один `4xx` не-credential от Anthropic не уводит ход на соседа неразличимо от сбоя апстрима». Зона `architect` + `backend`. |

Пусто по остальным пунктам. Новый долг добавляется сюда с cross-ref из кода/документации.
