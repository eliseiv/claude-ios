# 99 — Open Questions (cross-cutting)

Формат: `Q-NNN-N`. Дефолты применены там, где это не блокирует реализацию backend. Блокеры эскалированы в orchestrator (`blocking_questions`).

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-001-1 | TTL/expiry `sessionId`: сколько живёт сессия, что после истечения? | Open | Soft TTL **24h** по `updated_at`; продолжение истёкшей сессии → новая сессия, старый контекст не догружается. Реализуемо на дефолте. | Нет |
| Q-002-1 | Конкретный KMS-эквивалент для envelope encryption BYOK (AWS KMS / GCP KMS / Azure Key Vault / HashiCorp Vault / Transit). | **Open — отложен (post-MVP)** | **На MVP — `LocalKmsClient`** (реальный AES-256-GCM wrap DEK под `KMS_LOCAL_MASTER_KEY` из secret manager/env, [ADR-003](adr/ADR-003-byok-envelope-encryption.md), [05-security.md](05-security.md#byok--шифрование-at-rest-envelope-encryption)). Миграция на облачный KMS — post-MVP, тот же интерфейс `KmsClient`. Отвязан от deploy-target ([TD-005](100-known-tech-debt.md) закрыт независимо). | Нет (MVP работает на LocalKmsClient) |
| Q-003-1 | Точные значения rate limits per user/device/IP. | Open | chat/run: 30/60/120 req/min (user/device/IP); прочие POST: 60 req/min/user. Конфигурируемо через env. | Нет |
| Q-003-2 | Точные size-лимиты payload/context/tool-result. | Open | message ≤32KB, context ≤64KB, tool-result ≤256KB, body ≤512KB. Конфигурируемо. | Нет |
| Q-004-1 | **Формула конвертации usage (input/output tokens) → кредиты.** Тарифные коэффициенты на модель. | **Closed** ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)) | **1 кредит = 1 сообщение**: завершённый message-шаг (assistant_message) списывает `amount=1`; tool-раунды не списывают (1 списание на message-шаг, идемпотентно по `messageStepId` — не по gateway `requestId`; см. [ADR-005](adr/ADR-005-idempotency-ledger.md)). usage хранится в meta для аудита, на amount не влияет. | Нет (решено) |
| Q-005-1 | Конкретный JWT issuer / JWKS endpoint и flow выпуска токенов (Apple Sign-In? собственный auth? Firebase?). | **Closed (2026-06-02, решение пользователя)** | **Встроенный issuer в backend** ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), модуль [auth](modules/auth/README.md)): backend САМ выпускает (`/v1/auth/*`) и верифицирует RS256 JWT (self-consistent, `iss=https://broadnova.shop`, `aud=claude-ios`). Первичная аутентификация — device-based (анонимная). Email/пароль и Apple Sign-In — опциональное расширение (Q-018-2 ниже). Prod-требование: сконфигурировать приватный ключ подписи (must-configure-before-launch). | Нет (решено) |
| Q-006-1 | Начисление кредитов: при покупке подписки (grant) и/или докупка пакетов? Endpoint начисления (`credit`) в ТЗ не описан. | **Closed** ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)) | **Фиксированный пакет на период**: активация/продление подписки (subscription/sync, переход в active на новый период) начисляет `SUBSCRIPTION_CREDITS_PER_PERIOD` (config, дефолт 1000) кредитов как `credit`-tx, идемпотентно по `transactionId` периода. Публичный endpoint докупки пакетов — out of scope. | Нет (решено) |
| Q-007-1 | Sandbox vs production StoreKit verification, обработка отзыва/возврата (refund/revocation). | **Open — отложен (must-configure-before-launch)** | **На MVP — `STOREKIT_TEST_MODE`** (sandbox/test-режим, Apple prod certs не готовы, [TD-007](100-known-tech-debt.md)). Prod-верификация (`STOREKIT_TEST_MODE=false` + Apple root CA + реальный `APPSTORE_BUNDLE_ID` + заведённые IAP-продукты) — **обязательна до публичного запуска** ([07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch)). refund → `subscription.status=expired`. | Нет (не блокирует инфру/staging; **prod-блокер до приёма пользователей**) |
| Q-009-1 | Атрибуция конкретного оператора в admin-API (нужен ли «кто именно из админов»)? | Open | На старте actor обезличен (`admin`), статический `ADMIN_API_SECRET` ([ADR-009](adr/ADR-009-admin-token-auth.md)). При появлении нескольких операторов с требованием атрибуции — переход на отдельный admin-JWT issuer (вариант B ADR-009) отдельным ADR. | Нет |
| Q-009-2 | Поведение admin-grant на несуществующем `userId`: `404` vs admin-provisioning. | Open | Дефолт **`404 user_not_found`** — admin-grant не создаёт пользователей (источник идентичности — JWT issuer, [ADR-007](adr/ADR-007-lazy-user-provisioning.md)); опечатка видна оператору. См. [modules/admin/02-api-contracts.md](modules/admin/02-api-contracts.md#обоснование-404-на-несуществующем-userid-не-admin-provisioning). | Нет |
| Q-010-1 | Точное значение TTL preview signed URL. | Open | Дефолт `PREVIEW_URL_TTL_SECONDS=900` (15 мин), конфигурируемо ([ADR-010](adr/ADR-010-backend-hosted-preview.md)). | Нет |
| Q-010-2 | Точные лимиты размера/числа файлов и content-type allowlist превью. | Open | Дефолты: файл ≤ 1 MB, проект ≤ 10 MB, ≤ 200 файлов; allowlist — html/css/js/json/png/jpeg/svg/gif/webp/woff2/plain. Конфигурируемо ([ADR-010](adr/ADR-010-backend-hosted-preview.md), [modules/website-builder/05-security.md](modules/website-builder/05-security.md)). | Нет |
| Q-010-3 | Изоляция origin превью: выделенный путь + sandbox-заголовки vs отдельный поддомен `preview.<domain>`. | Open | Старт — путь `/v1/preview/*` + sandbox CSP (самодостаточно). Prod-рекомендация (операционная) — отдельный поддомен. | Нет |
| Q-010-4 | Тарифицировать ли хранение/превью отдельно от генерации? | Open | Дефолт **нет**: генерация — обычный chat-шаг (1 кредит, [ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)); хранение/превью бесплатны на старте. Пересмотр при росте объёма. | Нет |
| Q-017-1 | Конкретный домен сервиса для Traefik-роутинга (`Host(...)` rule + ACME-сертификат) ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). | **Closed (2026-06-02, значение пользователя)** | **`SERVICE_DOMAIN=broadnova.shop`** (HTTPS `https://broadnova.shop`). Предзапусковый операционный пункт: A-запись `broadnova.shop` → `87.239.135.154` (нужна для ACME-challenge внешнего Traefik) — см. [07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch). | Нет (решено) |
| Q-017-2 | Имя ACME-certresolver общего Traefik (`/opt/edge`) для label `tls.certresolver` ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). | **Closed (2026-06-02, значение владельца сервера)** | **`TRAEFIK_CERTRESOLVER=le`** (`--certificatesresolvers.le.acme...`). Нюанс: `le` сделан **дефолтным** на entrypoint `websecure` (`--entrypoints.websecure.http.tls.certresolver=le`), поэтому label `tls.certresolver` в нашем `docker-compose` **опционален** (сертификат выпустится автоматически для любого HTTPS-роутера). Явный label `traefik.http.routers.<service>.tls.certresolver=le` **рекомендован для надёжности**. | Нет (решено) |

## Открытые вопросы встроенного auth-issuer (2026-06-02, [ADR-018](adr/ADR-018-embedded-auth-issuer.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-018-1 | Усиление анти-Sybil для анонимной device-регистрации: достаточно ли per-IP rate-limit, нужен ли App Attest / DeviceCheck? | Open | Дефолт **per-IP rate-limit** (`AUTH_RATE_LIMIT_PER_IP=10/min`). App Attest/DeviceCheck — post-MVP усиление, путь не закрыт. Реализуемо на дефолте. | Нет |
| Q-018-2 | Апгрейд идентичности: добавлять ли email/пароль и/или Apple Sign-In (перенос идентичности между устройствами, account recovery)? | **Open — для пользователя** | Дефолт **device-based only на MVP** ([ADR-018](adr/ADR-018-embedded-auth-issuer.md)). Email/пароль — опциональное расширение (можно добавить `users.email`/`password_hash` без слома device-based); Apple Sign-In — через verify-only режим внешнего issuer (`JWT_JWKS_URL` сохранён). Путь не закрыт. | Нет (MVP реализуем на device-based) |

## Открытые вопросы расширения Figma-gap (2026-06-02)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-012-1 | Состав tool-реестра по `assistant_mode`: какие tools доступны Claude в `chat` vs `code` ([ADR-012](adr/ADR-012-assistant-mode-vs-billing-mode.md))? | Open | Дефолт: `code` — полный реестр (`files.*`, `site.*`, `calendar.*`, `reminders.*`); `chat` — без `site.*`/`files.*` (только `calendar.*`/`reminders.*`). Конфигурируемо. Реализуемо на дефолте. **Cross-ref [ADR-022](adr/ADR-022-optional-project-and-tool-gating.md):** `site.*` дополнительно гейтится наличием `project_id` поверх этого фильтра — итог `(assistant_mode допускает site.*) AND (project_id IS NOT NULL)` (И-композиция; фильтр по `assistant_mode` — не единственный). | Нет |
| Q-013-1 | Стратегия инъекции `workspace_files`/больших контекстов в prompt при росте объёма (полный текст vs усечение vs RAG) ([ADR-013](adr/ADR-013-workspace-projects-vs-website-builder.md)). | Open | Старт: простая вставка `extracted_text` с лимитом на суммарный размер контекста; усечение сверх лимита. RAG — будущее. | Нет |
| Q-014-1 | Полный allowlist media_type вложений (нужны ли DOCX/CSV/HEIC и пр.) ([ADR-014](adr/ADR-014-multimodal-attachments.md)). | **Заменён [Q-020-1](#q-020-1--allowlist-mediatype-вложений-mvp)** (ADR-014 superseded транспортом [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | — | Нет |
| Q-014-2 | Точные лимиты вложений (размер/число на сообщение) ([ADR-014](adr/ADR-014-multimodal-attachments.md)). | **Заменён [Q-020-2](#q-020-2--лимиты-вложений-mvp)** (ADR-014 superseded транспортом [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | — | Нет |
| **Q-015-1** | **Покупка токенов (consumable IAP) без активной подписки — продуктовое противоречие монетизации MVP.** | **Closed (2026-06-02, решение пользователя = вариант B)** | **Покупка токенов требует активной подписки** (докупка сверх месячного пакета). Без активной подписки `POST /v1/tokens/purchase` → `403 subscription_required` (policy-guard **до** grant). Сохраняет §2 ТЗ и [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений; устраняет «мёртвый» баланс. См. [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md). | Нет (решено) |
| **Q-016-1** | **Actions** (Plan Week, Meeting Notes, Tasks from Photo, Summarize Text, Project Structure, Daily Review, Design Brief) и **Choose style** (Normal/Learning/Concise/Formal): серверные пресеты (endpoint + хранение) ИЛИ клиентские (iOS подставляет промпт)? | **Open — для пользователя** | Дефолт **клиентские пресеты**: iOS формирует готовый промпт/`assistantMode`/`context`, backend получает уже готовое сообщение — **новый backend-эндпоинт не нужен**. Стиль ответа можно передавать через `context.style` (информативно). Реализуемо без backend-работы. | Нет |
| **Q-016-2** | **Web search** (toggle в Add To Chat): backend-tool (серверная интеграция веб-поиска, как `site.*`) ИЛИ клиентская фича? | **Open — для пользователя, НЕ реализуем до ответа** | Дефолт-намерение: backend server-side tool, но требует **выбора провайдера поиска** (Brave/Bing/Anthropic web_search и т.п.) и тарификации → **не реализуем до ответа пользователя**. Без выбора провайдера — нет контракта tool. | **Да — блокер для фичи web search** (не для остального) |

## Открытые вопросы мультимодальных вложений (MVP, 2026-06-03, [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-020-1 | Полный allowlist `mediaType` вложений: нужны ли DOCX/XLSX/HEIC/SVG и пр.? | Open | Старт: `image/jpeg|png|gif|webp`, `application/pdf`, `text/plain|markdown|csv`, `application/json`. Расширение по мере необходимости. Реализуемо на дефолте. | Нет |
| Q-020-2 | Точные значения `ATTACHMENT_*` лимитов (размер одного/суммарный, число, PDF-страницы, повышенный body-лимит `/chat/run`). | Open | Дефолты: `ATTACHMENT_MAX_COUNT=10`, `ATTACHMENT_MAX_BYTES_IMAGE=5 MB` / `ATTACHMENT_MAX_BYTES_DOCUMENT=8 MB`, `ATTACHMENT_TOTAL_BYTES=10 MB`, `ATTACHMENT_PDF_MAX_PAGES=100`, `ATTACHMENT_REQUEST_BODY_LIMIT=12 MB`. Конфигурируемо ([TD-004](100-known-tech-debt.md)-аналог — не калибровано на нагрузке). Согласованы с лимитами Anthropic по vision/PDF. | Нет |

### Q-020-1 — allowlist mediaType вложений (MVP)
Заменяет Q-014-1 (транспорт ADR-014 superseded). Дефолтный allowlist покрывает сценарии дизайна (фото, PDF, текстовые/csv/json-файлы). DOCX/XLSX требуют конвертации/извлечения — вне MVP. HEIC (iOS-фото) — кандидат на расширение, но iOS может конвертировать в JPEG на клиенте; решение отложено.

### Q-020-2 — лимиты вложений (MVP)
Заменяет Q-014-2. Дефолты эвристические, согласованы с ограничениями Anthropic Messages API (vision-изображения и PDF-document по размеру/разрешению). Калибровка по прод-метрикам — как [TD-004](100-known-tech-debt.md).

## Открытый вопрос консистентности run↔история ([ADR-024](adr/ADR-024-history-payload-domain-normalization.md), 2026-06-10)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-024-1 | При `status=tool_call` `ChatResponse` сейчас **отбрасывает** сопутствующий текст, который Claude выдал вместе с `tool_use` (`orchestrator.py:661`). Должен ли `/chat/run` (и `/chat/tool-result`) ТАКЖЕ возвращать этот текст как опц. `assistantMessage` при `tool_call` — для полной консистентности с историей (где шаг хранит `[text, tool_use]` вместе)? | **Closed (2026-06-10, решение пользователя = вариант A)** | **Вариант A:** при `status=tool_call` ответ `/chat/run` и `/chat/tool-result` опционально возвращает `assistantMessage` — текст из `text`-блоков того же assistant-шага (того, чей `tool_use` вернулся как `toolCall`). При отсутствии текста — `null`/опущено. Аддитивно/обратносовместимо (поле уже nullable; меняется лишь то, что теперь может быть НЕ-null при `tool_call`). Backend перестаёт отбрасывать текст (`orchestrator.py:661`). Семантика `assistantMessage` при `assistant_message` (финал) и `blocked` (`null`) — без изменений. См. [ADR-024 §Decision п.3](adr/ADR-024-history-payload-domain-normalization.md), [chat-orchestrator/02-api-contracts.md](modules/chat-orchestrator/02-api-contracts.md#response-200). | **Нет для ADR-024** (нормализация истории — нестыковки 1/2 + полнота на стороне истории — закрыта независимо). Влияет только на enrichment `ChatResponse`. |

### Q-024-1 — сопутствующий текст при `tool_call` (run↔история)
Нестыковка 3 из [ADR-024](adr/ADR-024-history-payload-domain-normalization.md): assistant-ход может нести в одном сообщении и `text`, и `tool_use`; история отдаёт оба блока шага (после доменной нормализации), а `ChatResponse` при `status=tool_call` возвращает только `toolCall`, теряя текст. ADR-024 **на стороне истории** уже даёт полный шаг (клиент берёт текст из `steps[].payload` по `stepId`). Остаётся продуктовый вопрос — дотягивать ли сам `ChatResponse`:
- **Вариант A (рекомендован):** добавить опц. `assistantMessage` в ответ при `status=tool_call`; backend перестаёт отбрасывать текст (`orchestrator.py:661`), кладёт его в `assistantMessage`. Плюс: прогресс-UI сразу показывает «Claude сказал X и вызвал инструмент Y» без запроса истории. Аддитивно (поле уже опционально).
- **Вариант B:** не менять `ChatResponse`; задокументировать, что полный ход доступен в истории по `stepId`/`messageStepId` ([ADR-023](adr/ADR-023-sync-ids-in-chat-response.md)). Минус: доп. round-trip/задержка для показа сопутствующего текста.

**Resolution (2026-06-10, решение пользователя = вариант A).** При `status=tool_call` `ChatResponse` (`/chat/run` и `/chat/tool-result`) **опционально** возвращает `assistantMessage` — текст из `text`-блоков **того же** assistant-шага, чей `tool_use` вернулся как `toolCall` (тот же шаг, на который указывает `stepId`, [ADR-023](adr/ADR-023-sync-ids-in-chat-response.md)). Значение = конкатенация/текст этих `text`-блоков; при их отсутствии `assistantMessage = null`/опущено. Backend перестаёт отбрасывать текст (`orchestrator.py:661`) и кладёт его в `assistantMessage`. Согласовано с нормализацией истории: текст в `GET /v1/chats/{id}` → `steps[].payload.content[]` того же шага (по `stepId`) — **тот же**. Изменение аддитивно/обратносовместимо: поле уже было опционально-nullable; меняется лишь то, что оно теперь может быть НЕ-null при `tool_call`; остальные поля `tool_call` (`toolCall` обязателен, `toolCall.id`/`name` доменные) не меняются. `assistantMessage` при `status=assistant_message` (финал) — без изменений; при `status=blocked` — `null`. Cross-ref: [ADR-024 §Decision п.3](adr/ADR-024-history-payload-domain-normalization.md), [chat-orchestrator/02-api-contracts.md §Response (200)](modules/chat-orchestrator/02-api-contracts.md#response-200), [chat-orchestrator/09-testing.md §ADR-024](modules/chat-orchestrator/09-testing.md), [API-REFERENCE.md §5](API-REFERENCE.md).

## Q-015-1 — продуктовое противоречие монетизации MVP (Closed)

**Статус:** Closed — 2026-06-02, решение пользователя. **Выбран вариант (б): покупка токенов требует активной подписки** (докупка сверх месячного пакета).

**Выявлено qa+reviewer (2026-06-02)** при синхронизации реализованного token-purchase с policy: при прежнем дефолте «покупка разрешена всем» пользователь без подписки мог купить кредиты, но не мог их потратить (потребление в `credits`-mode гейтится [ADR-002](adr/ADR-002-access-policy-state-machine.md): без подписки доступен только trial) — «мёртвый» баланс.

**Принятое решение (вариант б).** Покупка токенов осмысленна только при активной подписке: `POST /v1/tokens/purchase` гейтится policy-guard'ом — `subscription.status == active` проверяется **до** `WalletService.grant`. Без активной подписки → HTTP `403` с `code=subscription_required`, кредиты **не** начисляются, ledger не пишется. Покупка = докупка кредитов **сверх** `SUBSCRIPTION_CREDITS_PER_PERIOD` для подписчиков. Это **сохраняет** §2 ТЗ и [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений (policy-engine и правило потребления не меняются), устраняет «мёртвый» баланс. Вариант (а) (policy-патч, разблокировка `credits`-mode без подписки покупкой) — **отвергнут**.

**Cross-ref:** [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md), [modules/token-purchase/README.md](modules/token-purchase/README.md), [modules/token-purchase/02-api-contracts.md](modules/token-purchase/02-api-contracts.md), [figma-gap-analysis.md](figma-gap-analysis.md#mvp-scope-решение-пользователя-2026-06-02), [API-REFERENCE.md §20](API-REFERENCE.md#20-tokens).

## Q-017-1 / Q-017-2 — deploy-значения домена и Traefik certresolver (Closed)

**Статус:** Closed — 2026-06-02, финальные deploy-значения от пользователя/владельца сервера ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)).

**Q-017-1 (домен).** `SERVICE_DOMAIN=broadnova.shop`, публичный URL `https://broadnova.shop`. Предзапусковое операционное требование (не код): A-запись `broadnova.shop` → `87.239.135.154` должна существовать **до** запуска — она нужна внешнему Traefik для ACME HTTP-challenge при выпуске Let's Encrypt-сертификата. Остаётся пунктом [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).

**Q-017-2 (certresolver).** `TRAEFIK_CERTRESOLVER=le` (`--certificatesresolvers.le.acme...` в конфиге общего Traefik `/opt/edge`). Владелец сервера сделал `le` **дефолтным** резолвером на entrypoint `websecure` (`--entrypoints.websecure.http.tls.certresolver=le`), поэтому сертификат выпускается автоматически для любого HTTPS-роутера, и label `traefik.http.routers.<service>.tls.certresolver=le` в нашем `docker-compose` **опционален**. Тем не менее явный label **рекомендован для надёжности** (детерминированность при возможном изменении дефолта на entrypoint).

**Cross-ref:** [ADR-017](adr/ADR-017-shared-server-traefik-deploy.md), [07-deployment.md](07-deployment.md#маршрутизация-через-traefik-docker-labels), [07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).

## Блокеры (для orchestrator)
- ~~**Q-015-1 (покупка токенов × policy)**~~ — **Closed (2026-06-02, вариант б):** покупка токенов требует активной подписки (`403 subscription_required` до grant), [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений. Требует backend-доработки: policy-guard перед `WalletService.grant` в token-purchase. См. [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md).
- **Q-016-2 (web search)** — блокирует **только** фичу веб-поиска: нет выбора провайдера → нет контракта server-side tool. Остальное расширение не блокирует.
- **Q-016-1 (Actions/styles)** — помечен для пользователя; backend реализуем на дефолте (клиентские пресеты), ответ влияет на детали (нужен ли endpoint пресетов).
- ~~**Q-017-1 / Q-017-2 (домен + certresolver)**~~ — **Closed (2026-06-02, значения пользователя/владельца):** `SERVICE_DOMAIN=broadnova.shop`, `TRAEFIK_CERTRESOLVER=le` (default на `websecure`). Предзапусковый операционный пункт — A-запись `broadnova.shop` → `87.239.135.154` (для ACME) — остаётся в [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).
- ~~**Q-005-1 (JWT issuer)**~~ — **Closed (2026-06-02):** встроенный issuer в backend ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), модуль [auth](modules/auth/README.md)), device-based. Prod-блокер «реальный issuer» снят; остаётся операционный пункт «сконфигурировать приватный ключ подписи» в [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch). Backend-доработка: модуль auth + миграция 0005 ([modules/auth/07-implementation-phases.md](modules/auth/07-implementation-phases.md)).
- ~~**Q-024-1 (сопутствующий текст при `tool_call`)**~~ — **Closed (2026-06-10, вариант A):** при `status=tool_call` `ChatResponse` опционально возвращает `assistantMessage` (текст того же assistant-шага); backend перестаёт отбрасывать текст (`orchestrator.py:661`). Аддитивно/обратносовместимо. Не блокировал [ADR-024](adr/ADR-024-history-payload-domain-normalization.md) (нормализация истории внедрена независимо). Требует backend-доработки: заполнять `assistantMessage` из `text`-блоков того же assistant-шага при `tool_call`.
- Прочих активных блокеров нет. Q-004-1 и Q-006-1 закрыты [ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md).
- Admin (Фича A) и website-builder (Фича B) спроектированы с разумными дефолтами; открытые вопросы Q-009-* / Q-010-* неблокирующие.
