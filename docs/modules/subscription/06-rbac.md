# Subscription — RBAC

## Роль
- `user` — синхронизирует только свою подписку.

## Правила
- `POST /v1/subscription/sync` — `userId` == `sub` JWT.
- Транзакция верифицируется server-side; клиентскому статусу не доверяем.
- Нельзя синхронизировать подписку за другого пользователя.
