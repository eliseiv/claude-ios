# Module: Billing — Adapty

- Статус: Реализован ([ADR-029](../../adr/ADR-029-adapty-subscription-webhook.md)/[ADR-046](../../adr/ADR-046-adapty-webhook-outcome-logging.md)/[ADR-047](../../adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md)/[ADR-055](../../adr/ADR-055-adapty-webhook-user-resolution-via-auth-devices.md), код `src/app/billing_adapty/`); **[ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) — спроектирован, реализация не выполнена**.
- Ответственность: приём серверного вебхука Adapty (`POST /v1/billing/adapty/webhook`), идемпотентное обновление подписки и грант кредитов по тиру продукта; начисление пакетов токенов по `non_subscription_purchase` ([ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) §E). Основной путь биллинга по подпискам ([ADR-029](../../adr/ADR-029-adapty-subscription-webhook.md)).

## Документы
- [00-overview.md](00-overview.md) · [01-context.md](01-context.md) · [02-api-contracts.md](02-api-contracts.md) · [03-architecture.md](03-architecture.md) · [04-data-model.md](04-data-model.md) · [06-rbac.md](06-rbac.md) · [07-implementation-phases.md](07-implementation-phases.md) · [08-observability.md](08-observability.md) · [09-testing.md](09-testing.md)

## DoD
- Эндпоинт авторизуется статическим bearer-секретом (constant-time); неверный/нет токена → 401; секрет не задан → 500.
- После авторизации любое тело (пустое/не-JSON/неполное/неизвестное событие/дубликат) → `2xx` (Adapty сохраняет вебхук и не зацикливает ретраи). `5xx` только при реальном сбое.
- GRANTING (`trial_started`/`subscription_started`/`subscription_renewed`/`access_level_updated`@premium) → `status=active` + идемпотентный грант по тиру; EXPIRING (`subscription_expired`/`subscription_cancelled`/`access_level_updated`@is_active=false) → `status=expired`, кредиты не трогаются; **NOOP** (`*_renewal_cancelled`) → доступ НЕ отзывается, кредиты не трогаются ([ADR-047](../../adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md)).
- Идемпотентность ([ADR-047](../../adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md), [ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) §A): дедуп события по `profile_event_id`; **грант — один на период покупки по любому каналу**: при настоящем `transaction_id` ключ `sub-grant:{transaction_id}` — общий со StoreKit `sync`; до гранта проверяются `sub-grant:{T}` и исторический `adapty-txn:{T}`; занятый ключ = «уже начислено» независимо от суммы (без не-`2xx`). Фолбэк без `transaction_id` — `adapty-txn:{original_transaction_id ‖ event_id}`.
- Резолв пользователя ([ADR-055](../../adr/ADR-055-adapty-webhook-user-resolution-via-auth-devices.md), [ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) §B): общий `billing_common/resolve.py::resolve_user` (`users` → `lower(auth_devices.device_id)` → `legacy_user_ids`) — сначала по `customer_user_id`, затем, если его нет или он не резолвнут, по Adapty `profile_id`; начисление на резолвнутый `userId`; `missing_customer_user_id` — нет ни одного идентификатора; `user_not_found` — идентификатор есть, но ни один не резолвнут.
- Audit-событие `adapty_subscription` пишется через `assert_no_secrets`.

## Границы (см. [00-overview.md](00-overview.md))
- Пакеты токенов: **покрывает** `non_subscription_purchase` ([ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) §E) — сумма только из серверного каталога, ключ `token-purchase:{transaction_id}` общий с `POST /v1/tokens/purchase`, активная подписка **не** требуется. Возвраты (`*_refunded`) не обрабатываются — [TD-054](../../100-known-tech-debt.md).
- **НЕ** ломает `/v1/subscription/sync` (StoreKit JWS) — он остаётся; двойное начисление между `sync` и вебхуком исключено общим ключом периода ([ADR-106](../../adr/ADR-106-apple-billing-single-grant.md) §A), а не требованием «один путь на клиенте». Ретирование `sync` — [Q-029-2](../../99-open-questions.md).

## Changelog
- 2026-06-12: проектирование (architect), [ADR-029](../../adr/ADR-029-adapty-subscription-webhook.md). Новая таблица `adapty_webhook_events` (миграция `0008`), env `ADAPTY_WEBHOOK_SECRET` / `ADAPTY_PRODUCT_TOKENS` / `ADAPTY_SUBSCRIPTION_TOKENS_GRANT`. Заведены [Q-029-1/2/3](../../99-open-questions.md), [TD-020/021](../../100-known-tech-debt.md).
- 2026-06-30: наблюдаемость (architect), [ADR-046](../../adr/ADR-046-adapty-webhook-outcome-logging.md). Структурный лог исхода `handle()` (`"adapty_webhook_outcome"`, allowlist `result`/`reason`/`eventType`/`eventId`/`customerUserId`, уровни INFO/WARNING/DEBUG) — закрывает слепое пятно инцидента (промо-подписка → `ignored` без лога причины). Новый [08-observability.md](08-observability.md), Фаза 7 в [07](07-implementation-phases.md). Без миграции/env/контракта. [Q-029-3](../../99-open-questions.md) частично адресован (наблюдаемость), маппинг событий остаётся открытым.
- 2026-07-09: резолв пользователя deviceId→userId через `auth_devices` (architect), [ADR-055](../../adr/ADR-055-adapty-webhook-user-resolution-via-auth-devices.md). Инцидент avelyra: Adapty шлёт в `customer_user_id` deviceId → `handle()` искал его только в `users` → `ignored/user_not_found` → подписка/грант не применялись (подписка `894edaee` истекла при активных `subscription_renewed`). Тот же баг, что [ADR-053](../../adr/ADR-053-cloudpayments-webhook-user-resolution-via-auth-devices.md) в CloudPayments — не был перенесён из-за дублирования. Резолв вынесен в общий `src/app/billing_common/resolve.py::resolve_user` (оба вебхука), Adapty Stage 3 `_user_exists`→`resolve_user`, начисление на резолвнутый `userId`; наблюдаемость `resolvedVia`/`resolvedUserId`. Без миграции/env, контракт/HTTP/схема/идемпотентность/маппинг неизменны. Заведён [Q-055-1](../../99-open-questions.md) (`non_subscription_purchase`). Ретроактив (оператор, не код): продлить `894edaee`/avelyra, разблокировать `2c10eec7`/orvianix через [ADR-048](../../adr/ADR-048-admin-subscription-grant.md).
- 2026-06-30: исправление парсера/маппинга/идемпотентности по реальным payload'ам (architect), [ADR-047](../../adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md). `event_id`←`profile_event_id`; дефенсивный парсинг `event_properties.*` + новые поля (`transaction_id`/`is_active`/`access_level_id`/`will_renew`); маппинг `classify_event` GRANTING/EXPIRING/**NOOP** (`*_renewal_cancelled` доступ НЕ отзывает; `access_level_updated` условно по `is_active`/`premium`); **грант идемпотентен по `adapty-txn:{transaction_id}`** (один грант на период, дедуп события — отдельно по `profile_event_id`); грант на trial. Фаза 8 в [07](07-implementation-phases.md). Без миграции/env, контракт/HTTP/схема неизменны. [Q-029-3](../../99-open-questions.md) — маппинг определён, верификация wire-формата по прод-логам; новый [Q-047-1](../../99-open-questions.md) (персист `will_renew`, отложено).
- 2026-09-07: источник числа кредитов — единый резолвер **оверлей `admin_products` → `ADAPTY_PRODUCT_TOKENS` → `ADAPTY_SUBSCRIPTION_TOKENS_GRANT`** (architect), [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md): оператор правит тир продукта из CRM без деплоя. Пустой оверлей = сегодняшнее поведение бит-в-бит; ключ идемпотентности, парсинг и маппинг событий не меняются; архивный продукт **продолжает начислять**. Заведён [09-testing.md](09-testing.md).
- 2026-09-15: одна оплата Apple — одно начисление (architect), [ADR-106](../../adr/ADR-106-apple-billing-single-grant.md), docs-only. §A — ключ гранта при настоящем `transaction_id` `sub-grant:{transaction_id}` (общий со StoreKit `sync`), проверка исторического `adapty-txn:{T}`, занятый ключ = «уже начислено» без не-`2xx`; §B — резолв по `profile_id` после `customer_user_id`, поля лога `resolvedFrom`/`profileId`; §C — событие с более ранним сроком при активной подписке не меняет строку `subscriptions` (GRANTING/EXPIRING/NOOP), грант решает ключ; §D — WARNING и аудит `subscription_product_unmapped` при фолбэке `ADAPTY_SUBSCRIPTION_TOKENS_GRANT`; §E — `non_subscription_purchase` → пакет по `one_time_credits` под `token-purchase:{transaction_id}`, `ignored/missing_transaction_id`, `ignored/unknown_product` (WARNING). Без миграции и env. Закрыты [Q-055-1](../../99-open-questions.md), [Q-029-1](../../99-open-questions.md); заведены [TD-054](../../100-known-tech-debt.md), [TD-055](../../100-known-tech-debt.md). Статус модуля приведён к коду (был «ожидает реализации»). Обновлены 00–04, 07–09.
