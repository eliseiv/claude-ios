# Module: Admin

- Статус: Реализован
- Ответственность: операторские/саппорт-действия над аккаунтами под изолированной admin-авторизацией. Начисление кредитов (`wallet/grant`), read-only просмотр кошелька, ручная активация/продление подписки (`subscription/grant`, [ADR-048](../../adr/ADR-048-admin-subscription-grant.md)), read-only периодная разбивка расходов на провайдеров для CRM (`costs/daily`, [ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md)), **удалённое управление экономикой и продуктовыми настройками инстанса** (`products`/`pricing`/`capabilities`/`settings`, [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md) — спроектировано, реализация pending).

> **Про CRM-подмножество `/v1/admin/*`.** Эндпоинты панели broad-crm (`GET /v1/admin/users`, карточка, оплаты, запросы, `products`, `stats`, гранты токенов/подписки) живут в `src/app/api_gateway/routers/crm_admin.py` под тем же `require_admin`, но их контракты в этих module-docs **не описаны** — первоисточник для них [ADR-077](../../adr/ADR-077-crm-request-logs.md) / [ADR-078](../../adr/ADR-078-crm-request-history-derived-from-domain.md) / [ADR-079](../../adr/ADR-079-crm-provider-cost-duration-payments.md) и [08-api-documentation §CRM Admin API](../../08-api-documentation.md#crm-admin-api-история-запросов-adr-077-модель-чтения--adr-078). Исключение — `costs/daily` ([ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md)): его контракт описан здесь целиком, в [02-api-contracts.md](02-api-contracts.md#get-v1admincostsdaily--периодная-разбивка-расходов-adr-092).

## Документы
- [00-overview.md](00-overview.md)
- [01-context.md](01-context.md)
- [02-api-contracts.md](02-api-contracts.md)
- [03-architecture.md](03-architecture.md)
- [06-rbac.md](06-rbac.md)
- [07-implementation-phases.md](07-implementation-phases.md)
- [09-testing.md](09-testing.md)

## DoD
- `POST /v1/admin/wallet/grant` начисляет кредиты через существующий `WalletService.grant()`, идемпотентно по `idempotencyKey`, с обязательным `reason`; ответ `{newBalance, ledgerTxId, idempotentReplay}`.
- `GET /v1/admin/wallet/{userId}` отдаёт баланс + последние ledger-транзакции (read-only).
- `POST /v1/admin/subscription/grant` ([ADR-048](../../adr/ADR-048-admin-subscription-grant.md)) активирует/продлевает подписку (upsert `subscriptions` `status='active'`, `plan`, `expires_at`) **без** StoreKit-транзакции; срок = ровно одно из `expiresAt` (tz-aware, `>now()`) / `days` (>0); опц. начисление кредитов в том же запросе (дефолт `SUBSCRIPTION_CREDITS_PER_PERIOD`, явный `0` = без начисления) через `WalletService.grant`, идемпотентно по `idempotencyKey`; `404 user_not_found`; всё в одной транзакции; audit `admin_subscription_grant`.
- `GET /v1/admin/costs/daily` ([ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md)) отдаёт клетки `(день, провайдер)` за период (UTC, обе границы включительно, ≤ 92 дня, порядок `date ASC, provider ASC`): `requests` = число **оплаченных обращений к провайдеру**, `spend_usd`/`tokens` по закупочному прайсу [ADR-079](../../adr/ADR-079-crm-provider-cost-duration-payments.md) (`Fal` → `tokens = 0.0`); `null` — только когда не оценён ни один вызов клетки, «нет строки» — измеренный ноль; неатрибутируемый вызов (`usage` есть, имени модели нет) отдаётся отдельной клеткой с сырым ключом `"Unknown"`; `400` на невалидном периоде (в т.ч. на `2026-8-1` — форма строго 4/2/2 цифры), `422` на схеме, `404` **не возникает**. Имена пути/параметров/полей заморожены контрактом CRM v1.3 и из этого репозитория не меняются.
- Авторизация — изолированный admin-секрет ([ADR-009](../../adr/ADR-009-admin-token-auth.md)) в заголовке `X-Admin-Key` (CRM) или легаси `X-Admin-Token`, отдельная зависимость `require_admin` (нет заголовка → `403`, неверное значение → `401`), не пересекается с пользовательским JWT, не запускает provisioning, не трогает trial.
- Аудит-событие `admin_grant` (actor=admin, reason, без секрета). Отдельный rate limit, strict validation, size-лимиты.

## Changelog
- 2026-06-01: bootstrap модуля (architect). Зафиксированы [ADR-009](../../adr/ADR-009-admin-token-auth.md) (admin-auth), контракты grant/get-wallet, RBAC, фазы, тесты. Scope backend.
- 2026-06-01: реализован backend (`src/app/api_gateway/routers/admin.py`, `src/app/admin/service.py`): `POST /v1/admin/wallet/grant` + `GET /v1/admin/wallet/{userId}` под `require_admin`/`X-Admin-Token`, audit `admin_grant`, отдельный rate limit. Отревьюен и протестирован — offline-сьют зелёный (455/455, вкл. e2e admin-grant/get-wallet). Статус → «Реализован».
- 2026-07-01: спроектирован [ADR-048](../../adr/ADR-048-admin-subscription-grant.md) — `POST /v1/admin/subscription/grant` (ручная активация подписки без StoreKit, опц. начисление кредитов). Обновлены 02-api-contracts / 03-architecture / 07-implementation-phases (фаза ADM-8) / 09-testing (сценарии subscription/grant: integration/policy-e2e/validation/security). Scope backend (ТЗ в ADR-048). Реализация — pending.
- 2026-08-26: **`GET /v1/admin/costs/daily` реализован** (`crm_daily_costs` в `src/app/api_gateway/routers/crm_admin.py`, `src/app/admin/crm_costs.py`, `CrmAdminService.daily_costs`, `CrmDailyCostItem` в `src/app/schemas/crm_admin.py`, индекс `ix_steps_created_at` / миграция `0029_steps_created_idx`) — реализация замороженного контракта CRM v1.3. Решения, оставленные контрактом на бэк, зафиксированы [ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md); обновлены README / 00-overview / 02-api-contracts / 03-architecture / 06-rbac / 07-implementation-phases (фаза ADM-9) / 09-testing. Заодно приведена к коду шапка 02-api-contracts / 06-rbac: заголовок `X-Admin-Key` и код `403` на отсутствие заголовка (`src/app/api_gateway/auth.py:126-141`) в docs отсутствовали.
- 2026-08-26: **все нормы [ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md) приведены к коду, эндпоинт покрыт тестами** — сверка по стабильному дереву. (1) §6 закрыта (фаза ADM-10): неатрибутируемый вызов (`usage` есть, `model` не строка) больше не отбрасывается, а заводит клетку `PROVIDER_UNKNOWN = "Unknown"` с `requests = N` при `spend_usd`/`tokens`, остающихся `null` (`src/app/pricing/provider_prices.py:129`, `src/app/admin/crm_costs.py:198-210`); фильтр `AND s.usage IS NOT NULL` (`crm_costs.py:111`) сохранён — шаг-объявление медиа-визарда в счёт не входит. (2) Новая фаза ADM-11: форма даты ужесточена регуляркой `_ISO_DATE_RE` = `[0-9]{4}-[0-9]{2}-[0-9]{2}` **до** `strptime` (в `_parse_date`, `crm_admin.py`), который ширину компонент не проверяет и на `2026-8-1` отдавал `200` вместо нормативного `400`; ужесточение изолировано `costs/daily` — `/stats` и `/users` разбирают границы функцией `_parse_dt` (там же), она не тронута. (3) Тесты: `tests/integration/test_crm_daily_costs_adr092.py` — 32 кейса (зона `qa`). Обновлены README / 02-api-contracts / 03-architecture / 07-implementation-phases / 09-testing, ADR-092, `adr/INDEX.md`.
- 2026-09-07: спроектировано **удалённое управление экономикой и настройками инстанса** (architect), [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md) — реализация замороженного контракта CRM v1.1+v1.2+v1.4+v1.5 (`GET/POST /v1/admin/products`, `PATCH /products/{id}`, `GET/PATCH /pricing`, `GET /capabilities`, `GET/PATCH /settings`). Три таблицы-**оверлея** (`admin_products`/`admin_tariffs`/`admin_settings`, миграция `0033_admin_economics`, expand-only, **без backfill** — пустой оверлей воспроизводит сегодняшний день бит-в-бит), порядок разрешения «оверлей → env → дефолт кода», снимок в процессе с окном `ADMIN_OVERRIDES_REFRESH_SECONDS` (дефолт 30 с), из которого выводятся `effective_after_seconds`/`cache_effective_after_seconds`. Тариф чата переведён на **функцию модели** — закрывает [TD-025](../../100-known-tech-debt.md) и [Q-034-1](../../99-open-questions.md); изменения цены хода выписаны **предикатами, а не числами** ([§5.3](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)) — их несколько, и легаси-путь `/v1/chat/run` может **подорожать**, поэтому фактическая дельта снимается по данным шагом 1 плана выката ([§13](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)). Обновлены README / 02-api-contracts / 03-architecture / 06-rbac / 07-implementation-phases (фазы ADM-12…ADM-17) / 09-testing, [03-data-model](../../03-data-model.md), [API-REFERENCE](../../API-REFERENCE.md), [06-testing-strategy](../../06-testing-strategy.md), [01-architecture](../../01-architecture.md), [07-deployment](../../07-deployment.md) и docs шести затронутых модулей. Реализация — pending.
- 2026-09-08: **[ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md) реализован** (backend). Миграция `0033_admin_economics` (три таблицы-оверлея + ослабление `NOT NULL` у `audit_logs.user_id` — у правки каталога субъекта-пользователя нет; expand-only, без backfill). Новый пакет `src/app/instance_config/` — снимок в процессе, загрузка в lifespan до приёма трафика, фоновое обновление, резолверы «оверлей → env → дефолт кода». Восемь ручек в `src/app/api_gateway/routers/crm_admin.py` под `require_admin` + явный вызов `enforce_admin_economics_limits` на каждой; пишущая половина — `src/app/admin/economics_service.py`. Цена хода чата переведена на функцию модели (`chat_turn_credit_cost`), `Settings.chat_generation_credit_cost` удалён как второй механизм цены. Подключены пять путей начисления, витрина, 14 настроек, `prices[]` и вывод легаси-тройки медиа, пять метрик, **шесть** лог-событий, пять аудит-действий. Тесты — за `qa`.
- 2026-09-08 (поздний вечер): **`docs/` сверены с фактическим кодом волны [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)** (architect). Свип по форме «документ описывает состояние кода, которое с тех пор изменилось» дал **17** мест. Пять назвал `architect-reviewer` (константы `options`; видимость нормализации `general`; `patch_product`; `load_snapshot`; разность множеств `reason`), три — `qa` (они оказались подмножеством свипа, а не новым предметом), остальные девять не назвал никто: ADM-17 в [07-implementation-phases](07-implementation-phases.md), эта запись changelog, ловушка `db_error`→`unexpected` в [09-testing](09-testing.md), формулировка «состав логируется при каждом обновлении снимка» в [§2](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md) и пять адресных пакетов вне волны (ниже). ⚠️ **Вне волны, но сдвинуто ЕЮ:** гейты `_enforce_admin_economics_*`, добавленные в `crm_admin.py`, сместили всё ниже них примерно на 40 строк и обесточили **четыре** адреса в [ADR-092 §Статус реализации](../../adr/ADR-092-crm-daily-costs-endpoint.md) — приведены к именам символов там же; заодно переснят реестр долгов ([TD-032](../../100-known-tech-debt.md)/TD-033/TD-034/TD-039: адреса протухли, состав долгов не изменился, счёт TD-034 переснят и по-прежнему **11**). Все три требования к реализации, стоявшие в ADR как невыполненные, **закрыты `backend`**: `patch_product` ставит `source_kind_missing`/`source_tokens_missing`, `load_snapshot` различает сироту и отказ валидации, разность множеств «объявлено ↔ эмитируется» пуста в обе стороны (**10/10**). Перечень лог-событий переведён с состава на **признак** и вырос до **шести** — добавлен `admin_overrides_snapshot_changed` (носитель меры (б) [§2](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md), пишется на изменении состава оверлеев). Адреса кода в ADR-099 переведены с номеров строк на **имена символов** ([README §ссылка на код](../../README.md)): за сутки они протухали трижды от собственного дифа волны. Заведён [TD-042](../../100-known-tech-debt.md) — внутренние ссылки и имена env в сгенерированном OpenAPI (17 носителей, **вне** этой волны; правка кода этим выкатом не заказана).
- 2026-09-09 (ночь): **функциональный пробел архивации закрыт нормой; код и миграция — фронт работ [ADR-099 §14](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)** (architect, по major финального `reviewer`). `PATCH` продукта отвергал `400` правку **одного `archived`** на строке, чей источник не несёт `kind`/`credits`, — то есть на классе, который контрагент нормативно **обязан** присылать (`CRM ADR-073 §4`) и который у него **основной** (`CRM ADR-073 §Контекст`: 30 позиций из 39). Решение: три значимые колонки `admin_products` — **nullable** со смыслом «оверлей этого поля не задаёт» (§6.1, §9), пофилдовое слияние у каждого читателя строки, `tokens` в контракте отдаётся только при известном классе. Правка вносится **в саму `0033`** — она не выкачена ни на один из 41 инстанса. Перечень `reason` пересобран: `source_tokens_missing` **снят** (producer'а не остаётся), введён **`undeclared_bound`** — нижняя граница `tokens` продукта и тарифа даёт **`400`**, а не `422`: контракт разрешает `int≥0`, и CRM отклонить это в форме не могла (§11). [TD-040](../../100-known-tech-debt.md) закрывается выполнением §14; остаточное ограничение — [TD-043](../../100-known-tech-debt.md).
