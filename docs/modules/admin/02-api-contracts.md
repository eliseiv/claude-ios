# Admin — API Contracts

Все admin-эндпоинты под префиксом `/v1/admin/*`. Авторизация — изолированный admin-секрет
([ADR-009](../../adr/ADR-009-admin-token-auth.md)), зависимость `require_admin`. **Пользовательский
JWT не авторизует admin-действия.** Отдельный rate limit per source IP, `extra='forbid'`, тело
≤ 8 KB.

**Корзин лимита ДВЕ, и путь обязан знать свою** ([ADR-099 §10.1](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)):

| Корзина | Потолок | Пути |
|---|---|---|
| `rl:admin:{ip}` | `ADMIN_RATE_LIMIT_PER_MIN`, дефолт **10** | саппорт-действия: гранты кредитов и подписки, кошелёк, карточка пользователя, оплаты, запросы, `stats`, `costs/daily` |
| `rl:admin_econ:{ip}` | `ADMIN_ECONOMICS_RATE_LIMIT_PER_MIN`, дефолт **120** | восемь ручек экономики и настроек (`products`, `pricing`, `capabilities`, `settings`) |

Правило — **по форме вызова**: отдельную корзину получает путь, который CRM зовёт **пачкой при
отрисовке одной страницы** (6 вызовов на загрузку + правки), а не единичным операторским
действием. ⚠️ Лимит — **не middleware**, а явный вызов в каждом хендлере: путь, где его забыли,
остаётся без лимита **молча**.

**Заголовок и коды отказа (сверено с `src/app/api_gateway/auth.py:126-141`):** принимается
`X-Admin-Key` (заголовок CRM/broad-crm) **или** легаси `X-Admin-Token`; при наличии обоих
выигрывает `X-Admin-Key`. Ни одного заголовка → **`403`** (`{error.code:"forbidden"}`); заголовок
есть, значение не совпало → **`401`** (`{error.code:"unauthorized"}`); секрет на инстансе не
сконфигурирован → **`401`** (fail-closed, до чтения заголовков). Сравнение — constant-time,
поддержана ротация (`ADMIN_API_SECRET_PREV`).

## POST /v1/admin/wallet/grant
Начисление кредитов пользователю (саппорт/компенсация).

### Headers
- `X-Admin-Token: <ADMIN_API_SECRET>` (обязателен).

### Request
```json
{
  "userId": "uuid",
  "amount": 100,
  "idempotencyKey": "string",
  "reason": "string"
}
```
- `userId` — UUID существующего пользователя (см. Правила §Несуществующий userId).
- `amount` — целое **> 0** (BIGINT, целые кредиты, без дробей). `amount <= 0` → `422`.
- `idempotencyKey` — непустая строка, `max_length` 128. Ключ идемпотентности начисления (передаётся в `WalletService.grant(idempotency_key=...)`).
- `reason` — **обязателен**, непустая строка, `max_length` 512. Пишется в audit `admin_grant` и `ledger_transactions.meta`.

### Response (200)
```json
{
  "newBalance": 1100,
  "ledgerTxId": "uuid",
  "idempotentReplay": false
}
```
- `newBalance` — баланс после начисления.
- `ledgerTxId` — id `ledger_transactions` (`type=credit`).
- `idempotentReplay` — `true`, если ключ уже был использован с тем же payload (повторного начисления не было).

### Правила
- Переиспользует `WalletService.grant(user_id, amount, idempotency_key, meta, reason)` (`src/app/wallet/service.py`)
  — атомарно, идемпотентно по `(user_id, idempotency_key)`, пишет `ledger_transactions(type=credit)` + audit `billing_credit`.
- **Дополнительно** пишется audit-событие `admin_grant` (actor=admin, `userId`, `amount`, `reason`, `idempotencyKey`,
  `ledgerTxId`) — отдельно от `billing_credit`, фиксирует именно admin-инициацию. **Секрет `X-Admin-Token` в audit не пишется.**
- Идемпотентность: тот же `idempotencyKey` + тот же payload → тот же `ledgerTxId`, `idempotentReplay=true`, без повторного начисления.
- Тот же `idempotencyKey`, **другой** `amount` → `409` (конфликт, как в `WalletService.grant`), без начисления.
- **Несуществующий userId → `404 {error.code:"user_not_found"}`** (admin-grant **не создаёт** пользователей — см. 03-architecture; обоснование ниже).
- `reason` отсутствует/пустой → `422`.

## POST /v1/admin/subscription/grant
Ручная активация/продление подписки пользователю (саппорт/компенсация/тестирование) — **без** StoreKit-транзакции ([ADR-048](../../adr/ADR-048-admin-subscription-grant.md)).
Зачем отдельно от `wallet/grant`: по [ADR-002](../../adr/ADR-002-access-policy-state-machine.md) при `subscription_status=none` кредиты **не проверяются** — пользователь блокируется по `trial_used`
**даже с ненулевым балансом**, поэтому одного начисления кредитов недостаточно, нужна активная подписка.

### Headers
- `X-Admin-Token: <ADMIN_API_SECRET>` (обязателен).

### Request
```json
{
  "userId": "uuid",
  "expiresAt": "2026-12-31T23:59:59Z",
  "days": 30,
  "plan": "manual_grant",
  "idempotencyKey": "string",
  "credits": 1000
}
```
- `userId` — UUID существующего пользователя. Отсутствует → `404 user_not_found` (admin **не создаёт** пользователей, [ADR-007](../../adr/ADR-007-lazy-user-provisioning.md)).
- Срок — **ровно одно** из `expiresAt` / `days` (оба или ни одного → `422`):
  - `expiresAt` — ISO8601 datetime, **tz-aware** и **строго в будущем** (`> now()`). В прошлом/naive → `422`. Требование «в будущем» обязательно: policy-loader (`src/app/policy/loader.py`) применяет lazy-expiry — `active` c `expires_at <= now()` трактуется как `expired`, т.е. грант в прошлое не дал бы доступа.
  - `days` — положительный int (`> 0`); сервер вычисляет `expires_at = now() + days`. `≤ 0` → `422`.
- `plan` — опц. строка (`max_length` 128), метка плана. Дефолт `"manual_grant"`.
- `idempotencyKey` — **обязателен**, непустая строка (`max_length` 128). Ключ идемпотентности начисления кредитов.
- `credits` — опц. int `≥ 0`. **Опущено (null) → `SUBSCRIPTION_CREDITS_PER_PERIOD`** (тот же пакет, что даёт реальный период — активированная подписка сразу рабочая). Явный `0` → активировать подписку **без** начисления (у пользователя уже есть баланс). `< 0` → `422`.

### Response (200)
```json
{
  "status": "active",
  "expiresAt": "2026-12-31T23:59:59Z",
  "plan": "manual_grant",
  "creditsGranted": 1000,
  "newBalance": 1100,
  "ledgerTxId": "uuid",
  "idempotentReplay": false
}
```
- `status` — новый статус подписки (`"active"`).
- `expiresAt` — эффективный момент истечения (из `expiresAt` или `now()+days`), ISO8601 | `null`.
- `plan` — записанный план | `null`.
- `creditsGranted` — эффективно начисленная сумма (0, если не начислялось).
- `newBalance` / `ledgerTxId` / `idempotentReplay` — присутствуют (не `null`) **только** при `creditsGranted > 0`; иначе `null` (ledger-транзакции нет).

### Правила
- Upsert строки `subscriptions` (PK `user_id`): `status='active'`, `plan`, `expires_at`. Прямая запись через ORM `Subscription`, **без** StoreKit-верификации (в отличие от `/v1/subscription/sync`). Idempotent по PK: повтор перезаписывает те же значения.
- При эффективной сумме `> 0` — начисление через `WalletService.grant(...)` **как есть** (`src/app/wallet/service.py`): атомарно, идемпотентно по `(user_id, idempotency_key)`, ledger `credit` + audit `billing_credit`. Ledger-ключ **производный с namespace**: `admin-sub-grant:{idempotencyKey}` (не коллидирует с `admin/wallet/grant` и `sub-grant:{transaction_id}`).
- Тот же `idempotencyKey` c **другим** `credits` → `409` (из `WalletService.grant`), активации/начисления нет.
- **Дополнительно** пишется audit-событие `admin_subscription_grant` (actor=admin, `userId`, `plan`, `status`, `expiresAt`, `creditsGranted`, `idempotencyKey`, `ledgerTxId` при наличии). **Секрет `X-Admin-Token` не логируется/не в audit.**
- Всё (upsert + grant + оба audit) — в **одной** транзакции запроса.
- **Коды:** `200`; `401`; `404` (`user_not_found`); `409` (тот же `idempotencyKey`, другой `credits`); `422` (нет `userId` / оба|ни одного из `expiresAt`/`days` / `expiresAt` не tz-aware|в прошлом / `days ≤ 0` / `credits < 0` / схема); `429`; `5xx`.
- **OpenAPI-тексты** (`summary`, `description` роута, `Field(description=...)`, тег `Admin`) — по [08-api-documentation §R2ter](../../08-api-documentation.md#r2ter-лаконичность-user-facing-текстов-для-тестировщиков): лаконичные профессиональные формулировки для оператора, **без** ссылок `ADR-`/`Q-`/`TD-` и расшифровок-аббревиатур в скобках. Внутренняя мотивация (root-cause ADR-002, обоснование дефолтов, namespace-ключ) — только здесь и в [ADR-048](../../adr/ADR-048-admin-subscription-grant.md), **не** в OpenAPI-строках.

## GET /v1/admin/wallet/{userId}
Read-only просмотр кошелька для саппорта.

### Headers
- `X-Admin-Token: <ADMIN_API_SECRET>` (обязателен).

### Response (200)
```json
{
  "userId": "uuid",
  "balance": 1100,
  "lastTransactions": [
    { "id": "uuid", "type": "credit|debit", "amount": 100, "createdAt": "ISO8601", "meta": {} }
  ]
}
```
- Переиспользует `WalletService.get_wallet_view(user_id, last_n)` (дефолт `last_n=20`, по `created_at DESC`).
- `meta` — без секретов (usage/model/reason).

### Правила
- Несуществующий `userId` → `404 {error.code:"user_not_found"}` (read-only не создаёт пользователя).
- Только чтение; не мутирует состояние и не пишет мутирующий audit (логируется на уровне tool/request lifecycle).

## Обоснование «404 на несуществующем userId» (не admin-provisioning)
Admin-grant **не создаёт** пользователей. Причины:
- Источник истины идентичности — доверенный JWT issuer ([ADR-007](../../adr/ADR-007-lazy-user-provisioning.md));
  создание `users` из admin-API ввело бы второй, неаутентифицированный путь рождения идентичности и риск
  начисления на «фантомный» (опечатанный) `userId`.
- `404` делает опечатку в `userId` видимой оператору сразу, вместо молчаливого создания мусорного аккаунта с балансом.
- Реальные пользователи создаются лениво при первом аутентифицированном запросе (ADR-007); к моменту легитимного
  admin-grant пользователь, как правило, уже существует. Если нужно начислить «наперёд» — это отдельный продуктовый
  вопрос, не решается тихим созданием строки. См. [Q-009-2](../../99-open-questions.md) (не блокер; дефолт — `404`).

## GET /v1/admin/costs/daily — периодная разбивка расходов ([ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md))

Read-only. Сколько бизнес заплатил AI-провайдерам за каждый день выбранного периода и в чей счёт —
источник данных страницы CRM «Расход API».

> ⚠️ **Контракт заморожен НЕ здесь.** Путь, имена query-параметров, имена полей ответа, порядок
> сортировки, предел периода и семантика отсутствия заданы CRM (`broad-crm`, расширение контракта
> бэков **v1.3**, её `ADR-084 §1`) и **из этого репозитория не меняются**: обе стороны пишутся
> разными командами в разных репозиториях, и расхождение в имени поля даёт у оператора **молча
> пустой экран**, а не ошибку. Всё, что решено здесь (чем наполняются величины, когда `null`, какие
> коды отказа), — [ADR-092](../../adr/ADR-092-crm-daily-costs-endpoint.md).

### Headers
- `X-Admin-Key: <ADMIN_API_SECRET>` (или легаси `X-Admin-Token`) — см. коды отказа в шапке документа.

### Query
| Параметр | Тип | Обяз. | Значение |
|---|---|---|---|
| `date_from` | `YYYY-MM-DD` | **да** | Левая граница периода, **UTC, включительно** |
| `date_to` | `YYYY-MM-DD` | **да** | Правая граница периода, **UTC, включительно** |
| `limit` | int `1…1000` | нет | Размер страницы, дефолт **1000** |
| `offset` | int `≥ 0` | нет | Смещение страницы, дефолт **0** |

Максимальная длина периода — **92 дня** (`date_to − date_from + 1`).

### Response (200)
```json
{
  "total": 2,
  "items": [
    { "date": "2026-08-24", "provider": "OpenAI", "spend_usd": 0.0341, "requests": 87, "tokens": 412900.0 },
    { "date": "2026-08-24", "provider": "Fal",    "spend_usd": 0.78,   "requests": 3,  "tokens": 0.0 }
  ]
}
```
- `total` — число клеток `(день, провайдер)` за **весь** период, а не размер страницы: обходчик CRM
  листает до `len(items) < limit` и сверяет полноту обхода именно по нему.
- `date` — `YYYY-MM-DD`, календарь **UTC**.
- `provider` — **сырой** ключ бэка (`"OpenAI"` / `"Anthropic"` / `"Fal"` / `"Unknown"`).
  Нормализацию (`openai`/`anthropic`/`fal`/`other`) делает **потребитель**, не мы: незнакомый ключ
  CRM сводит в `other` и **не теряет**. `"Unknown"` — не вендор, а клетка неатрибутируемого трафика
  (см. Правила).
- `spend_usd` — `number | null`, USD, закупочные цены [ADR-079 §1](../../adr/ADR-079-crm-provider-cost-duration-payments.md).
- `requests` — `int`, число **оплаченных обращений к провайдеру** (см. Правила).
- `tokens` — `number | null`, токены, посчитанные провайдером (см. Правила).
- **Порядок — `date ASC, provider ASC`.** Пара `(date, provider)` в ответе уникальна, поэтому
  порядок полный и постраничная нарезка стабильна без дополнительного tie-break.

### Правила

- **`requests` считает ВЫЗОВЫ, а не ходы.** Шаг чата с непустым `usage` = один оплаченный вызов LLM
  (tool-loop одного хода — несколько вызовов, каждый оплачен отдельно); одна строка `media_jobs` =
  одна оплаченная генерация. Ход (`message_step_id`) — единица истории пользователя и отвечает на
  другой вопрос («за что списали кредиты»), здесь **не применяется**.
  > **Следствие:** дневная сумма `spend_usd` этого эндпоинта **≥** суммы колонки «Себестоимость»
  > вкладки «Запросы» за тот же день. Прайс и правило «модель → вендор» общие, расходится
  > гранулярность отбрасывания неоценимого: там отбрасывается целый **ход**, здесь — отдельный
  > **вызов**. Утверждать «ровно те же деньги» **нельзя**.
- **`tokens` — по конвенции прайс-строки.** Учитывается флаг `cache_read_in_input`
  ([ADR-079 §1](../../adr/ADR-079-crm-provider-cost-duration-payments.md)): OpenAI включает
  кэшированный префикс в `inputTokens`, и наивная сумма `input + cache_read` посчитала бы кэш
  **дважды** и разошлась бы со `spend_usd` той же клетки.
- **`Fal` отдаёт `tokens = 0.0`, а не `null`.** Ноль здесь — **измерение**: fal берёт деньги за кадры
  и секунды, токенов не считает вовсе. `null` объявил бы величину неизмеряемой и навсегда пометил бы
  токенную сводку CRM неполной у любого бэка, где есть генерации.
- **`null` ≠ `0` и ≠ «нет строки» — три разных случая, подменять нельзя:**

  | Что видит CRM | Что это значит |
  |---|---|
  | `404` на пути | Бэк **не реализует** расширение v1.3 (здесь неприменимо — реализует) |
  | `200`, строки за `(день, провайдер)` **нет** | Расхода в этот день по этому провайдеру **не было** — измеренный ноль |
  | `200`, строка есть, поле = `null` | Величина **не измерена**: не оценён **ни один** вызов клетки |

  Нулевые строки за каждый день × провайдера **не отдаются**. Частично оценённая клетка отдаёт
  сумму оценённого — честное занижение (наследуется от [ADR-079 §5](../../adr/ADR-079-crm-provider-cost-duration-payments.md):
  подставить ноль за неизвестную цену — записать факт, которого нет). `requests` у существующей
  клетки `null` не бывает: число обращений известно всегда.
- **Неатрибутируемый вызов отдаётся клеткой, а не исчезает** ([ADR-092 §6](../../adr/ADR-092-crm-daily-costs-endpoint.md)):
  вызов с `usage`, но без строкового имени модели → клетка с сырым ключом **`"Unknown"`**,
  `requests = N`, `spend_usd = null`, `tokens = null` (`src/app/admin/crm_costs.py:198-210`).
  Молчание превратило бы день, весь трафик которого неатрибутируем, в `$0.00` — то есть в
  измеренный ноль там, где расход был. `"Unknown"` — легальный сырой ключ: потребитель, не
  узнавший его, сводит клетку в `other` и не теряет.
  Отдельно: assistant-шаг **без** `usage` в `requests` **не входит и входить не должен** — это
  шаг-объявление медиа-визарда, а не вызов LLM; счёт удвоил бы уже посчитанную строку `media_jobs`.
  Поэтому список сырых ключей — **четыре**: `"OpenAI"`, `"Anthropic"`, `"Fal"`, `"Unknown"`.

### Коды

| Код | Когда |
|---|---|
| `200` | успех (в том числе `items: []` — за период расхода не было) |
| `400` | `date_from`/`date_to` не `YYYY-MM-DD`; `date_from > date_to`; период длиннее **92** дней |
| `401` | заголовок передан, значение не совпало; либо admin-секрет на инстансе не сконфигурирован |
| `403` | ни `X-Admin-Key`, ни `X-Admin-Token` не переданы |
| `422` | отсутствует обязательный `date_from`/`date_to`; `limit`/`offset` вне диапазона или не число (штатный конвейер валидации FastAPI) |
| `429` | admin rate limit |

> **`YYYY-MM-DD` — строго 4/2/2 ASCII-цифры.** Запись без ведущих нулей (`2026-8-1`), дата-время
> (`2026-08-10T00:00:00Z`) и любой другой формат → `400`. Форма проверяется отдельной регуляркой
> **до** разбора: `datetime.strptime` ширину компонент не проверяет и `2026-8-1` принял бы молча
> ([ADR-092 §5](../../adr/ADR-092-crm-daily-costs-endpoint.md)). Календарно невалидная дата
> правильной формы (`2026-02-30`) — тоже `400`.

> **`404` на этом пути не возникает никогда — и это нормативно.** По контракту v1.3 `404` означает
> ровно одно: «расширение не реализовано». Отдать его в ответ на кривой параметр значило бы сообщить
> CRM, что эндпоинта нет, и она перестала бы опрашивать бэк вовсе (`daily_costs_supported = false`).
> Поэтому невалидный период — `400`, а не `404` и не `422`.

## Экономика и настройки инстанса — контракт CRM v1.1 + v1.2 + v1.4 + v1.5 ([ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md))

> ⚠️ **Контракт заморожен НЕ здесь.** Пути, имена полей, значения перечислений, коды ответов и
> семантику отсутствия задала CRM (`broad-crm`: её `ADR-072` v1.1, `ADR-073` v1.2, `ADR-109` v1.4,
> `ADR-110` v1.5) и **из этого репозитория не меняются**: обе стороны пишутся разными командами в
> разных репозиториях, и расхождение в имени поля даёт у оператора **молча пустой экран**, а не
> ошибку. Всё, что решено на нашей стороне (где живут значения, какие строки мы объявляем, как
> продукт доходит до начисления), — [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md).
> Ссылки вида `CRM ADR-NNN` указывают на решения контрагента; `ADR-072`/`ADR-073` **в этом**
> репозитории — другие решения.

**Общее для всех восьми путей.** Заголовок и коды отказа — как в шапке документа
(`X-Admin-Key` / легаси `X-Admin-Token`). Тела и ответы — **snake_case** (контракт CRM), в отличие
от пользовательского API. Опциональный заголовок **`X-Admin-Actor`** (≤ 255 символов; CRM шлёт
`crm:<user_uuid>`) — **заявление, а не аутентификация**: значение ничем не подтверждается,
единственная аутентификация — admin-ключ; строить на нём отчёт «кто менял» нельзя. Списки **не
пагинируются**. Даты — ISO 8601 UTC. Ошибки — `{"detail": ...}`.

**Ключевая семантика кодов (нарушать нельзя — ломает чужой UI):**

| Ситуация | Код |
|---|---|
| Неизвестный `product_id` / `tariff_id` / `setting_id` **на реализованном пути** | **`400`** (не `404`) |
| Дубликат `product_id` на `POST /products` | **`400`** (не `409` — он занят конфликтом версий) |
| Конфликт `if_updated_at` на любом `PATCH` | **`409`** |
| Отказ по тому, что **мы сами объявили** (схема ручки, верхняя граница `limits`, `tariff_decimal_places`, `type`/`options`/`constraints`) | **`422`** |
| Отказ по правилу, которого **в нашем объявлении нет и быть не может** (нижняя граница `tokens`, непустой `avatar_tokens`, межэлементный инвариант настроек, неполнота источника продукта) | **`400`** |
| `404` | **не возникает ни на одном из этих путей** — он означает «расширение не реализовано» |

⚠️ **Предикат разведения `400`/`422` — один и проверяется механически** ([ADR-099 §11](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)):
**могла ли CRM отклонить это в своей форме по тому, что ей известно.** Практическая цена: `422` на
проде означает «клиентская проверка CRM не сработала», и ложное срабатывание обесценивает сигнал.
Оператор разницы не видит — CRM сводит оба кода в `backend_admin_bad_request` и показывает наш
текст транзитом; различение существует **для нас**.

### GET /v1/admin/products

Каталог продуктов инстанса: то, за что начисляются кредиты.

**Query:** `scope` = `grantable` (дефолт) \| `all`. На этом сервисе обе выборки совпадают: каждый
продукт каталога может быть выдан оператором вручную, поэтому `grantable` у всех строк — `true`.

**Response (200):** `{"items": [...]}`, элемент:

| Поле | Тип | Значение |
|---|---|---|
| `product_id` | str | Идентификатор продукта — **тот же, что в App Store Connect / панели поставщика** |
| `name` | str | Отображаемое название |
| `price` / `period` | str \| null | Витринные поля контракта v1; этот сервис достоверного прайса не имеет ⇒ всегда `null` |
| `tokens` | int \| null | Кредиты, начисляемые по этому продукту. ⚠️ **Отдаётся только при известном `purchase_kind`**, иначе `null`: без класса число не читает ни один резолвер начисления, а непустое `tokens` заставило бы CRM отрисовать карандаш правки, которую мы обязаны отвергнуть ([ADR-099 §6.1](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md), правило 4) |
| `avatar_tokens` | int \| null | **Всегда `null`** — второй валюты сервис не ведёт |
| `grantable` | bool | Всегда `true` (см. Query) |
| `purchase_kind` | str \| null | `subscription` \| `one_time` \| `null` (источник не позволяет определить) |
| `archived` | bool | Снят ли продукт с витрины приложения |
| `updated_at` | ISO \| null | `null` = «оператор эту строку ни разу не менял» |

**Источники строк и их слияние** (порядок фиксирован, первое вхождение выигрывает; строка
операторского оверлея накрывает любую из них по `product_id`):
`TOKEN_PRODUCTS` (`one_time`) → `ADAPTY_PRODUCT_TOKENS` (`subscription`) →
`CLOUDPAYMENTS_PRODUCT_TOKENS` (`subscription`) → `PRODUCTS_CATALOG` (витрина) →
созданные оператором (таблица `admin_products`).

### POST /v1/admin/products

Создаёт продукт в каталоге инстанса.

**Request:** `{"product_id": str, "name": str, "purchase_kind": "subscription"|"one_time", "tokens": int, "avatar_tokens": absent}`

- `product_id` — непустая строка ≤ 128 символов без пробельных символов. **Вводит оператор**: это
  ключ связи с платёжным провайдером, и придуманный сервисом идентификатор не получил бы ни одной
  покупки. Проверить, заведён ли он в сторе, сервис не может и не пытается: «продукт работает»
  здесь означает ровно серверную сторону — инстанс отдаёт его в каталоге и начисляет по нему;
- `tokens` — `≥ 1` для `one_time` (пакет, не дающий ни одного кредита, — это покупка без предмета)
  и `≥ 0` для `subscription` (подписка вправе давать только доступ). ⚠️ **Коды двух границ
  РАЗНЫЕ, и оба помечены:** отрицательное значение нарушает **сам контракт** (`tokens: int≥0`) и
  отдаёт `422` штатным конвейером схемы; нижняя граница `≥ 1` для `one_time` — **наше собственное**
  правило, которого в `limits` нет и выразить нечем (оно зависит от `purchase_kind` того же тела),
  поэтому его нарушение — **`400`** с лейблом `undeclared_bound`
  ([ADR-099 §11](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)). Верхняя граница
  `product_tokens_max` объявлена в `limits` ⇒ `422`;
- `avatar_tokens` — **непустое значение отвергается `400`**: второй валюты сервис не ведёт, а
  молча принять и проигнорировать значит сообщить оператору о сохранении, которого не было.

**Response (201):** элемент продукта (в форме `GET`) + `effective_after_seconds` (int).
**Дубликат `product_id`** (в любом из источников выше) → **`400`** с текстом причины.

### PATCH /v1/admin/products/{product_id}

**Request:** `{"tokens": int?, "avatar_tokens": absent, "archived": bool?, "if_updated_at": ISO?}` —
хотя бы одно значимое поле (`tokens` \| `archived`).

**Response (200):** элемент продукта + `previous_tokens` (int \| null) + `changed` (bool) +
`effective_after_seconds` (int).

- **`PATCH` идемпотентен** и **никогда не создаёт продукт**: неизвестный `product_id` → `400`.
  Правка продукта, пришедшего из env, создаёт строку оверлея — это не создание продукта, а
  материализация правки уже существующей строки каталога;
- ⛔ **Правка одного `archived` не имеет ветки отказа НИ ПРИ КАКОЙ полноте источника** — ни `400`,
  ни `422`. Строка оверлея записывает **только** `archived`, а `name`/`purchase_kind`/`tokens`
  остаются пустыми со смыслом «оверлей этого поля не задаёт»
  ([ADR-099 §6.1](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)). Это норма
  контрагента, а не наша уступка: `CRM ADR-073 §4` прямо выводит контрол архива из-под правила
  «`tokens: null` ⇒ строка read-only»;
- ⚠️ **Соседнее правило ПРОТИВОПОЛОЖНО, помечены оба:** правка, несущая `tokens`, **требует**
  известного класса покупки (оверлея или источника) — число без класса не прочитал бы ни один
  резолвер начисления; класса нет ⇒ **`400` `source_kind_missing`**
  ([TD-043](../../100-known-tech-debt.md)). Переносить «по аналогии» ни одно правило на другое
  нельзя: у них разные адресаты (витрина против резолвера начисления);
- при правке одного `archived` `previous_tokens` = **текущее, неизменившееся** значение `tokens`;
  у строки без собственного числа это `null` — схема ответа контрагента толерантна и этот случай
  нормирует (`CRM ADR-073 §8`, тост без дельты), подставлять что-либо запрещено;
- `changed: false` — фактически ничего не изменилось (повтор того же значения).

**Что делает `archived: true`** (нормативно):

| Поверхность | После архивации |
|---|---|
| Витрина `GET /v1/tokens/products` | продукт **не предлагается** — во всех ветках источника |
| Все пять путей начисления (StoreKit consumable, CloudPayments one_time/subscription, Adapty, StoreKit-подписка) | **начисляют как прежде** — иначе архив ломал бы уже оплаченное |
| Ручная выдача плана оператором (`POST /v1/admin/users/{id}/subscription`) | **остаётся доступной** |
| Строка в `GET /v1/admin/products` | **остаётся**, `archived: true` |

⚠️ Вторая и третья строки — **не исключение, а другой адресат**: «перестал выдаваться» относится к
клиенту приложения. Копировать на них правило витрины «по аналогии» запрещено.

### GET /v1/admin/pricing

Тарифы списания: сколько кредитов стоит одно обращение. Инстанс отдаёт строку на **каждый**
поддерживаемый вариант, включая те, чья цена ещё не настраивалась (тогда `tokens` — действующее
значение). Варианта, которого нет в этом ответе, для CRM не существует.

**Response (200):** `{"items": [...]}`, элемент:

| Поле | Тип | Значение |
|---|---|---|
| `tariff_id` | str | **Opaque для CRM**, ключ пути `PATCH`. Схема — ниже |
| `kind` | str | `chat` \| `photo` \| `video` |
| `name` | str \| null | Человекочитаемая подпись варианта |
| `tokens` | number | Кредиты за одну единицу, названную в `unit` |
| `provider` | str \| null | `openai` \| `anthropic` \| `fal` |
| `model` | str \| null | Идентификатор модели |
| `unit` | str | `message` (чат) \| `image` (фото) \| `generation` (видео) |
| `options` | object \| null | Прочие измерения цены: `resolution`, `duration_seconds`, `audio` |
| `updated_at` | ISO \| null | `null` = «оператор эту строку ни разу не менял» |

**Схема `tariff_id`** (вычислимая координата варианта, стабильна между перезапусками):

| Вид | Схема | Пример |
|---|---|---|
| Чат | `chat:<provider>:<model_id>` | `chat:openai:gpt-4.1` |
| Фото | `photo:<model_id>:<resolution>` | `photo:nano-banana-2:0.5k` |
| Видео | `video:<model_id>:<resolution\|na>:<duration_seconds>:<audio 0\|1>` | `video:veo-3.1:1080p:8:1` |

Сегменты — `lower()`; длительность — целые секунды; отсутствующее у модели измерение — литерал
`na`.

**Единица объявляется честно** (и это половина смысла поля):

- **фото — `image`**: `tokens` = цена **одного** изображения в этом разрешении, итог запуска =
  `tokens × numImages` (1…4). Объявить «за генерацию» значило бы назвать единицу, по которой
  сервис не считает;
- **видео — `generation`**: `tokens` = **полная** цена запуска с этими параметрами. Длительность,
  разрешение и звук **развёрнуты в координаты строки**, поэтому множителей не остаётся ни одного;
  `unit: "second"` был бы ложью — сервис тарифицирует пачками (6 с и 8 с у Veo стоят одинаково), и
  делённое значение выглядело бы ценой секунды, ею не являясь. Длительность каждой строки видна в
  `options.duration_seconds`;
- **чат — `message`**: цена одного завершённого хода на этой модели. Режим генерации на цену **не
  влияет** и измерением не является.

**Строки чата выводятся из полного известного каталога моделей**, а не из настройки
`chat.models_offered`: модель, снятая с витрины, продолжает обслуживать уже созданные сессии, и
строка без тарифа означала бы списание по неотображаемой цене.

### PATCH /v1/admin/pricing/{tariff_id}

**Request:** `{"tokens": int, "if_updated_at": ISO?}`. **Response (200):** элемент тарифа +
`previous_tokens` + `changed` + `effective_after_seconds`.

- **`tokens ≥ 1`**, целое. Ноль отвергается **`400`** с лейблом `undeclared_bound`, а **не** `422`:
  контракт объявляет тело как `tokens: int≥0`, а наш `limits` несёт только верхнюю границу — то
  есть нижнюю границу CRM отклонить в форме не могла
  ([ADR-099 §11](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)). Сама граница
  сохраняется дословно: балансовый гейт ноль пропускает, а списание берёт ноль — генерация тихо
  становится бесплатной. ⚠️ **Соседние проверки того же поля с ДРУГИМ кодом, помечены обе:**
  отрицательное значение → `422` (нарушен сам контракт, реализуемый схемой ручки); выше
  `tariff_tokens_max` и дробное → `422` (обе границы объявлены в `limits`/`tariff_decimal_places`);
- неизвестный `tariff_id` → `400`; `PATCH` строк не создаёт;
- правка применяется **не мгновенно**: величина окна — в `effective_after_seconds`.

### GET /v1/admin/capabilities

**Response (200):**

```json
{
  "contract_version": 1,
  "features": ["products.read", "products.write_tokens", "products.write_archived",
               "products.create", "pricing.read", "pricing.write_tokens",
               "settings.write", "requests.costs"],
  "limits": {"product_tokens_max": 1000000,
             "tariff_tokens_max": 100000, "tariff_decimal_places": 0},
  "cache_effective_after_seconds": 30
}
```

- `features` — **единственный** источник, из которого CRM выводит право записи (fail-closed):
  значение объявляется только там, где путь реализован. Парной `settings.read` в контракте нет;
- `limits` — **runtime-данные этого инстанса**, а не константы контракта: заморожены только имена
  ключей и типы. **Ключей три.** `product_avatar_tokens_max` **не отдаётся вовсе**: предикат
  `limits` — наличие ключа, поэтому ключ даже со значением `0` объявил бы вторую валюту
  существующей и заставил бы CRM нарисовать поле «Аватар-токены», которое не может предложить ни
  одного значения. Отсутствие ключа — единственная форма высказывания «величины нет»;
- `cache_effective_after_seconds` = то же окно применения, что и `effective_after_seconds`.

### GET /v1/admin/settings

Продуктовые настройки инстанса. Поверхность **самоописываема**: состав, типы, подписи и допустимые
значения приходят в ответе, потребитель их не хардкодит.

**Response (200):** `{"items": [...]}`, элемент: `setting_id` (str, opaque, ключ пути `PATCH`),
`type` (`bool` \| `enum` \| `multi_enum` \| `string`), `label` (str), `value` (по типу),
`group` (str \| null), `description` (str \| null), `options` (`[{value,label}]` \| null),
`constraints` (`{max_length, min_items, max_items}` \| null), `readonly` (bool \| null),
`updated_at` (ISO \| null).

**Реестр — 14 величин** (`setting_id` / `type`); ⚠️ **ответ конкретного инстанса может быть уже**: строка, неприменимая к его конфигурации, отсутствует (см. пометку «только на Anthropic-инстансах» ниже), поэтому число элементов — не константа контракта:
`chat.default_model` (`enum`), `chat.models_offered` (`multi_enum`, `min_items: 1`),
`chat.advertised_generation_modes` (`multi_enum`, **`min_items: 1`**), `chat.reasoning_level` (`enum`),
`chat.anthropic_thinking_display` (`enum`, **только на Anthropic-инстансах**),
`chat.characters_enabled`, `chat.memory_enabled`, `chat.voice_input_enabled`,
`chat.code_tools_enabled`, `chat.media_tools_enabled` (все `bool`),
`chat.disabled_tool_families` (`multi_enum`), `moderation.enabled` (`bool`),
`moderation.block_categories` (**`string`**, CSV), `catalog.presets_default_locale` (`enum`).

**`options` не хардкодятся ни здесь, ни в CRM — они выводятся из единственного объявления в
коде** (`allowed_models_union()` для моделей, `GENERATION_MODE_ORDER`, `DISABLEABLE_TOOL_FAMILIES`,
`SUPPORTED_PRESET_LOCALES`, наборы нормализаторов `reasoning_level`/`thinking_display`). Перечень,
переписанный в документ, был бы вторым домом факта и протух бы молча: `SUPPORTED_PRESET_LOCALES`
уже содержит **три** значения (`en`, `ru`, `zh-Hans`), и инстанс на `zh-Hans` при перечне из двух
стал бы невозвратимым. По той же причине `moderation.block_categories` объявлена **`string`**:
словарь категорий принадлежит провайдеру модерации и меняется без нашего релиза.

**Две модельные строки ограничены ВКЛЮЧЁННЫМИ провайдерами** (`credits_providers()`): предложить
на OpenAI-инстансе Anthropic-модель без ключа значило бы дать выбор, дающий `5xx` на каждом ходе.
**Инвариант `chat.default_model` ∈ `chat.models_offered`** проверяется явно: правка, снимающая с
витрины текущую дефолтную модель, и правка дефолта на модель вне витрины **отвергаются** с текстом
причины (код дополнительно держит принудительное включение дефолта в каталог как второй барьер).
**Код отказа — в [§`PATCH …/settings/{setting_id}`](#patch-v1adminsettingssetting_id)**, где у
кодов этого документа единственный дом: повторять его здесь значило бы завести второе место, где
код можно забыть обновить.

Полное описание каждой величины, её точки применения и обоснование состава —
[ADR-099 §8](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md).

⛔ **Чего эта поверхность НЕ несёт и нести не будет:** ключей провайдеров, материала подписи JWT,
мастер-ключа шифрования BYOK, admin-секретов, секретов вебхуков, строк подключения к БД/Redis,
таймаутов, ретраев, размеров пулов и любых ресурсных лимитов; величин домена **продуктов и цен**
(их дом — `/products` и `/pricing`: не только числа кредитов, но и признаки строки каталога —
например `TOKEN_PRODUCTS_DEFAULT`, [Q-099-6](../../99-open-questions.md)); а также величин, небезопасных при некоторых своих значениях
(`CHAT_LEGACY_WEB_SEARCH_ENABLED`, `TOKEN_PRODUCTS_PRICE_MINOR_UNITS`). Перечень поимённо и
предикат отнесения — [ADR-099 §8.2](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md).
**Ручки «прочитай/запиши переменную окружения по имени» нет и не будет** — allowlist, который
отдаёт `GET /v1/admin/settings`, и есть техническая граница поверхности.

### PATCH /v1/admin/settings/{setting_id}

**Request:** `{"value": <по типу>, "if_updated_at": ISO?}`.
**Response (200):** элемент настройки + `previous_value` + `changed` (bool) +
`effective_after_seconds` (int).

- **Значение валидирует сервис**, а не CRM: допустимость зависит от конфигурации инстанса, которой
  CRM не видит. **Код отказа определён однозначно** ([ADR-099 §11](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)):
  - **`422`** — нарушено **наше собственное объявление** этой строки: значение не по `type`, вне
    `options`, вне `constraints`. Сюда же **пустой `chat.models_offered`** и **пустой
    `chat.advertised_generation_modes`** — обе строки объявляют `min_items: 1`. ⚠️ **Пустой список
    не означает «вернуть дефолт»** ни у одной `multi_enum`-строки: пустой env — это «оператор
    ничего не сказал», пустой оверлей — «оператор явно выбрал ничего»;
  - **`400`** — правило, которого в объявлении **нет и быть не может**: `chat.default_model` вне
    `chat.models_offered` (межэлементный инвариант — его нечем выразить в `constraints` одной
    строки, и CRM не могла отклонить такую правку в форме);
  - обе стороны CRM сводит в `backend_admin_bad_request` и показывает текст транзитом; различение
    нужно **нам**: `422` означает, что клиентская проверка не сработала по нашему же объявлению;
- неизвестный `setting_id` → **`400`**; `PATCH` настроек **не создаёт**;
- `PATCH` идемпотентен: повтор того же значения → `changed: false`.
- ⚠️ **`chat.advertised_generation_modes`: значение может вернуться НЕ ТЕМ, что отправлено, — и это
  единственная такая строка.** `general` — режим по умолчанию выпущенных сборок, поэтому он
  добавляется к присланному списку **на записи**: в оверлей ложится нормализованное значение,
  и `value` ответа `PATCH` (как и последующий `GET`) несёт `general`, даже если оператор его снял.
  Отказа здесь нет намеренно — `defaultGenerationMode` не является настройкой, и `400` не оставил
  бы оператору ни одного пути вперёд ([ADR-099 §8.1](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md)).
  Об этом же предупреждает `description` строки; CRM ничего специального не делает — она просто
  показывает вернувшееся `value`. Ни одна другая строка присланное значение не меняет.

### Правила, общие для всех пишущих вызовов

- **Сначала фиксируется факт, затем формируется ответ.** Правка коммитится и пишется в аудит
  **до** сборки тела ответа; ни одна ветка сборки ответа не имеет права поднять исключение раньше
  аудита — иначе правка состоялась, оператор видит ошибку, следа нет, и он повторяет операцию.
- **Аудит** — существующий механизм `audit_logs`, поле пользователя-цели опционально. Действия:
  `admin_product_created`, `admin_product_updated`, `admin_product_archived`,
  `admin_tariff_updated`, `admin_setting_updated`; деталь несёт дельту `<prev>-><new>` (обрезается
  до 200 символов на сторону). **Имя действия называет изменённое** — записать смену настройки
  действием тарифа запрещено. Секреты в аудит и логи не попадают.
- **Цена и число кредитов никогда не приходят из тела пользовательского запроса** — только из
  серверного источника (оверлей → env → дефолт кода). Оверлей заполняется по admin-ключу, а не
  пользовательским JWT.
- **OpenAPI-тексты** (`summary`, `description`, `Field(description=...)`) — по
  [08-api-documentation §R2ter](../../08-api-documentation.md#r2ter-лаконичность-user-facing-текстов-для-тестировщиков):
  без ссылок `ADR-`/`Q-`/`TD-` и без имён переменных окружения. Внутренняя мотивация — здесь и в
  [ADR-099](../../adr/ADR-099-crm-admin-economics-and-instance-settings.md).
