# Scheduled Chats — RBAC

- Роль `user`. Все операции строго для `sub`.
- Чужой `id` / чужой `sessionId` → `404` (не `403`), чтобы не раскрывать существование.
- Admin CRUD не вводится.
- Воркер исполняет только задачи с `user_id` строки; повышение привилегий запрещено.
