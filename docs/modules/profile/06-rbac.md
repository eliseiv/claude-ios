# Profile — RBAC

- Роль `user`. `GET`/`PATCH /v1/profile` оперируют строго строкой `users` для `sub`.
- `userId` в пути/теле (если есть) обязан совпадать с `sub` → иначе `403`.
- Нет admin-операций.
- `accountId` — публичное отображение, не используется для авторизации (всегда JWT `sub`).
