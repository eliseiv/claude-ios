# Module: Chat Orchestrator

- Статус: Реализован
- Ответственность: вызовы LLM-провайдера (Anthropic Messages / OpenAI Chat Completions через нейтральный `LLMClient`, [ADR-033](../../adr/ADR-033-llm-provider-abstraction.md)) + prompt caching (Anthropic-only), управление tool-loop state, формирование `status` ответа, реконструкция контекста сессии.

## Документы
- [00-overview.md](00-overview.md)
- [01-context.md](01-context.md)
- [02-api-contracts.md](02-api-contracts.md) — endpoints + строго типизированные tool-схемы
- [03-architecture.md](03-architecture.md)
- [04-data-model.md](04-data-model.md)
- [05-events.md](05-events.md)
- [06-rbac.md](06-rbac.md)
- [07-implementation-phases.md](07-implementation-phases.md)
- [09-testing.md](09-testing.md)

## DoD
Policy Engine вызывается перед каждой генерацией; tool-loop стабилен на нескольких шагах; tool payload строго типизирован; status=blocked машиночитаем; usage учитывается; mode=byok использует ключ пользователя; mode=credits инициирует списание.

## Changelog
- 2026-05-21: bootstrap (architect).
- 2026-05-21: ADR-006 — credits-debit правило (1 кредит = 1 сообщение, 1 списание на message-шаг; tool-раунды не списывают). Унифицированы usage-ключи `cacheReadTokens/cacheWriteTokens`. Закрыт Q-004-1.
- 2026-05-21: введён `messageStepId` — billing idempotency key, генерируется в `/chat/run`, персистится в `chat_steps.message_step_id`/`tool_calls.message_step_id`, переиспользуется при re-entry из `/chat/tool-result`. Разведён с gateway `requestId` (ADR-005).
- 2026-05-21: реализован (backend), тесты зелёные, ревью пройдено. Код: `src/app/chat/` (orchestrator, anthropic_client, repository, tools). Trial-flip атомарен и идемпотентен (`mark_trial_used`); окно двойной бесплатной генерации при гонке — осознанный риск, ADR-002 §Trial concurrency / TD-006.
- 2026-05-25: BUG-3 (CRITICAL) — dotted tool-имена (`files.read`, …) отклонялись Anthropic API (`400`, шаблон `^[a-zA-Z0-9_-]{1,128}$`), весь Claude-путь нерабочий. Решение: двунаправленный статический маппинг `domain (точка) ↔ anthropic (подчёркивание)`, применяется только в Anthropic-клиенте (at request build + at tool_use parse). Публичный iOS-контракт §5 (доменные имена с точкой) НЕ меняется. См. [02-api-contracts.md §Имена tools](02-api-contracts.md#имена-tools-доменный-ios-vs-anthropic-формат), [03-architecture.md §Маппинг имён tools](03-architecture.md#маппинг-имён-tools-bug-3). Scope backend.
- 2026-06-01: введён класс server-side tools (`site.*`, website-builder) — исполняет backend в tool-loop без round-trip к iOS ([ADR-011](../../adr/ADR-011-server-side-tools.md)). Orchestrator различает client-side/server-side по реестру `SERVER_SIDE_TOOLS`; guard `MAX_SERVER_TOOL_ROUNDS`. domain↔anthropic mapping расширен `site.*`. Схемы — [website-builder/02-api-contracts.md](../website-builder/02-api-contracts.md). Scope backend.
- 2026-06-02: TD-014 (observability, minor) — зафиксирован контракт логирования upstream-ошибок Anthropic. При перехвате ошибки SDK (`APIStatusError`/`APITimeoutError`/`APIConnectionError`/`AuthenticationError`) логировать структурированную запись (событие `anthropic_upstream_error`) с camelCase лог-ключами `status_code`/`errorType`/`errorMessage`/`anthropicRequestId`/`model`/`exceptionClass`, где значения `errorType`/`errorMessage`/`anthropicRequestId` берутся из полей-источника тела ошибки Anthropic `error.type`/`error.message`/SDK `request_id` (матрица WARNING(4xx)/ERROR(5xx+timeout)) **до** маппинга в `UpstreamError`; api-key/BYOK-ключ/user-content — под redaction. Error-contract наружу (502) не меняется. См. [03-architecture.md §Логирование upstream-ошибок Anthropic](03-architecture.md#логирование-upstream-ошибок-anthropic-td-014), [05-security.md](../../05-security.md), [01-architecture.md §Наблюдаемость](../../01-architecture.md#наблюдаемость). Scope backend.
- 2026-06-03: **реализован мультимодальный ввод (inline base64-вложения, [ADR-020](../../adr/ADR-020-inline-base64-attachments-mvp.md), заменяет транспорт [ADR-014](../../adr/ADR-014-multimodal-attachments.md)).** `ChatRunRequest.attachments[]` (опц., ≤10, классы image/document/text, allowlist `mediaType`, поля `type`/`mediaType`/`filename`/`data`). Маппинг: image→`image`-блок, PDF→нативный `document`-блок, text→`text`-блок с разметкой имени. Валидация: magic-bytes, лимиты до декодирования, base64-валидность, PDF page-guard, нет URL-fetch. **Реплей/хранение:** полные блоки — только на витке 0; `chat_steps.payload` хранит лёгкий плейсхолдер (НЕ base64); на tool-витках ≥1 тяжёлый контент не реплеится. Только `/chat/run` (первый user-turn), не `/chat/tool-result`. Биллинг неизменен (1 кредит). Повышенный body-лимит роута `/chat/run` (12 MB). Новые `ATTACHMENT_*` settings ([Q-020-1](../../99-open-questions.md)/[Q-020-2](../../99-open-questions.md)). SDK 0.39.0 не типизирует document-блок → dict-passthrough ([TD-016](../../100-known-tech-debt.md)). Двухшаговый upload/модуль `attachments` отложены ([TD-015](../../100-known-tech-debt.md)). См. [02-api-contracts.md §POST /v1/chat/run](02-api-contracts.md#post-v1chatrun), [03-architecture.md §Мультимодальные вложения](03-architecture.md#мультимодальные-вложения-inline-base64-adr-020), [04-data-model.md](04-data-model.md), [05-security.md §Мультимодальные вложения](../../05-security.md#мультимодальные-вложения--валидация-и-модель-угроз-adr-020), [06-testing-strategy.md](../../06-testing-strategy.md). Реализация: `src/app/chat/attachments.py` (валидация/маппинг/PDF page-guard), `src/app/schemas/chat.py::ChatRunRequest.attachments`, интеграция в `orchestrator.py` (full-content виток 0, плейсхолдер в `chat_steps.payload`), per-route body-лимит в `SizeLimitMiddleware`. Реально добавлена зависимость `pypdf>=5,<6` (`pyproject.toml`/`uv.lock`). Код реализован, reviewer approve, `production_ready=true`. Остаётся открыт [TD-016](../../100-known-tech-debt.md) — **live-подтверждение PDF document-блока (e2e с реальным Anthropic) обязательно после восстановления org Anthropic.** Scope backend.
- 2026-06-04: **спроектирован опциональный `projectId` + гейтинг `site.*` ([ADR-022](../../adr/ADR-022-optional-project-and-tool-gating.md), docs-only — код не написан).** Позиционирование: сервис — чат-агрегатор; website-builder — опциональная фича. `ChatRunRequest.projectId` → опционален (`str | None`); `chat_sessions.project_id` → nullable (миграция `0007`, `DROP NOT NULL`, expand-only). Без `projectId` → «чистый чат»: `site.*` (`SERVER_SIDE_TOOLS`) НЕ предлагаются Claude (`anthropic_tool_definitions()` фильтруется по наличию `project_id` сессии); прочие client-side tools доступны. `projectId` фиксируется на сессию (как `mode`/`assistantMode`), при resume берётся из сессии — поле запроса игнорируется. Биллинг/policy без изменений (1 кредит = 1 сообщение). Защита: `_external_project_id()` вызывается только при непустом `project_id`; `site.*` без проекта — upstream-аномалия (не исполняется). См. [02-api-contracts.md §POST /v1/chat/run](02-api-contracts.md#post-v1chatrun), [03-architecture.md §Гейтинг site.* tools](03-architecture.md#гейтинг-site-tools-по-наличию-проекта-adr-022), [04-data-model.md](04-data-model.md). Next: backend (схема + миграция `0007` + фильтрация tools + guard), qa. Scope backend.
- 2026-06-04: BUG-5 (CRITICAL) — реконструкция диалога server-side tool-loop (`site.*`) ломалась с реальным Claude. `tool_use` и `tool_result` пишутся в одной транзакции → равный transaction-time `created_at`; `list_steps` сортировал по `(created_at, id)`, UUID-tie-break случайно ставил `tool_result` раньше `tool_use` → orphan tool_result → Anthropic `400` → `502`. Client-side не затронут (разные транзакции). Решение ([ADR-021](../../adr/ADR-021-deterministic-step-order-and-block-normalization.md)): новая колонка `chat_steps.seq BIGINT GENERATED ALWAYS AS IDENTITY` (монотонный порядок вставки); `list_steps`/`next_step_after` сортируют по `seq`, НЕ `created_at`; индекс `ix_steps_session_seq`. Дополнительно — нормализация content-блоков перед персистом: служебное SDK-поле `caller` (из `block.model_dump()`) вырезается, в `chat_steps.payload`/реплее только wire-валидные поля Anthropic (raw `tool_use.id` сохраняется, ADR-008 цел). Миграция `0006` (add column + backfill по `(created_at,id)` + NOT NULL + индекс), цепочка `0001`→`0006`. Контракт `/chat/run` наружу НЕ меняется. См. [03-architecture.md §Детерминированный порядок шагов](03-architecture.md#детерминированный-порядок-шагов-и-нормализация-payload-adr-021), [04-data-model.md](04-data-model.md), [09-testing.md](09-testing.md). Scope backend + qa.
- 2026-06-05: **спроектированы id синхронизации в `ChatResponse` ([ADR-023](../../adr/ADR-023-sync-ids-in-chat-response.md), docs-only — код не написан).** Аддитивно добавлены два nullable-поля: `messageStepId` (= `chat_steps.message_step_id`, ключ хода, стабилен через tool-loop) и `stepId` (= `chat_steps.id`, id конкретного assistant/tool-шага этого ответа). Семантика: `assistant_message`/`tool_call` → оба присутствуют (`stepId` = id финального assistant-шага / assistant-шага с `tool_use`); `blocked` → оба `null` (шаг/ход не создаются, блок до генерации); `/chat/tool-result` → `messageStepId` стабилен в рамках хода (из `tool_calls.message_step_id` по `toolCallId`), `stepId` = id нового шага. Инвариант: совпадают с `steps[].messageStepId`/`steps[].id` в `GET /v1/chats/{id}` ([chats/02-api-contracts.md](../chats/02-api-contracts.md#get-v1chatsid)). `toolCall.id` не меняется (≠ `stepId`, ADR-008 цел). Обе величины уже есть в orchestrator (message_step_id генерится на ход; id шага — PK `ChatStep`) — фикс чисто контрактный (без миграции/биллинга/policy). См. [02-api-contracts.md §Response (200)](02-api-contracts.md#response-200). Next: backend (добавить поля в `ChatResponse`, заполнять в orchestrator), qa. Scope backend + qa.
- 2026-05-25: BUG-4 (CRITICAL) — continuation сломан с реальным Claude. Доменный `toolCallId` выводился из anthropic `tool_use.id` (`uuid4`, если id не-UUID), а реплеемая история содержала raw `toolu_...` → `tool_result.tool_use_id` ≠ `tool_use.id` истории → Anthropic `400` → `502`. Решение ([ADR-008](../../adr/ADR-008-provider-tool-use-id.md)): новая колонка `tool_calls.provider_tool_use_id (TEXT NOT NULL)` хранит raw `toolu_...`; доменный `id` = независимый `uuid4`; continuation строит `tool_result.tool_use_id` из `provider_tool_use_id`, `payload` реплеится дословно. Миграция Alembic expand/contract. См. [03-architecture.md §Согласованность tool_use.id](03-architecture.md#согласованность-tool_useid-в-истории-anthropic-bug-4), [04-data-model.md](04-data-model.md), [09-testing.md](09-testing.md) (fake обязан отдавать `toolu_...`). Scope backend + qa fake.
- 2026-06-10: **спроектирован server-side global tool `time.now` + класс project-independent server-side tools ([ADR-026](../../adr/ADR-026-global-server-side-tools-and-time-now.md), docs-only — код не написан).** Репорт iOS: модель отвечает «2024 год» (системный промт статичен, даты нет → угадывает). Решение: новый класс **server-side global** (`GLOBAL_SERVER_SIDE_TOOLS = {time.now}`, отдельный от project-scoped `SERVER_SIDE_TOOLS` = `site.*`) — backend исполняет в tool-loop без round-trip к iOS, но **без проекта** (работает в основном flow «чистого чата», [ADR-022](../../adr/ADR-022-optional-project-and-tool-gating.md)). Маршрутизация в `_handle_tool_use`: ветка `GLOBAL_SERVER_SIDE_TOOLS` **до** `SERVER_SIDE_TOOLS`, исполнение **без** `external_project_id` (минует `assert external_project_id is not None`). `anthropic_tool_definitions(include_server_side=...)`: флаг гейтит только `site.*`; `time.now` предлагается **всегда**. Контракт: args `tz?` (IANA, опц., `extra=forbid`, ≤64); result — `utc`/`unix`/`weekday` всегда + `local`/`timezone` при валидном `tz`; невалидный `tz` → tool-result error `invalid_timezone` (ход не падает). Не мутирующий (нет audit-мутации). Биллинг неизменен (1 кредит = 1 сообщение, [ADR-006](../../adr/ADR-006-credit-billing-and-subscription-grant.md)). Системный промт (оба режима) — статичная EN-инструкция «нет встроенного знания даты → вызови time.now» (дата НЕ вписывается → prompt cache не ломается). Clock-провайдер (инъектируемый `Clock`+`SystemClock`/`FixedClock`) для детерминизма qa. Executor — отдельный `GlobalToolHandlers` (не привязан к WebsiteService). Каталог `/v1/tools` → 14 tools (`time.now`: `execution=server`, `mutating=false`). **Без миграции.** Открытые вопросы: [TD-019](../../100-known-tech-debt.md) (tzdata в slim-образе — devops), [Q-026-1](../../99-open-questions.md) (лимит длины `tz` / дефолтный tz). См. [02-api-contracts.md §time.now](02-api-contracts.md#timenow--server-side-global-tool-adr-026) / [§Классы tools](02-api-contracts.md#классы-tools-client-side-vs-server-side-adr-011-adr-026), [03-architecture.md §Global server-side tools](03-architecture.md#global-server-side-tools-и-timenow-adr-026), [02-tech-stack.md](../../02-tech-stack.md), [06-testing-strategy.md](../../06-testing-strategy.md). Next: backend (tools.py реестр+схема, global_tools.py, orchestrator маршрутизация+промт, DI), devops ([TD-019](../../100-known-tech-debt.md) tzdata), qa (Clock-контракт). Scope backend + devops + qa.
- 2026-06-12: **спроектирован `serverTools[]` в `ChatResponse` ([ADR-028](../../adr/ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md) Решение 2, аддитивно, docs-only — код не написан).** Репорт iOS: через `/chat/run` не видно, какие server-side инструменты (`site.*` [ADR-011](../../adr/ADR-011-server-side-tools.md) / `time.now` [ADR-026](../../adr/ADR-026-global-server-side-tools-and-time-now.md)) отработали — наружу только `assistantMessage`/client-side `toolCalls[]`. Решение: новое опц. поле `ChatResponse.serverTools[]` (= `/chat/run` + `/chat/tool-result`) — элементы `{ toolName (dot), status: completed\|errored, summary?: string\|null }`. `summary` **компактный** (≤`_SUMMARY_MAX_CHARS`=120, как steps-view; дефолт `"ok"`/короткий доменный итог; для `errored` — короткий код), **БЕЗ raw result / путей / URL / signed-token** (безопасность/размер); полный результат — в истории `GET /v1/chats/{id}` ([ADR-024](../../adr/ADR-024-history-payload-domain-normalization.md)). Семантика «за один вызов `/chat/run`» (не за сессию); накапливает все server-side выполнения tool-loop этого вызова. Присутствие: `assistant_message`/`tool_call` → присутствует (может быть `[]`); policy-`blocked` → `[]` (tool-loop не запускался); `max_tokens`-`blocked` → может быть НЕ пустым (server-side отработали до обрыва). Биллинг неизменен ([ADR-006](../../adr/ADR-006-credit-billing-and-subscription-grant.md)); каталог/число tools (14) не меняются; без миграции. Открыт [Q-028-1](../../99-open-questions.md) (формат `summary` единый vs per-tool). Backend: новая схема элемента в `src/app/schemas/chat.py` + `ChatResponse.serverTools`; аккумуляция server-side выполнений в `orchestrator.py::_generate_loop` (собирать `(toolName, status, summary)` в `_execute_server_side_tool`/`_execute_global_server_side_tool` или их вызовах, протаскивать через `_TurnOutcome`/`ChatRunOut` во все терминальные ветки — `_finalize_assistant`/client-side `tool_call`/`_handle_max_tokens`/`_blocked`-policy=`[]`), `summary` обрезать до `_SUMMARY_MAX_CHARS`, **не** класть raw. См. [02-api-contracts.md §Response (200)](02-api-contracts.md#response-200). Next: backend, qa. Scope backend + qa.
- 2026-06-16: **спроектирована провайдер-абстракция LLM (Anthropic | OpenAI, [ADR-033](../../adr/ADR-033-llm-provider-abstraction.md), docs-only — код не написан).** Цель — развернуть OpenAI-клон 3-м мульти-инстансом тем же кодом (не форк), провайдер по env `LLM_PROVIDER` (дефолт `anthropic` → `claude-ios`/`avelyra` без изменений). Нейтральный `LLMClient` (`src/app/chat/llm_client.py`): `AnthropicClient` (как есть) + `OpenAIClient` (Chat Completions, function-calling + vision, non-streaming); factory `get_llm_client()`; orchestrator/byok инжектят `LLMClient`. Нормализованный `stop_reason ∈ {tool_use, max_tokens, end_turn}` (OpenAI `tool_calls→tool_use`/`length→max_tokens`/`stop→end_turn`) — orchestrator диспетчеризует только по каноническим значениям (заменить строковые литералы Anthropic). **Граница orchestrator↔client (центральное):** провайдер-(де)сериализация wire-формата — внутри клиента; orchestrator передаёт нейтральные system_prompt/messages(history)/tools/attachments, клиент строит provider-messages + парсит ответ в нейтральный `LLMResult` + отдаёт `content_blocks` в wire-формате своего провайдера для персиста (инстанс одно-провайдерный → payload = wire активного провайдера). `provider_tool_use_id` ([ADR-008](../../adr/ADR-008-provider-tool-use-id.md)) обобщается (OpenAI `call_...`). **Tools:** underscore-map переиспользуется (точки запрещены и у OpenAI `^[a-zA-Z0-9_-]{1,64}$`); reverse-map OpenAI `function.name`→domain, `arguments` JSON-строка → parse в dict. **Attachments:** OpenAI image→`image_url` data-URI + text; **PDF→422** ([TD-023](../../100-known-tech-debt.md)). **BYOK:** OpenAI `validate_key` (401→invalid/network→offline/ok→valid); `OPENAI_BYOK_DEFAULT_MODEL`. **Config:** `LLM_PROVIDER`/`OPENAI_API_KEY`/`OPENAI_MODEL`(gpt-4o)/`OPENAI_MAX_TOKENS`/`OPENAI_TIMEOUT_SECONDS`/`OPENAI_MAX_RETRIES`/`OPENAI_BYOK_DEFAULT_MODEL`; `anthropic_*` обратносовместимы. **Observability:** `anthropic_upstream_errors_total`→`llm_upstream_errors_total` + label `provider`; OpenAI-ключ под redaction (покрыт). **Без миграции.** Открыты [Q-033-1](../../99-open-questions.md) ([TD-024](../../100-known-tech-debt.md), нейтральный формат персиста), [Q-033-2](../../99-open-questions.md) (Responses API), [Q-033-3](../../99-open-questions.md) (домен/ключ 3-го инстанса). См. [03-architecture.md §Провайдер-абстракция LLM](03-architecture.md#провайдер-абстракция-llm-anthropic--openai-adr-033), [02-tech-stack.md](../../02-tech-stack.md), [05-security.md §Провайдер LLM: OpenAI](../../05-security.md#провайдер-llm-openai-adr-033), [07-deployment.md §Мульти-инстанс](../../07-deployment.md#мульти-инстанс--клонирование-сервиса). **Указания backend (файлы/интерфейс/маппинги/контракт границы/config) — в JSON-вердикте architect.** Next: backend, devops (стек+`OPENAI_*`+3-й инстанс), qa. Scope backend + devops + qa.
- 2026-06-10: **спроектированы Fix A (max_tokens/обрезка) + Fix B (параллельные client-side tool-вызовы) ([ADR-025](../../adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md), docs-only — код не написан).** Прод-кейс broadnova: «сделай лендинг» → `assistant_message`/`toolCall=null`/`outputTokens=4096`, обрезанные `files.write` молча потеряны. **Fix A:** `ANTHROPIC_MAX_TOKENS` дефолт `4096`→`16000` (non-streaming, [TD-018](../../100-known-tech-debt.md) — streaming отложен), `ANTHROPIC_TIMEOUT_SECONDS` `60`→`120`; диспетчеризация ответа по `stop_reason` (не по наличию tool_use-блоков); `stop_reason="max_tokens"` → `status=blocked`, новый `blockReason=max_tokens` ([ADR-004](../../adr/ADR-004-blocked-http-200.md) enum расширен), неполные tool_use не отдаются, **кредит не списывается**, `usage`/`messageStepId`/`stepId` присутствуют (отличие от policy-blocked). **Fix B:** `ChatResponse.toolCalls[]` (все client-side tool_use хода; `toolCall` одиночный = `toolCalls[0]`, deprecated); `/chat/tool-result` батч `results[]` (одиночная форма — deprecated-алиас); барьер хода — continuation к Anthropic только когда собраны `tool_result` на все client-side вызовы хода; смешанный ход (server-side `site.*` исполняется сразу + client-side в `toolCalls[]`). Биллинг неизменен (1 кредит = 1 message-step, [ADR-006](../../adr/ADR-006-credit-billing-and-subscription-grant.md)); `messageStepId`/`stepId` ([ADR-023](../../adr/ADR-023-sync-ids-in-chat-response.md)) и нормализация истории ([ADR-024](../../adr/ADR-024-history-payload-domain-normalization.md)) согласованы (stepId хода один; toolCalls все из того же шага). Без миграции. См. [02-api-contracts.md §Response (200)](02-api-contracts.md#response-200) / [§POST /v1/chat/tool-result](02-api-contracts.md#post-v1chattool-result), [03-architecture.md §Параллельные client-side tool-вызовы](03-architecture.md#параллельные-client-side-tool-вызовы-и-барьер-хода-adr-025) / [§Обработка обрезки](03-architecture.md#обработка-обрезки-по-max_tokens-adr-025), [09-testing.md §Параллельные tool-вызовы + max_tokens](09-testing.md#integration--параллельные-tool-вызовы--max_tokens-adr-025), [02-tech-stack.md](../../02-tech-stack.md), [API-REFERENCE.md](../../API-REFERENCE.md). Next: backend (схемы `ChatResponse.toolCalls`/`ChatToolResultRequest.results`, диспетчеризация `stop_reason`, барьер хода, config-дефолты), devops (`ANTHROPIC_MAX_TOKENS=16000`/`ANTHROPIC_TIMEOUT_SECONDS=120` в `.env` обоих инстансов), qa. Scope backend + devops + qa.
