# Notifications — RBAC

- Роль `user`. Регистрация/удаление токена — строго для `sub` и его устройств.
- `userId` ≠ `sub` → `403`. Токен другого пользователя недоступен.
- Нет admin-операций.
- `push_token` — минимизация в логах (обрабатывается как чувствительный идентификатор устройства).
