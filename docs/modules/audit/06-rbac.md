# Audit — RBAC

## Роль
- Нет публичного доступа на старте. Запись — внутренние модули.

## Правила
- `record` — внутренний вызов, не через HTTP.
- Чтение аудита (admin) — out of scope bootstrap; при появлении — отдельная роль `admin` + ADR.
- Записи неизменяемы (append-only).
