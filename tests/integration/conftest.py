"""Фикстуры, общие для интеграционных тестов.

Стенд голосового режима подключается ЗДЕСЬ, а не импортом в каждом тест-файле: импортированная
фикстура и одноимённый параметр теста — это переопределение имени в модуле (`F811`), и обходить
его пришлось бы `noqa` в каждой сигнатуре. Сама реализация живёт в `tests/voice_harness.py`.
"""

from __future__ import annotations

from tests.voice_harness import voice_stand  # noqa: F401 — фикстура публикуется через conftest

__all__ = ["voice_stand"]
